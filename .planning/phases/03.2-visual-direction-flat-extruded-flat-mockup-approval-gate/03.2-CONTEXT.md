# Phase 3.2: Visual Direction Mockup + Approval Gate (Flat / Extruded-Flat) - Context

**Gathered:** 2026-05-04 (auto-drafted from Phase 3 redirect; ready for `/gsd-discuss-phase 3.2` refinement after Phase 3.1 completes)
**Status:** Blocked on Phase 3.1 completion (research is the input)

<domain>
## Phase Boundary

Replace the redirected Phase 3 visual direction with **flat MD3 / MD3 Expressive** language and optional **extruded-flat 3D raised** variation per the Flat-3D Game UI pattern. Approval-gate **5 themes × 4 variations** (flat × raised × desktop × mobile) before any `.tres` styling work begins. Hard gate.

**Architecture supports undefined number of themes** — v1 ships **N approved themes × 4 variations** where N is decided by the user at the gate (typical N: 1-3 for v1).

**In scope:**
- 5 named themes produced as flat MD3 mockups (no textures, no patterns, no embossing, no painterly chrome, no gradients)
- Each theme delivered in 4 variation mockups: `flat-desktop`, `flat-mobile`, `raised-desktop` (extruded-flat), `raised-mobile` (extruded-flat)
- Two-stage funnel: 5 lighter concept boards (flat MD3 only) → 1-3 finalists with full-fidelity mockups (all 4 variations each)
- User feedback log from Phase 3 redirect applied: keep elements user liked (Midnight Marquee colors / Cabinet Chrome colors / Prize Pop Plaza personality / Orbital Playdeck modern-iOS feel); drop Boardwalk Sunset entirely; reinterpret all surviving directions through the flat-MD3 filter
- 5th theme research-derived from Phase 3.1 (MD3 + MD3 Expressive findings)
- `DESIGN_TOKENS.md` finalized with token-matrix architecture (per-theme color/type/shape blocks + per-variation override blocks + per-platform overrides)
- Anti-cyberpunk + anti-texture filter audit on every finalist
- Hard blocker preserved: no `.tres` styling commits, Phase 4 cannot start until written approval

**Out of scope:**
- Any `.tres` styling commits (preserved hard rule)
- Implementing the `@tool` generator (Phase 4)
- Per-Control authoring (Phases 5-7)
- Re-running Phase 3.1 research (treats Phase 3.1 outputs as canonical input)
- Re-rendering Phase 3 v0 painterly concept images for v0 historical preservation (those are immutable)

</domain>

<decisions>
## Implementation Decisions (auto-drafted; refine via `/gsd-discuss-phase 3.2` after Phase 3.1)

### Theme Lineup (preliminary; user may revise after Phase 3.1)

- **D-01:** 5 themes produced. Names tentative until Phase 3.1 + concept boards produce stronger candidates:
  - **Midnight Marquee (re-rendered)** — colors loved by user; flat MD3 reinterpretation, no 3D arcade textures
  - **Cabinet Chrome (re-rendered)** — colors loved by user; flat MD3 dark professional reinterpretation
  - **Prize Pop Plaza (re-rendered)** — personality loved by user; flat MD3 + extruded-flat raised becomes the playful/colorful theme (the "raised" variation matters most here per user's "raised buttons preferable" feedback)
  - **Orbital Playdeck (re-rendered)** — modern-iOS feel loved by user; flat MD3 dark minimal reinterpretation
  - **5th theme (research-derived)** — discovered through Phase 3.1's MD3 Expressive work; should fill a gap the other 4 don't cover (e.g., light theme stub for v2 prep, or a strongly playful MD3-Expressive-saturated direction)
- **D-02:** Boardwalk Sunset is **dropped**. Phase 3.2 does not produce a flat-MD3 reinterpretation of Boardwalk Sunset (the user explicitly rejected it).
- **D-03:** Theme names may change at the discuss-phase or planning step if Phase 3.1 research surfaces stronger naming aligned with MD3 / MD3 Expressive personality.

### Variation Matrix (locked by user redirect)

- **D-04:** Each theme has 4 variation mockups + 4 `.tres` outputs (Phase 4 implements):
  - `{theme}_flat_desktop.tres` — flat surface, standard desktop sizes (Button 32px, body 14px)
  - `{theme}_flat_mobile.tres` — flat surface, mobile sizes (Button 48px, body 16px, +50% spacing)
  - `{theme}_raised_desktop.tres` — extruded-flat (offset darker shape duplicate), desktop sizes
  - `{theme}_raised_mobile.tres` — extruded-flat, mobile sizes
- **D-05:** Flat variation: `shadow_size = -1` on every StyleBoxFlat (Conflict 3 carry-forward, no shadow at all).
- **D-06:** Raised variation: `shadow_color` = darker-tone version of the surface color, `shadow_offset = Vector2(0, N)` where N ∈ {2, 4, 6} per Control class (smaller for chrome, larger for buttons), `shadow_size = 0` (no blur — pure offset shape per Flat-3D-UI pattern). Specific values determined by Phase 3.1 FLAT-3D-UI-RESEARCH.md recipes.
- **D-07:** Desktop and mobile variations differ in: `Button.minimum_size.y` (32 vs 48), body `font_size` (14 vs 16), spacing scale (mobile +50% on space.4+), tap target floors (mobile ≥48px on all interactive Controls). Corner radii STAY IDENTICAL across desktop/mobile (brand identity rule from CROSS-PLATFORM.md).

### Gallery + Approval Workflow

- **D-08:** **Step 1 — Concept boards:** 5 lighter-weight flat MD3 concept boards (one per theme, flat variation only — no raised yet). User picks 1-3 finalists.
- **D-09:** **Step 2 — Finalist full-fidelity mockups:** For each finalist, produce HTML mockups for all 4 variations: flat-desktop, flat-mobile, raised-desktop, raised-mobile. Every Control × every state combination (including `pressed_focus`, `checked_focus`, `hover_pressed` per Pitfall 1.1).
- **D-10:** **Step 3 — Approval & DESIGN_TOKENS.md:** User approves N final themes (N ≥ 1). DESIGN_TOKENS.md captures:
  - Per-theme: palette block (M3 5-stop tonal ramp + accent colors with WCAG AA pairs verified), optional shape/personality overrides
  - Per-variation: override block (flat → no shadow; raised → shadow_color/shadow_offset; mobile → spacing/sizing/tap-targets)
  - Per-platform: minimum sizes + base font sizes
  - M3 type scale (10 entries with concrete sizes + Inter weights)
  - State-layer model (M3 deterministic — hover 8% / focus 12% + 2px ring / pressed 12% / disabled 38%/12%)
  - Token-matrix structure that Phase 4 generator consumes directly
- **D-11:** **Up to 3 targeted revision rounds.** If not approved by round 3, escalation discussion before proceeding (mirroring Phase 3 v0 D-25).

### Anti-Cyberpunk + Anti-Texture Filter

- **D-12:** Every finalist mockup audited against:
  - Phase 2 anti-cyberpunk patterns (no synthwave / no neon-noir / no dystopian / no scanline)
  - **NEW (2026-05-04):** anti-texture rules — no textures, no patterns, no embossing, no painterly/leather/wood/grunge backgrounds, no gradients on chrome
- **D-13:** Audit happens **before** finalist selection AND before final approval. Two checkpoints, not one.

### Mobile + Cross-Platform

- **D-14:** Mobile mockups at **360×800** (smallest common phone) **+ 768×1024** (small tablet) viewports per CROSS-PLATFORM.md. Tap-target overlay visible (≥48px on every interactive Control).
- **D-15:** Real-arcade reference mood-board content (from Phase 3 v0) carries forward as **palette/personality DNA only** — surface treatment is reset to flat per redirect. Mood-board references with tagged `texture` or `painterly` extraction notes are demoted to "color-only" extraction.

### MD3 Expressive Affordances

- **D-16:** Phase 3.1 research will surface specific MD3 Expressive principles (more saturated colors, stronger personality). Phase 3.2 mockups SHOULD use them where appropriate (especially the playful theme like Prize Pop Plaza re-rendered) — MD3 Expressive is the answer to the user's "colorful and playful and expressive" desire.

### Token Architecture (informational; Phase 4 implements)

- **D-17:** TokenSet matrix structure (Phase 3.2 documents; Phase 4 codes):
  ```
  TokenSet:
    base:           // shared across all themes/variations
      type_scale, state_layer_model, focus_ring_strategy, ...
    themes:
      midnight_marquee_flat:
        colors: {surface, primary, ...}
        shapes: {corner_radius_default, ...}
      cabinet_chrome_flat:
        ...
      ...
    variation_overrides:
      raised:        // applied to raised .tres outputs
        shadow: {color_fn: darken_surface_by_15, offset: Vector2(0, 4), size: 0}
      mobile:        // applied to mobile .tres outputs
        sizes: {button_height: 48, body_font_size: 16, ...}
        spacing: {scale_factor_above_space_3: 1.5}
  ```
- **D-18:** Generator produces `themes/{theme}_{flat|raised}_{desktop|mobile}.tres` for every (theme × variation × platform) combination user approved. v1 ships `4 × N` `.tres` files where N is approved theme count.

### Claude's Discretion

- Concept-board image generator choice (continue with whatever was used in Phase 3 v0, or change based on Phase 3.1 findings)
- Exact mockup tool (HTML+CSS, Figma, etc.) — likely HTML+CSS for diffability
- Specific Control selection in concept boards (hero set vs comprehensive)
- 5th theme name and personality (after Phase 3.1 surfaces direction options)

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Direct Inputs (Phase 3.1 must complete before Phase 3.2 plans)
- `.planning/research/MD3-RESEARCH.md` (Phase 3.1 output) — flat MD3 + MD3 Expressive design language
- `.planning/research/FLAT-3D-UI-RESEARCH.md` (Phase 3.1 output) — extruded-flat raised variation patterns + Godot StyleBoxFlat recipes
- `.planning/research/SOURCES.md` Sections 11 + 12 (Phase 3.1 update)
- `.planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-CONTEXT.md`

### Project Canon (post-redirect)
- `.planning/PROJECT.md` — visual identity LOCKED 2026-05-04 (flat MD3, no textures, no gradients on chrome, optional extruded-flat raised variation, theme variation matrix)
- `.planning/ROADMAP.md` — Phase 3.2 success criteria + theme matrix architecture
- `.planning/STATE.md` — current state, redirect log, Phase 3 → 3.1/3.2 transition

### Phase 3 v0 Historical Reference (preserved, NOT binding for Phase 3.2)
- `.planning/phases/03-visual-direction-mockup-approval-gate/REDIRECTED.md` — full redirect explanation + user feedback log per direction
- `.planning/mockups/concepts/*.png` — 5 painterly concept images (DNA reference for color/personality only; surface treatment is reset)
- `.planning/mockups/03-direction-boards.html/.md` — v0 direction-board gallery (DNA reference only)
- `.planning/research/mood-board/INDEX.md` — 25 references; tagged extraction notes apply but `texture`/`painterly` content is downgraded to color-only

### Phase 1/2 Outputs (still authoritative)
- `.planning/research/MINIMAL-THEME-DISSECTION.md` — godot-minimal-theme coverage axis (37 user-facing classes)
- `.planning/research/MINIMAL-THEME-COVERAGE-DELTA.md` — 37-row scorecard + 8 NeoCade-additives
- `.planning/research/LDTK-UI-MINING.md` — 24 adopted patterns + Pattern → Control reverse index (Phase 2)
- `.planning/research/SUMMARY.md` — Conflict resolutions (esp. Conflict 3 → reframed: no shadows on flat variation; explicit shadow on raised variation)
- `.planning/research/PITFALLS.md` — Pitfalls 1.1 (focus-overlay, must show pressed_focus/checked_focus in mockups), 1.7 (popups separate Window, must theme all 8 popup classes)
- `.planning/research/CROSS-PLATFORM.md` — mobile spec (48px tap targets, 16px body, +50% spacing on space.4+; corner radii identical across desktop/mobile)
- `.planning/research/FONT-REVIEW.md` — Inter Variable Roman ONLY (Option D)

### Locked Cross-Cutting Constraints
- Inter Variable Roman ONLY for typography (Option D / FONT-REVIEW.md)
- GL Compatibility renderer is target (project.godot)
- 8 NeoCade-additive Controls (CodeEdit, FoldableContainer, SpinBox, ColorPickerButton, LinkButton, FileDialog, ConfirmationDialog, TooltipLabel) — no upstream benchmark; NeoCade owns the design
- 35-row coverage scorecard (37 numbered rows in COVERAGE-DELTA.md) — every class needs theme entry in v1
- Anti-cyberpunk discipline preserved
- **NEW 2026-05-04:** anti-texture / anti-pattern / anti-embossing / anti-gradient-on-chrome rules

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `.planning/mockups/concepts/` — 5 v0 painterly concept images (DNA reference only)
- `.planning/mockups/03-direction-boards.html` — v0 HTML direction-board template (structure may inform Phase 3.2 mockup HTML, but visual content is reset)
- `addons/neocade_theme/neocade_theme.tres` — empty scaffold (untouched; do not modify in Phase 3.2)
- `main.tscn` — scaffold scene; eventually a target for `/gsd-execute-phase` showcase work but NOT Phase 3.2

### Established Patterns
- Phase 3 v0 used HTML+CSS mockups rendered via Playwright/Edge for desktop/mobile screenshots — that workflow carries forward
- Two-stage funnel (concept boards → finalist mockups) carries forward from Phase 3 v0
- Anti-cyberpunk filter audit pattern from Phase 2 carries forward and is extended with anti-texture rules

### Integration Points
- `.planning/mockups/3.2/` — new mockup directory for Phase 3.2 outputs (do NOT overwrite v0 in `.planning/mockups/concepts/` or `03-direction-boards.*`)
- `.planning/DESIGN_TOKENS.md` — final output, consumed by Phase 4 generator
- Phase 4 plan input: token-matrix architecture documented in DESIGN_TOKENS.md

</code_context>

<specifics>
## Specific Ideas

- **The user wants 5 themes again, in 2 visual variations × 2 platforms = 4 .tres each.** Architecture supports undefined N themes; v1 ships whatever subset user approves.
- **Themes the user explicitly liked elements of:** Midnight Marquee colors (LDtk-like), Cabinet Chrome colors, Prize Pop Plaza personality (childish/friendly/raised buttons OK), Orbital Playdeck modern-iOS-dark feel.
- **Theme the user rejected:** Boardwalk Sunset (too warm, leather, painterly).
- **The "extruded flat" raised variation matters most for Prize Pop Plaza-style themes** where bubbly/jelly raised buttons enhance the playful character. Modern-iOS dark themes (Orbital Playdeck) may de-emphasize raised, prefer subtle.
- **Anti-texture is a hard rule.** No exceptions, even on background panels. Backgrounds are flat solid colors only. This is the single biggest delta from Phase 3 v0.
- **MD3 Expressive's increased saturation + personality** is the design language affordance for "colorful + playful + expressive" without textures. Phase 3.1 surfaces specifics.

</specifics>

<deferred>
## Deferred Ideas

- **Light theme variants** — deferred to v2; a 5th theme MAY include a light-MD3 stub for future-proofing the architecture, but v1 does not ship a light theme.
- **MD3 dynamic color** (user-chosen-color → tonal-palette algorithm) — informational only in DESIGN_TOKENS.md; v1 ships fixed per-theme palettes.
- **Motion / animation tokens** — already deferred (anti-feature). DESIGN_TOKENS.md may document M3 motion tokens informationally; v1 has no motion.
- **Whether to ship all 5 themes in v1** — user-gate decision at Step 3 approval. Likely 1-3 themes ship in v1; the rest become v1.x candidates.
- **Re-rendering all 25 mood-board references through anti-texture filter** — out of scope for Phase 3.2; existing mood-board carries forward with surface-extraction notes downgraded to color-only.

</deferred>

---

*Phase: 3.2-Visual Direction Mockup + Approval Gate (Flat / Extruded-Flat)*
*Context auto-drafted: 2026-05-04 (post-Phase-3 redirect)*
*Refine via `/gsd-discuss-phase 3.2` after Phase 3.1 completes; or proceed directly to `/gsd-plan-review-convergence 3.2 --opencode` if context is satisfactory.*

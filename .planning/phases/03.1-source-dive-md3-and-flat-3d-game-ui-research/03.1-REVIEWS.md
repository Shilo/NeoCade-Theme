---
phase: 3.1
review_cycle: 2
reviewers: [opencode, claude]
reviewed_at: 2026-05-05T04:22:06Z
replan_commit: 2b64eb382a725f9c252f6952838faf13d73288db
plans_reviewed:
  - 03.1-01-provenance-and-artifact-skeletons-PLAN.md
  - 03.1-02-md3-foundations-and-godot-mapping-PLAN.md
  - 03.1-03-md3-expressive-synthesis-PLAN.md
  - 03.1-04-flat-3d-source-survey-and-pattern-catalogue-PLAN.md
  - 03.1-05-raised-matrix-and-stylebox-recipes-PLAN.md
  - 03.1-06-sources-and-cross-doc-audit-PLAN.md
current_high: 0
---

# Cross-AI Plan Review — Phase 3.1 Cycle 2

## Cycle Context

Cycle 1 raised 2 HIGH concerns:

1. JS-rendered/hard-to-extract source fallback was under-specified.
2. Verification relied too much on keyword presence instead of citation quality, numeric correctness, and source agreement.

Replan commit 2b64eb382a725f9c252f6952838faf13d73288db claims to address both by adding JS-rendered source fallback chains, provenance labels across Plans 01-06, numeric MD3 checks, source agreement notes, URL/citation floors, visual-capture requirements, survey quotas, and hard-fail PowerShell checks.

## OpenCode Review

> build · deepseek-v4-pro

→ Read .planning\phases\03.1-source-dive-md3-and-flat-3d-game-ui-research\03.1-01-provenance-and-artifact-skeletons-PLAN.md
→ Read .planning\phases\03.1-source-dive-md3-and-flat-3d-game-ui-research\03.1-02-md3-foundations-and-godot-mapping-PLAN.md
→ Read .planning\phases\03.1-source-dive-md3-and-flat-3d-game-ui-research\03.1-03-md3-expressive-synthesis-PLAN.md
→ Read .planning\phases\03.1-source-dive-md3-and-flat-3d-game-ui-research\03.1-04-flat-3d-source-survey-and-pattern-catalogue-PLAN.md
→ Read .planning\phases\03.1-source-dive-md3-and-flat-3d-game-ui-research\03.1-05-raised-matrix-and-stylebox-recipes-PLAN.md
→ Read .planning\phases\03.1-source-dive-md3-and-flat-3d-game-ui-research\03.1-06-sources-and-cross-doc-audit-PLAN.md

$ git log --oneline -5
2b64eb3 docs(03.1): address plan review concerns
7b17928 docs: cross-AI review for phase 3.1
5aafc28 docs(03.1): create MD3 and flat-3D research plans
aa10234 docs(state): correct stale fields after Phase 3.1 discuss-phase — fix percent (80→14), refresh status/Current Position, update last_activity
3599829 docs(state): record phase 3.1 context session

$ git diff --stat HEAD~2..HEAD
....1-01-provenance-and-artifact-skeletons-PLAN.md |  21 +-
 ....1-02-md3-foundations-and-godot-mapping-PLAN.md |  34 ++-
 .../03.1-03-md3-expressive-synthesis-PLAN.md       |  33 ++-
 ...-3d-source-survey-and-pattern-catalogue-PLAN.md |  31 ++-
 ...1-05-raised-matrix-and-stylebox-recipes-PLAN.md |  29 ++-
 .../03.1-06-sources-and-cross-doc-audit-PLAN.md    |  40 ++-
 .../03.1-REVIEWS.md                                | 285 +++++++++++++++++++++
 7 files changed, 442 insertions(+), 31 deletions(-)

Now let me verify the replan commit to confirm what actually changed from Cycle 1:
$ git diff HEAD~1..HEAD -- ".planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-02-md3-foundations-and-godot-mapping-PLAN.md"
diff --git a/.planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-02-md3-foundations-and-godot-mapping-PLAN.md b/.planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-02-md3-foundations-and-godot-mapping-PLAN.md
index 22709e3..cdb2ecb 100644
--- a/.planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-02-md3-foundations-and-godot-mapping-PLAN.md
+++ b/.planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-02-md3-foundations-and-godot-mapping-PLAN.md
@@ -17,6 +17,8 @@ must_haves:
     - "D-13 is covered by implementation-depth limits: formulas/recipes are specific enough for Phase 3.2/4 but no architecture validation or code structure is decided"
     - "D-14 is covered by composed mappings for SpinBox, CodeEdit, GraphEdit, GraphNode, and Godot-additive controls instead of forced fake Material equivalents"
     - "The MD3 state layer values required by ROADMAP.md are captured: hover 8%, focus 12% plus 2px ring, pressed 12%, dragged 16%, disabled 38% content / 12% container"
+    - "JS-rendered MD3 pages use a concrete fallback chain: official URL retained, accessible upstream/source equivalent checked, manual/browser verification labeled, and unresolved gaps explicitly recorded instead of inferred from memory"
+    - "Verification checks citation density, all required numeric state-layer values, and source agreement rather than only keyword presence"
   artifacts:
     - .planning/research/MD3-RESEARCH.md
   key_links:
@@ -61,6 +63,17 @@ Required official/source URLs:
 - `https://docs.godotengine.org/en/stable/classes/class_styleboxflat.html`
 - `https://docs.godotengine.org/en/stable/classes/class_theme.html`
 - `https://docs.godotengine.org/en/stable/tutorials/ui/gui_theme_type_variations.html`
+
+Source-access fallback chain:
+1. Try official Material page URL and record whether content was directly extracted.
+2. If the official page is JS-rendered or incomplete, use Material Web token/source files or Android Developers pages as corroborating fallback sources and record exact URL/path plus retrieved date.
+3. For Godot APIs, verify against current official Godot docs and, when available in the runtime, Context7 MCP/library docs; record source-access status in provenance.
+4. If a required value cannot be corroborated by an accessible source, mark it `unresolved extraction gap` with the missing value named. Do not fill gaps from training memory.
+
+Minimum citation-quality gates:
+- At least 10 distinct source URLs across color, type, shape, state, elevation, motion, and Godot API notes.
+- Every numeric token/value row includes source URL, evidence role, access status, retrieved date, and source agreement note.
+- State-layer values must include hover 8%, focus 12% plus 2px ring, pressed 12%, dragged 16%, disabled content 38%, and disabled container 12%.
 </interfaces>
 </context>
 
@@ -81,16 +94,19 @@ Required official/source URLs:
     - Elevation: MD3 tonal elevation as inspiration only; NeoCade adopts color/tonal depth but keeps soft shadows disabled per project conflict resolution.
     - Motion: informational only; no motion token implementation in v1 Theme resources.
 
-    Use accessible upstream token implementation pages where the official m3 site is JavaScript-rendered. Mark any manually verified or browser-only pages as such in the provenance table.
+    Use the source-access fallback chain from `<interfaces>` where the official m3 site is JavaScript-rendered or incomplete. Keep the official m3 URL in provenance, then add the accessible upstream/source equivalent that corroborates the fact. Mark each source row with one of the skeleton source-access labels and explicitly write `unresolved extraction gap` for any required value that cannot be corroborated. Do not fill missing numeric facts from training memory.
   </action>
   <verify>
     ```powershell
-    Select-String -Path .planning\research\MD3-RESEARCH.md -SimpleMatch "key colors"
-    Select-String -Path .planning\research\MD3-RESEARCH.md -SimpleMatch "displayLarge"
-    Select-String -Path .planning\research\MD3-RESEARCH.md -SimpleMatch "hover 8%"
-    Select-String -Path .planning\research\MD3-RESEARCH.md -SimpleMatch "focus 12%"
-    Select-String -Path .planning\research\MD3-RESEARCH.md -SimpleMatch "disabled 38%"
-    Select-String -Path .planning\research\MD3-RESEARCH.md -SimpleMatch "motion"
+    $content = Get-Content -Raw .planning\research\MD3-RESEARCH.md
+    foreach ($needle in "key colors","displayLarge","hover 8%","focus 12%","pressed 12%","dragged 16%","disabled 38%","disabled container 12%","motion","source agreement") {
+      if ($content -notmatch [regex]::Escape($needle)) { throw "Missing MD3 foundation evidence: $needle" }
+    }
+    $urlCount = ([regex]::Matches($content, 'https?://')).Count
+    if ($urlCount -lt 10) { throw "Expected at least 10 source URLs in MD3 research; found $urlCount" }
+    foreach ($needle in "browser/manual verified","upstream-token corroborated","fallback source used","unresolved extraction gap") {
+      if ($content -notmatch [regex]::Escape($needle)) { throw "Missing source-access fallback label: $needle" }
+    }
     ```
   </verify>
   <done>MD3 foundations are documented with source roles, concrete values, and NeoCade-specific adoption/rejection notes.</done>
@@ -137,6 +153,7 @@ Required official/source URLs:
     - Focus: separate 2px solid focus ring and no glow.
     - Disabled: content alpha and container alpha handling as theme colors/styleboxes.
     - Surface ramp: map MD3 surface roles to NeoCade's locked tonal-ramp vocabulary without picking final hex values.
+    - Numeric correctness note: explicitly cross-check state-layer opacity values against the cited MD3/material-web or Android Developers source and record agreement/disagreement in the provenance or verification log.
   </action>
   <verify>
     ```powershell
@@ -152,6 +169,9 @@ Required official/source URLs:
 
 <verification>
 - [ ] MD3 foundations cover color, type, shape, state layers, elevation, and motion.
+- [ ] Source-access fallback status is recorded for official Material pages, upstream token sources, Android Developers corroboration, and Godot API docs.
+- [ ] Citation quality is checked with URL count, evidence-role labels, access-status labels, retrieved dates, and source-agreement notes.
+- [ ] All required state-layer numeric values are verified: hover 8%, focus 12% plus 2px ring, pressed 12%, dragged 16%, disabled 38% content / 12% container.
 - [ ] Component mapping covers all major Godot Control families and labels exact/composed/NeoCade-owned mappings.
 - [ ] Recipe notes are Godot-specific but implementation-neutral.
 - [ ] No `.tres`, `.gd`, font, icon, addon, or mockup file changed.

$ git diff HEAD~1..HEAD -- ".planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-04-flat-3d-source-survey-and-pattern-catalogue-PLAN.md"
diff --git a/.planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-04-flat-3d-source-survey-and-pattern-catalogue-PLAN.md b/.planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-04-flat-3d-source-survey-and-pattern-catalogue-PLAN.md
index 268dc1a..4e8a907 100644
--- a/.planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-04-flat-3d-source-survey-and-pattern-catalogue-PLAN.md
+++ b/.planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-04-flat-3d-source-survey-and-pattern-catalogue-PLAN.md
@@ -16,6 +16,8 @@ must_haves:
     - "The broad survey records accepted and rejected examples, with every adoption passing D-02 and D-08 hard filters"
     - "Pattern catalogue defines the valid extruded-flat construction as solid-color top shape plus offset darker duplicate; no texture, gradient, blur, soft shadow, bevel, or painterly chrome"
     - "Survey notes preserve D-01 broad-source posture while D-04 distinguishes authority from inspiration"
+    - "Itch.io and other hard-to-extract visual sources get source-access labels, manual/browser verification notes, and screenshot/image URL fields instead of being treated as citation-grade from URL presence alone"
+    - "Survey verification enforces row-type quotas, rejection count, citation quality, and hard-fail PowerShell checks"
   artifacts:
     - .planning/research/FLAT-3D-UI-RESEARCH.md
   key_links:
@@ -48,6 +50,13 @@ Minimum broad survey:
 - At least 6 additional public flat/extruded game UI examples or articles.
 - At least 2 rejected examples where the rejection reason is explicit.
 - Sources must be public URLs and labeled by evidence role.
+
+Source discovery guidance:
+- Seed platforms/searches: itch.io GUI/UI asset tags, Godot Asset Library GUI/theme examples, commercial mobile game UI screenshots/articles, and design commentary from design system/game UI sites.
+- Time-box: try 2-3 focused searches per platform before moving on; record low-yield platforms in the verification log rather than chasing indefinitely.
+- Quality quota: include at least 1 commercial example, at least 1 design-commentary source, at least 2 asset-pack examples, and at least 2 rejected examples.
+- Every survey row must include source URL, evidence role, source-access status, screenshot/image URL or visual-capture note, hard-filter audit, and adopt/reject/open decision.
+- Reject low-context image boards or listings that provide no inspectable visual evidence, unless they are used only as rejected examples with the low-confidence reason stated.
 </interfaces>
 </context>
 
@@ -62,6 +71,8 @@ Minimum broad survey:
   <action>
     Populate `## User Exemplar Provenance` with:
     - Source rows for both itch.io pages.
+    - Source-access status for each page, including browser/manual verification or fallback note if page content is hard to extract.
+    - Screenshot/image URL or visual-capture note for each exemplar because the judgment is visual.
     - What was read from each page.
     - What is adopted as visual vocabulary.
     - What is rejected or non-portable because it is a sprite-pack/PNG/vector-art workflow rather than a Godot Theme resource.
@@ -89,19 +100,27 @@ Minimum broad survey:
     Populate `## Broad Visual Survey` with at least 6 additional public examples. For each row include:
     - Source URL.
     - Evidence role.
+    - Source-access status.
+    - Screenshot/image URL or visual-capture note.
     - Observed flat/extruded pattern.
     - Adopt/reject/open decision.
     - Hard-filter audit notes.
 
-    Include at least 2 rejection rows for examples that use gradients, bevels, textures, soft shadows, cyberpunk/noir cues, or pixel-art styling.
+    Use the source discovery guidance from `<interfaces>`. Include at least 1 commercial example, at least 1 design-commentary source, at least 2 asset-pack examples, and at least 2 rejection rows for examples that use gradients, bevels, textures, soft shadows, cyberpunk/noir cues, or pixel-art styling. If a source is visually useful but text extraction is poor, label it `browser/manual verified` and describe the captured visual evidence.
   </action>
   <verify>
     ```powershell
     $content = Get-Content -Raw .planning\research\FLAT-3D-UI-RESEARCH.md
-    (($content | Select-String -Pattern "https?://" -AllMatches).Matches.Count) -ge 8
-    Select-String -Path .planning\research\FLAT-3D-UI-RESEARCH.md -SimpleMatch "REJECT"
-    Select-String -Path .planning\research\FLAT-3D-UI-RESEARCH.md -SimpleMatch "gradient"
-    Select-String -Path .planning\research\FLAT-3D-UI-RESEARCH.md -SimpleMatch "texture"
+    $urlCount = ([regex]::Matches($content, 'https?://')).Count
+    if ($urlCount -lt 8) { throw "Expected at least 8 total URLs including user exemplars; found $urlCount" }
+    foreach ($needle in "commercial example","design commentary","asset-pack example","source-access status","screenshot","visual-capture","hard-filter audit") {
+      if ($content -notmatch [regex]::Escape($needle)) { throw "Missing survey quality field: $needle" }
+    }
+    $rejectCount = ([regex]::Matches($content, '(?i)\bREJECT\b')).Count
+    if ($rejectCount -lt 2) { throw "Expected at least 2 rejected examples; found $rejectCount" }
+    foreach ($needle in "gradient","texture") {
+      if ($content -notmatch [regex]::Escape($needle)) { throw "Missing rejection/audit cue: $needle" }
+    }
     ```
   </verify>
   <done>Broad survey includes enough accepted/rejected examples to support the pattern catalogue.</done>
@@ -139,6 +158,8 @@ Minimum broad survey:
 <verification>
 - [ ] Both user exemplars are cited and analyzed.
 - [ ] Broad survey has at least 8 total URLs including the user exemplars.
+- [ ] Every survey row has evidence role, source-access status, screenshot/image URL or visual-capture note, hard-filter audit, and adopt/reject/open decision.
+- [ ] Survey includes at least 1 commercial example, 1 design commentary source, and 2 asset-pack examples.
 - [ ] At least 2 rejected examples are present.
 - [ ] Pattern catalogue defines accepted and forbidden construction.
 - [ ] No `.tres`, `.gd`, font, icon, addon, or mockup file changed.

$ git diff HEAD~1..HEAD -- ".planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-06-sources-and-cross-doc-audit-PLAN.md"
diff --git a/.planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-06-sources-and-cross-doc-audit-PLAN.md b/.planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-06-sources-and-cross-doc-audit-PLAN.md
index c7ef1ce..ad101b2 100644
--- a/.planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-06-sources-and-cross-doc-audit-PLAN.md
+++ b/.planning/phases/03.1-source-dive-md3-and-flat-3d-game-ui-research/03.1-06-sources-and-cross-doc-audit-PLAN.md
@@ -21,6 +21,7 @@ must_haves:
     - "D-15 is fully satisfied: MD3-RESEARCH.md, FLAT-3D-UI-RESEARCH.md, and SOURCES.md Sections 11/12 exist and cross-reference each other"
     - "D-16 is fully satisfied: both MD3-RESEARCH.md and FLAT-3D-UI-RESEARCH.md end with a Flat vs Raised: When to Use Which matrix"
     - "Final audit confirms RES-NEW-01, RES-NEW-02, and DOCS-05 coverage and confirms no `.tres` styling commits, no mockup commits, no addon/font/icon changes"
+    - "Final audit checks citation quality, source-access fallback coverage, numeric correctness, and cross-source agreement across MD3, MD3 Expressive, itch.io, Google blog, and broad-survey sources"
   artifacts:
     - .planning/research/SOURCES.md
     - .planning/research/MD3-RESEARCH.md
@@ -58,6 +59,17 @@ Each section must include:
 - What NeoCade rejects.
 - What remains open.
 - Confidence rating.
+- Source-access status.
+- Retrieved date.
+- Source agreement / conflict notes.
+- Fallback source used or unresolved extraction gap, where applicable.
+
+Final audit quality gates:
+- No `unresolved extraction gap` may remain without an explicit downstream owner or reason it is non-blocking.
+- Numeric MD3 state values must appear in MD3-RESEARCH.md and be cross-checked in the Phase 3.1 Verification Log: hover 8%, focus 12%, pressed 12%, dragged 16%, disabled 38% content / 12% container, plus 2px focus ring.
+- MD3/MD3 Expressive sources must include source-access status and source agreement notes.
+- Flat-3D survey rows must include evidence role, source-access status, screenshot/image URL or visual-capture note, hard-filter audit, and adopt/reject/open decision.
+- Anti-Cyberpunk and Anti-Texture Audit sections must contain populated findings, not only headings.
 </interfaces>
 </context>
 
@@ -72,7 +84,7 @@ Each section must include:
   <action>
     Append or insert `## 11. MD3 + MD3 Expressive sources (added 2026-05-05 by Phase 3.1)` before the summary section at the end of `SOURCES.md`.
 
-    Follow the existing dossier style: source name/location, what was read, what was adopted, what was rejected, what remains open, and confidence. Include official/spec sources and upstream token sources from `MD3-RESEARCH.md`.
+    Follow the existing dossier style: source name/location, what was read, what was adopted, what was rejected, what remains open, and confidence. Include official/spec sources and upstream token sources from `MD3-RESEARCH.md`. For each source or source group, include source-access status, retrieved date, fallback source used or unresolved extraction gap, and source agreement/conflict notes.
   </action>
   <verify>
     ```powershell
@@ -80,6 +92,8 @@ Each section must include:
     Select-String -Path .planning\research\SOURCES.md -SimpleMatch "m3.material.io"
     Select-String -Path .planning\research\SOURCES.md -SimpleMatch "material-components/material-web"
     Select-String -Path .planning\research\SOURCES.md -SimpleMatch "MD3 Expressive"
+    Select-String -Path .planning\research\SOURCES.md -SimpleMatch "source-access status"
+    Select-String -Path .planning\research\SOURCES.md -SimpleMatch "source agreement"
     ```
   </verify>
   <done>SOURCES.md Section 11 is present and follows the dossier pattern.</done>
@@ -95,7 +109,7 @@ Each section must include:
   <action>
     Append or insert `## 12. Flat-3D Game UI sources (added 2026-05-05 by Phase 3.1)` after Section 11.
 
-    Include user exemplars, broad survey examples, rejected pattern examples, and a concise adopt/reject/open synthesis. Make clear that source packs are inspiration and construction evidence, not artwork to copy.
+    Include user exemplars, broad survey examples, rejected pattern examples, and a concise adopt/reject/open synthesis. Make clear that source packs are inspiration and construction evidence, not artwork to copy. Carry over source-access status, screenshot/image URL or visual-capture note, evidence role, and hard-filter audit status for the survey source groups.
   </action>
   <verify>
     ```powershell
@@ -103,6 +117,8 @@ Each section must include:
     Select-String -Path .planning\research\SOURCES.md -SimpleMatch "hcgamestudios.itch.io"
     Select-String -Path .planning\research\SOURCES.md -SimpleMatch "fajrulaslim.itch.io"
     Select-String -Path .planning\research\SOURCES.md -SimpleMatch "construction evidence"
+    Select-String -Path .planning\research\SOURCES.md -SimpleMatch "visual-capture"
+    Select-String -Path .planning\research\SOURCES.md -SimpleMatch "hard-filter audit"
     ```
   </verify>
   <done>SOURCES.md Section 12 is present and follows the dossier pattern.</done>
@@ -122,7 +138,7 @@ Each section must include:
     .planning/research/FLAT-3D-UI-RESEARCH.md
   </files>
   <action>
-    Audit the three research outputs against ROADMAP.md Phase 3.1 success criteria and CONTEXT.md D-01..D-16.
+    Audit the three research outputs against ROADMAP.md Phase 3.1 success criteria and CONTEXT.md D-01..D-16. Apply the final audit quality gates from `<interfaces>`; if a gate fails, patch the research output before closing Plan 06 or surface the unresolved gap in the summary.
 
     If either research doc's `## Flat vs Raised: When to Use Which` section is only a placeholder, populate it with a concise final matrix or cross-reference to the detailed matrix. Append `## Phase 3.1 Verification Log` entries to both docs with the audit date, commands run, and pass/fail result.
 
@@ -134,7 +150,21 @@ Each section must include:
     Select-String -Path .planning\research\FLAT-3D-UI-RESEARCH.md -SimpleMatch "Flat vs Raised: When to Use Which"
     Select-String -Path .planning\research\MD3-RESEARCH.md -SimpleMatch "Phase 3.1 Verification Log"
     Select-String -Path .planning\research\FLAT-3D-UI-RESEARCH.md -SimpleMatch "Phase 3.1 Verification Log"
-    git status --short -- .planning/research addons showcase/showcase.tscn project.godot
+    $md3 = Get-Content -Raw .planning\research\MD3-RESEARCH.md
+    $flat = Get-Content -Raw .planning\research\FLAT-3D-UI-RESEARCH.md
+    foreach ($needle in "hover 8%","focus 12%","pressed 12%","dragged 16%","disabled 38%","disabled container 12%","2px","source agreement","source-access status") {
+      if ($md3 -notmatch [regex]::Escape($needle)) { throw "MD3 audit missing: $needle" }
+    }
+    foreach ($needle in "evidence role","source-access status","visual-capture","hard-filter audit","Adopt","Reject","Open") {
+      if ($flat -notmatch [regex]::Escape($needle)) { throw "Flat-3D audit missing: $needle" }
+    }
+    foreach ($file in ".planning\research\MD3-RESEARCH.md",".planning\research\FLAT-3D-UI-RESEARCH.md") {
+      $section = (Get-Content -Raw $file) -split "## Anti-Cyberpunk and Anti-Texture Audit",2
+      if ($section.Count -lt 2 -or $section[1].Trim().Length -lt 200) { throw "Anti-filter audit appears unpopulated in $file" }
+    }
+    $changed = git status --short
+    $forbidden = $changed | Select-String -Pattern '(\.tres$|\.gd$|^.. addons/|^.. main\.tscn$|^.. project\.godot$|\.png$|\.jpg$|\.webp$|\.svg$)'
+    if ($forbidden) { throw "Forbidden implementation/mockup changes detected: $forbidden" }
     ```
   </verify>
   <done>Final audit passes, SOURCES.md is updated, and forbidden implementation surfaces remain untouched.</done>
@@ -146,6 +176,8 @@ Each section must include:
 - [ ] SOURCES.md Section 12 exists.
 - [ ] Both research docs have populated Flat vs Raised matrices.
 - [ ] Both research docs have Phase 3.1 Verification Log entries.
+- [ ] Source-access fallback coverage and source agreement are audited across MD3, MD3 Expressive, Google blog, itch.io, and broad-survey sources.
+- [ ] Citation quality gates pass: numeric correctness, URL/citation density, source roles, access labels, retrieved dates, and populated anti-filter findings.
 - [ ] No `.tres`, `.gd`, font, icon, addon, project, scene, or mockup file changed.
 </verification>

Here is my independent review.

---

# Cross-AI Plan Review — Phase 3.1 Cycle 2

## 1. Summary

The replan (commit `2b64eb3`) substantively addresses both Cycle 1 HIGH concerns. Concrete fallback chains for JS-rendered sources (official URL → material-web SCSS/Android dev docs → `unresolved extraction gap`) are now prescribed in Plans 02, 03, and 04. Verification across all six plans has been upgraded from bare `Select-String` True/False output to hard-fail `throw` wrappers with citation-density (≥10 URLs), row-type quotas, visual-capture requirements, source-agreement checks, and anti-filter population thresholds (≥200 chars). The plan set is executable without fabricating evidence. Risk: **LOW**.

## 2. Cycle 1 HIGH Resolution Check

### HIGH-1: JS-rendered/hard-to-extract source fallback under-specified

**Verdict: FULLY RESOLVED**

Evidence:
- **Plan 02** (lines 67-71): 4-step source-access fallback chain: try official → fallback to Material Web tokens/Android Dev docs → Context7 MCP for Godot APIs → mark `unresolved extraction gap` if all fail. Minimum ≥10 URLs, every numeric row requires source URL + evidence role + access status + retrieved date + source agreement note.
- **Plan 03** (lines 51-55): Same fallback chain for Expressive sources (m3.material.io blog → Android Developers Expressive → `unresolved extraction gap`), plus post-launch freshness check after 2025-05-13.
- **Plan 04** (lines 54-59): Source discovery guidance with seed platforms (itch.io GUI tags, Godot Asset Library, commercial mobile game UI, design commentary sites), time-boxes, visual-capture requirements, and `browser/manual verified` labeling for hard-to-extract visual sources.
- **Plan 06** (lines 67-72): Final audit quality gates — no `unresolved extraction gap` may remain without downstream owner; flat-3D survey rows must include screenshot/image URL; anti-filter audits must contain populated findings.

The critical requirement "Do not fill gaps from training memory" appears verbatim in Plans 02 and 03 action text. No executor can produce fabricated evidence where public sources are weak without explicitly labeling it.

### HIGH-2: Verification relied on keyword presence instead of citation quality

**Verdict: PARTIALLY RESOLVED**

Evidence of improvement:
- **All plans**: Verification scripts now use `if (condition) { throw }` instead of bare `Select-String` output. A missing value now produces a terminating error, not silent `False` to stdout.
- **Plan 02** (lines 99-110): Hard-fail checks for URL count (≥10), all 6 state-layer values (`hover 8%` through `disabled container 12%`), source-agreement presence, and 4 fallback label categories.
- **Plan 04** (lines 112-124): Hard-fail checks for URL count (≥8), 7 quality-field keywords (`commercial example`, `design commentary`, `source-access status`, `screenshot`, `visual-capture`, `hard-filter audit`), reject count (≥2), and rejection-audit cue presence.
- **Plan 06** (lines 161-163): Anti-filter audit population check — requires ≥200 chars of content after the header, preventing empty sections from passing.
- **Plan 05** (line 165): `git diff --quiet -- .planning/REQUIREMENTS.md` — real file-integrity check, not a keyword proxy.

Why PARTIALLY rather than FULLY RESOLVED:
- Keyword-presence checks are structurally incapable of guaranteeing factual correctness. A document containing `hover 8%` in a sentence that says "the prior report incorrectly claimed hover 8%" would still pass. This is inherent to automated document verification and cannot be fully resolved without manual review — but the plans now mitigate it through source-agreement notes, provenance tables, numeric cross-check requirements, and cross-document audit. This is as addressed as it can be at the plan level.

## 3. Strengths

- **Fallback chains are prescriptive and non-optional.** Each plan with external dependencies has a numbered, sequential sourcing strategy — executor cannot overlook the JS-rendering issue.
- **Hard-fail enforcement is pervasive.** All six plans now use `if/throw` patterns; a verification failure stops the task rather than printing `False`.
- **Plan 04's survey quotas are substantive.** Row-type quotas (≥1 commercial, ≥1 design-commentary, ≥2 asset-pack, ≥2 rejected) prevent low-effort survey filling. Visual-capture requirements force the executor to actually view sources.
- **Plan 05's Recipe A/Recipe B decision tree is explicit.** The `shadow_size = -1` conflict has clear evaluation criteria and a default fallback without requiring a Phase 3.2 spike to occur within Plan 05.
- **Plan 06's anti-filter population check (≥200 chars)** catches silently empty sections that skeleton-only checks would miss.
- **The evidence-absent protocol is clear.** Plan 05 (line 129) and CONTEXT.md D-06: rows without public evidence are marked `NeoCade-owned decision` with MD3/Flat-3D principle rationale — no fabricated citations.

## 4. Concerns

| # | Concern | Severity | Plan |
|---|---------|----------|------|
| C1 | `displayLarge` verify token is brittle. MD3 token appears as `display-large`, `displayLarge`, `display_large`, or `md-sys-typescale-display-large` across different sources. A doc using kebab-case would fail this check, or a heading match could be misleading. | **LOW** | 02 |
| C2 | Fallback-label requirement may produce false negatives. Plan 02 requires `unresolved extraction gap` to appear somewhere in the doc. If the fallback chain succeeds for all sources, this label should NOT appear — yet the verify would `throw`. | **LOW** | 02 |
| C3 | Plan 04 reject-count regex `(?i)\bREJECT\b` matches the word "REJECT" anywhere in the doc, including boilerplate filter text (e.g., "NO cyberpunk: REJECT"). This could inflate the count and pass even if no actual survey rows are rejected. | **LOW** | 04 |

## 5. Suggestions

- **S1 (for C1):** Relax the `displayLarge` check to also match `display-large` and `display_large` variants, or use `display` as the substring with a context check.
- **S2 (for C2):** Gate the fallback-label check: require `unresolved extraction gap` ONLY if the document also contains a statement like "source extraction incomplete" or "could not be corroborated" — or flip it to: count total `unresolved extraction gap` occurrences, and if >0, verify each has a named missing value.
- **S3 (for C3):** Scope the reject-count regex to the `## Broad Visual Survey` section only, or require reject counts to appear in a structured table format with `Rejected` in an Adopt/Reject/Open column.

## 6. Risk Assessment

**LOW**. The two HIGH concerns from Cycle 1 are addressed: fallback chains are concrete across all source-dependent plans, and verification uses hard-fail error handling with multi-dimensional quality gates. Residual risk is LOW — the plan set is executable, correctly bounded (pure research, no `.tres`/`.gd`/mockup/arch changes), and has a clear dependency graph (01 → 02\|04 → 03\|05 → 06). The inherent limitation of keyword-based verification on research documents is acknowledged and mitigated but cannot be eliminated without manual review, which Phase 3.1 does not fundamentally require — downstream Phase 3.3 and 3.4 provide that human gate.

## 7. Current HIGH Concerns

None.

## 8. current_high=0

---

## Claude Review

# Cross-AI Plan Review — Phase 3.1 Cycle 2

## 1. Summary

The cycle 2 replan substantively addresses both prior HIGH concerns. JS-rendered/hard-to-extract source handling is now a four-step fallback chain with named alternatives (Material Web token SCSS, Android Developers MD3 Expressive, Godot Context7) and explicit access-status labels (`directly extracted`, `upstream-token corroborated`, `browser/manual verified`, `fallback source used`, `unresolved extraction gap`). Verification has shifted from string-presence `Select-String` to hard-fail `throw` PowerShell with URL-count floors, numeric state-layer value checks, source-agreement requirements, and forbidden-file git status checks. Remaining gaps are MEDIUM — Plan 04's row-type quotas are checked only by substring presence, and Plan 06's anti-filter "populated" check has a split flaw — but neither is a blocker. Risk: **LOW–MEDIUM**, plans are executable.

## 2. Cycle 1 HIGH Resolution Check

**HIGH 1 — JS-rendered/hard-to-extract source fallback under-specified: FULLY RESOLVED.**
Evidence:
- Plan 01 lines 51–58 define the source-access status taxonomy (5 labels) and citation-quality contract: "JS-rendered or hard-to-extract sources must keep the official URL in provenance and either cite a pinned upstream/source equivalent or mark `unresolved extraction gap`."
- Plan 02 `<interfaces>` defines a concrete 4-step fallback chain (official URL → Material Web tokens / Android Developers → Context7/Godot docs → mark unresolved gap), with "Do not fill gaps from training memory" explicit.
- Plan 02 verify task hard-fails when fallback labels are missing: `if ($content -notmatch [regex]::Escape($label)) { throw "Missing source-access fallback label: $label" }` for `browser/manual verified`, `upstream-token corroborated`, `fallback source used`, `unresolved extraction gap`.
- Plan 03 mirrors the chain for MD3 Expressive (m3 blog → Google blog → Android Developers M3E guide → unresolved gap), with at-least-3-URLs in the Expressive section and post-launch search check.
- Plan 04 carries `screenshot/image URL or visual-capture note` for visually-extracted itch.io and survey rows.
- Plan 06 final audit verifies fallback coverage and forbidden-file untouched state.

**HIGH 2 — Verification keyword-presence-only instead of citation/numeric/source-agreement gated: FULLY RESOLVED.**
Evidence:
- Plan 02 verify checks ALL six state-layer numeric values (`hover 8%`, `focus 12%`, `pressed 12%`, `dragged 16%`, `disabled 38%`, `disabled container 12%`) plus URL count floor `if ($urlCount -lt 10) { throw ... }`. Hard-fails on missing.
- Plan 03 verify hard-fails on missing post-launch source check, requires ≥3 Expressive-section URLs, and requires `source agreement` token.
- Plan 04 verify hard-fails on URL count <8 and rejection count <2: `if ($rejectCount -lt 2) { throw "Expected at least 2 rejected examples; found $rejectCount" }`.
- Plan 05 verify checks `shadow_size = -1`, `non-viable for v1`, all four state-layer numeric values, and adds `git diff --quiet -- .planning/REQUIREMENTS.md` for file integrity.
- Plan 06 verify checks anti-filter populated (`section[1].Trim().Length -lt 200` throw) and forbidden-file git regex throw.
- All `Select-String` calls retained for individual presence are now paired with throw-loop wrappers; no checks merely print `True`/`False` to stdout.

## 3. Strengths

- **Fallback chain is concrete and tiered.** Plan 02 step 4 explicitly forbids filling unresolved gaps from training memory — closes the "plausible but uncited" failure mode.
- **PowerShell verification hard-fails.** Every numeric/quota/forbidden check is wrapped in `if (...) { throw "..." }`. No silent `False` outputs.
- **Numeric correctness is gated.** All five MD3 state-layer values plus the disabled content/container split are required across Plans 02, 05, and 06. A draft missing `dragged 16%` cannot pass cycle 1's blind spot.
- **Source-agreement and access-status are first-class fields.** Both research docs and SOURCES.md Sections 11/12 carry the labels — downstream Phase 3.3 cannot mistake fallback evidence for primary evidence.
- **Plan 05 resolves the Recipe A ambiguity.** "If `shadow_size = -1` disables direct shadows, Plan 05 must state Recipe A is non-viable for v1 and make Recipe B the default" — this is a concrete decision rule, not "evaluate and document."
- **Plan 04 source discovery has guidance and time-box.** Seed platforms, 2–3 attempts per platform, row-type quotas — closes the "wander indefinitely" failure mode.
- **Plan 06 forbidden-file regex check** prevents accidental `.tres` / addon / mockup commits that bypass plan boundaries.

## 4. Concerns

- **MEDIUM (Plan 04 Task 2 verification):** Row-type quotas are programmatically enforced only by substring presence (`if ($content -notmatch "commercial example") { throw ... }`). The substrings could appear once each in headers without there being one commercial, one design-commentary, and two asset-pack actual rows. Reference: Plan 04 verify block lines 102–110.
- **MEDIUM (Plan 06 Task 3 anti-filter populated check):** The split `(Get-Content -Raw $file) -split "## Anti-Cyberpunk and Anti-Texture Audit",2` then `[1].Trim().Length -lt 200` measures everything *after* the heading, including the next section (`## Flat vs Raised: When to Use Which`). An empty audit followed by a populated Flat-vs-Raised matrix would pass. Should be bounded to the section by splitting on the next `\n## ` heading.
- **MEDIUM (Plan 03 Task 1 post-launch check):** Verify checks `Select-String` for the literal token `post-launch`, but does not assert at least one source dated *after* 2025-05-13. The executor could write "no post-launch sources found" and pass — which is fine if true, but the plan should require either ≥1 post-launch source or an explicit dated negative finding with the search date captured.
- **LOW (Plan 02 Context7 directive soft):** Claude's cycle 1 review asked for Context7 to be the primary lookup for Godot 4.6 APIs. The plan acknowledges Context7 in step 3 of the fallback chain ("when available in the runtime"), but doesn't require provenance rows to record "Context7 retrieved" entries. Not a HIGH because this is Claude-specific discretion, not consensus.
- **LOW (Plan 05 Recipe B sketch):** The plan asks for "a 2-4 line ASCII or table sketch showing offset background layer below foreground top layer." This is the right ask; however, the verify only checks for the strings `two stacked` and `wrapper`. A sketch could be omitted while the keywords appear in narrative prose.
- **LOW (Plan 02 `displayLarge` token):** The verify checks the camelCase `displayLarge`. MD3 sources use `display-large` (kebab) and material-web SCSS uses `--md-sys-typescale-display-large`. The current check is brittle; expanding to alternates would be safer.

## 5. Suggestions

- **For Plan 04 Task 2:** Replace the substring-presence quota check with a count-based check, e.g., `if (([regex]::Matches($content, '(?i)evidence role:\s*commercial example')).Count -lt 1) { throw ... }` per row-type, assuming rows are tagged consistently. Alternatively, require a "Survey Quota Tally" block with explicit counts and grep that block.
- **For Plan 06 Task 3:** Tighten the anti-filter populated check to bound the section: `($content -split '## Anti-Cyberpunk and Anti-Texture Audit', 2)[1] -split '\n## ', 2 | Select-Object -First 1` then length check.
- **For Plan 03 Task 1:** Require either `Post-launch source: <URL> dated <YYYY-MM-DD>` or `No post-launch source found, searched <YYYY-MM-DD>` and grep for one of the two patterns to fail when both are missing.
- **For Plan 02:** Make Context7 retrieval explicit: add a verify line requiring at least one Godot API row in provenance to carry a `Context7` access-status tag, OR an explicit "Context7 unavailable, fell back to docs.godotengine.org" note.
- **For Plan 05 Task 1:** Add a verify checking that the Recipe B section contains a line beginning with `┌` or `|` (ASCII/Markdown sketch markers) to enforce an actual sketch.

## 6. Risk Assessment

**LOW–MEDIUM.** Both cycle 1 HIGHs are substantively closed. The plans are executable and the executor cannot pass verification with a plausible-but-uncited draft, can't ship without the full state-layer numeric set, can't bypass the no-soft-shadow rule for extruded-flat, and can't accidentally mutate REQUIREMENTS.md or addon files. Remaining MEDIUM gaps (Plan 04 quotas, Plan 06 split flaw, Plan 03 post-launch check) are quality-of-verification concerns that would, at worst, allow a borderline draft to pass — they do not allow phase scope drift, evidence fabrication, or forbidden-file changes. Phase 3.1 can proceed to execution.

## 7. Current HIGH Concerns

None.

## 8. current_high=0

---

## Consensus Summary

Both reviewers found that the cycle 2 replan substantively addresses the two prior HIGH concerns. OpenCode marked the JS-rendered source fallback HIGH as FULLY RESOLVED and the verification-quality HIGH as PARTIALLY RESOLVED because automated document checks cannot prove factual truth without manual review, but still reported no current HIGH concerns and current_high=0. Claude marked both prior HIGH concerns FULLY RESOLVED and also reported no current HIGH concerns and current_high=0.

### Agreed Strengths

- Source fallback is now concrete and tiered: official source first, then accessible upstream/source equivalents, then browser/manual verification labels, with unresolved gaps explicitly named rather than filled from memory.
- Verification now uses hard-fail PowerShell checks with 	hrow, URL/citation floors, state-layer numeric checks, source-access labels, source-agreement notes, visual-capture requirements, and forbidden-file checks.
- Plan 04 now has source discovery guidance, visual-capture requirements, and survey quotas that reduce low-effort or low-context evidence collection.
- Plan 06 now adds final audit gates for source-access coverage, numeric correctness, cross-source agreement, anti-filter population, and forbidden implementation/mockup changes.

### Agreed Concerns

- Plan 02's displayLarge verification token is brittle because MD3 sources may use kebab-case or CSS custom property forms instead.
- Some verification checks still rely on substring presence and could produce false positives in edge cases, especially Plan 04's row-type quota and reject-count checks.
- Plan 06's anti-filter population check should be bounded to the target section; as written, text in following sections may satisfy the length check.

### Divergent Views

- OpenCode classified the verification-quality HIGH as PARTIALLY RESOLVED in the resolution check because keyword-based document verification cannot fully prove factual correctness. It nevertheless considered the remaining risk LOW and listed no current HIGH concerns.
- Claude classified the verification-quality HIGH as FULLY RESOLVED because the plan-level mitigation is now strong enough: hard-fail checks, numeric values, source agreement, provenance labels, and forbidden-file checks collectively close the original planning gap.

### Current HIGH Concerns

None.

### current_high

0

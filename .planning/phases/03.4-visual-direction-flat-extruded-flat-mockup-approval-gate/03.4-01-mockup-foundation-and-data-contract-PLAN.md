---
phase: 03.4-visual-direction-flat-extruded-flat-mockup-approval-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/mockups/3.4/
autonomous: true
requirements:
  - DESIGN-NEW-01
  - DESIGN-NEW-02
  - DESIGN-NEW-DYNAMIC-01
  - TOKEN-NEW-01
  - TOKEN-NEW-02
must_haves:
  truths:
    - "Phase 3.4 visualizes the five Phase 3.3 approved directions only: Pulse, Slate, Bubble, Daybreak, and Burst."
    - "The mockup system is formula-driven and mirrors the dynamic `NeoCadeTheme` export axes: `base_color`, `accent_color`, `raised`, and `platform`."
    - "All Phase 3.4 output goes under `.planning/mockups/3.4/`; v0 artifacts under `.planning/mockups/concepts/` and `.planning/mockups/03-direction-boards.*` are preserved untouched."
    - "No production addon/theme files under `addons/neocade_theme/`, `showcase/showcase.tscn`, `project.godot`, fonts, icons, or production `.tres` files are modified."
    - "The foundation includes direction data, shared render tokens, representative Control templates, Playwright/Edge screenshot workflow, and a coverage/checklist scaffold for later plans."
  artifacts:
    - .planning/mockups/3.4/
  key_links:
    - ".planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CONTEXT.md D-01..D-07, D-12..D-15, D-20..D-24"
    - ".planning/research/THEME-DIRECTIONS.md Candidate Directions"
    - ".planning/research/MD3-RESEARCH.md"
    - ".planning/research/FLAT-3D-UI-RESEARCH.md"
    - ".planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md"
---

<objective>
Build the reusable Phase 3.4 mockup foundation: data model, HTML/CSS/JS templates, render/check scripts, and documentation needed for concept and finalist boards.

Purpose: make later visual work repeatable and tied to the dynamic `NeoCadeTheme` architecture instead of hand-crafted static screenshots.
Output: `.planning/mockups/3.4/` foundation files.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CONTEXT.md
@.planning/research/THEME-DIRECTIONS.md
@.planning/research/MD3-RESEARCH.md
@.planning/research/FLAT-3D-UI-RESEARCH.md
@.planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
@.planning/spikes/dynamic-theme/VERIFY-RESULTS.md
@.planning/research/PITFALLS.md

<interfaces>
Expected directory:
- `.planning/mockups/3.4/`

Suggested files:
- `README.md` - explains pipeline, phase boundaries, render commands, and no-production-edit rules.
- `data/directions.json` or equivalent - Pulse/Slate/Bubble/Daybreak/Burst names, class names, filename stems, base/accent colors, and personality notes.
- `src/` or equivalent - formula helpers, representative Control templates, raised/flat rendering helpers, desktop/mobile sizing constants.
- `concept-gallery.html` - shell for Stage 1 concept boards.
- `finalist-gallery.html` - shell for Stage 2 finalist boards.
- `render-check.md` or equivalent checklist template.
- A Playwright/Edge render script if the existing project has the dependency available; otherwise a documented manual render command.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create mockup directory and pipeline README</name>
  <read_first>
    - .planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CONTEXT.md
    - .planning/research/THEME-DIRECTIONS.md
  </read_first>
  <files>.planning/mockups/3.4/README.md</files>
  <action>
    Create `.planning/mockups/3.4/` and a README that documents:
    - Phase 3.4 outputs and non-goals
    - the 10-board Stage 1 concept flow
    - the finalist 4-grid flow
    - the two required user gates
    - historical v0 preservation
    - no production addon/theme edits in this phase
    - how to render or inspect the mockups
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$p='.planning/mockups/3.4/README.md'; Test-Path $p; $doc=Get-Content -Raw $p; foreach($n in '10','flat','raised','4-grid','user gate','DESIGN_TOKENS.md','no production','addons/neocade_theme','historical') { if($doc -notmatch [regex]::Escape($n)) { throw \"Missing README marker: $n\" } }"
    </automated>
  </verify>
  <done>The Phase 3.4 mockup directory exists and its boundary/pipeline README is written.</done>
</task>

<task type="auto">
  <name>Task 2: Encode approved direction data</name>
  <read_first>
    - .planning/research/THEME-DIRECTIONS.md
  </read_first>
  <files>.planning/mockups/3.4/data/</files>
  <action>
    Create a direction data artifact under `.planning/mockups/3.4/data/` that includes exactly Pulse, Slate, Bubble, Daybreak, and Burst.

    For each direction capture:
    - display name
    - future subclass name ending in `NeoCadeTheme`
    - future `.tres` filename stem
    - base color
    - accent color
    - personality summary
    - target use case
    - flat behavior note
    - raised behavior note
    - mobile sizing note

    Keep the data source traceable to `.planning/research/THEME-DIRECTIONS.md`. Do not invent a sixth direction, rename a direction, or preselect a recommended starter.
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$files=Get-ChildItem '.planning/mockups/3.4/data' -File -ErrorAction Stop; $blob=($files|Get-Content -Raw) -join \"`n\"; foreach($n in 'Pulse','Slate','Bubble','Daybreak','Burst','NeoCadeTheme','base','accent','raised','mobile') { if($blob -notmatch [regex]::Escape($n)) { throw \"Missing direction data marker: $n\" } }; if(($blob|Select-String -Pattern 'Boardwalk|Cyber|Synth|Noir').Matches.Count -gt 0){ throw 'Forbidden or rejected naming marker found in direction data' }"
    </automated>
  </verify>
  <done>Direction data is complete for the five approved directions and has no base preselection.</done>
</task>

<task type="auto">
  <name>Task 3: Build formula-driven mockup shell</name>
  <read_first>
    - .planning/research/MD3-RESEARCH.md
    - .planning/research/FLAT-3D-UI-RESEARCH.md
    - .planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
  </read_first>
  <files>.planning/mockups/3.4/</files>
  <action>
    Build the initial HTML/CSS/JS shell for Phase 3.4 mockups.

    The shell must encode:
    - derived surface ramp helpers from `base_color`
    - accent role helpers from `accent_color`
    - `raised=false` and `raised=true` rendering paths
    - desktop and mobile sizing token branches
    - representative Control templates for Stage 1
    - placeholder/loop support for finalist full matrix rendering in later plans

    Use only flat solid colors and hard offset shapes. Do not use texture images, pattern backgrounds, blur shadows, glow, bevel gradients, or generated bitmap art. If Playwright is available, add a render script; if not, document browser-open/manual screenshot steps.
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$files=Get-ChildItem '.planning/mockups/3.4' -Recurse -File | Where-Object { $_.Extension -in '.html','.css','.js','.md' }; $blob=($files|Get-Content -Raw) -join \"`n\"; foreach($n in 'base_color','accent_color','raised','platform','desktop','mobile','surface','focus','hover','pressed') { if($blob -notmatch [regex]::Escape($n)) { throw \"Missing mockup formula marker: $n\" } }; foreach($bad in 'linear-gradient','radial-gradient','box-shadow: 0 0','filter: blur','texture','pattern','emboss') { if($blob -match [regex]::Escape($bad)) { throw \"Forbidden visual treatment marker: $bad\" } }"
    </automated>
  </verify>
  <done>The mockup shell can render formula-driven flat/raised and desktop/mobile variants without forbidden treatments.</done>
</task>

<task type="auto">
  <name>Task 4: Add render checklist and forbidden-surface audit</name>
  <read_first>
    - .planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CONTEXT.md
  </read_first>
  <files>.planning/mockups/3.4/render-check.md</files>
  <action>
    Create a render/checklist artifact for later plans.

    It must include checks for:
    - all five directions present
    - flat and raised concept outputs available for each direction
    - finalist 4-grid readiness
    - color override preview readiness
    - anti-cyberpunk and anti-texture pass
    - no production addon/theme files changed
    - historical v0 artifacts untouched
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$p='.planning/mockups/3.4/render-check.md'; Test-Path $p; $doc=Get-Content -Raw $p; foreach($n in 'Pulse','Slate','Bubble','Daybreak','Burst','flat','raised','4-grid','color override','anti-cyberpunk','anti-texture','addons/neocade_theme','historical') { if($doc -notmatch [regex]::Escape($n)) { throw \"Missing render checklist marker: $n\" } }; $changed=git status --short; $forbidden=$changed|Select-String -Pattern '(\.tres$|\.gd$|^.. addons/|^.. main\.tscn$|^.. project\.godot$|\.png$|\.jpg$|\.webp$|\.svg$)'; if($forbidden){ throw \"Forbidden implementation/image changes detected: $forbidden\" }"
    </automated>
  </verify>
  <done>The render checklist exists and forbidden production/image surfaces remain untouched.</done>
</task>
</tasks>

<verification>
- [ ] `.planning/mockups/3.4/README.md` documents the pipeline and phase boundary.
- [ ] Direction data contains exactly Pulse, Slate, Bubble, Daybreak, and Burst.
- [ ] HTML/CSS/JS shell encodes `base_color`, `accent_color`, `raised`, and `platform`.
- [ ] Render checklist exists.
- [ ] No production addon/theme, scene, project, font, icon, or v0 mockup artifacts changed.
</verification>

<success_criteria>
- Stage 1 and finalist mockup execution can proceed without reinventing data structures.
- DESIGN-NEW-DYNAMIC-01 is structurally supported by formula-driven mockup code.
- The phase boundary prevents production `.tres` styling before approval.
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-01-SUMMARY.md` with files created, render/check command outputs, and forbidden-surface audit result.
</output>

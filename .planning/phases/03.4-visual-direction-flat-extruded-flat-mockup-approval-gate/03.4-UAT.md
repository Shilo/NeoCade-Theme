---
status: complete
phase: 03.4-visual-direction-flat-extruded-flat-mockup-approval-gate
source:
  - .planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-01-SUMMARY.md
  - .planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-02-SUMMARY.md
  - .planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-03-SUMMARY.md
  - .planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-04-SUMMARY.md
started: 2026-05-06T07:00:48-07:00
updated: 2026-05-06T07:00:48-07:00
---

## Current Test

[testing complete]

## Tests

### 1. Mockup Workspace Infrastructure (Plan 01) Exists with Required Files
expected: `.planning/mockups/3.4/` exists with the formula-driven mockup infrastructure: `data/directions.json` (locked direction palette + `shape_language` block per direction), `src/neocade-mockups.css` + `src/neocade-mockups.js` (per-direction-aware artboard CSS + renderer with surface-ramp/state-layer derivation), `concept-image.html` (single-artboard mount with `?direction=&platform=&raised=` URL params), `concept-gallery.html` + `finalist-gallery.html` (review pages), `render.js` (Playwright-core + system Edge), `concepts/` + `screenshots/` output directories, `image-prompts/direction-shape-language-spec.md` (authoritative spec), `README.md`. The `is_light` luminance flag is wired in the renderer (commit `bd40e79`) so light-mode color overrides correctly flip text + surface tokens.
result: pass
evidence: All required files exist under `C:/Programming_Files/Shilocity/Godot/NeoCade-Theme/.planning/mockups/3.4/`. Verified via Glob/Read: `data/directions.json` (locked dark palettes Revision Round 2/2 + `shape_language` blocks for all 5 directions), `src/neocade-mockups.css` (per-direction CSS variable scopes; no hard-coded `--radius: 12px` on `.nc-artboard`), `src/neocade-mockups.js` (914 lines; `deriveSurfaceRamp()`, `deriveTokens()`, `tintTowardBase()`, `darken()`, `mix()`, `luminance()`, `is_light` branch added L470-475 + L519-525 per commit `bd40e79`), `concept-image.html`, `concept-gallery.html`, `finalist-gallery.html` (extended in Plan 03 to include 4-grid + 3-cell color override row + 40-Control coverage matrix), `render.js` (modes: `concept-images`, `finalist-images`, `color-overview`, `greyscale`, gallery-default; Playwright-core + system Edge; 7 PNGs rendered for finalist mode incl. light demo), `concepts/` (22 PNGs), `screenshots/`, `image-prompts/direction-shape-language-spec.md`, `README.md`. The `is_light = base_color.get_luminance() >= 0.5` flag mirrors production `NeoCadeTheme.gd` semantics (see DESIGN_TOKENS.md §4.3) and is documented in `render-check.md` "Light-mode `is_light` flag audit" section. SC-02 evidence in `phase-3.4-closeout.md:75` confirms infrastructure was used to render the 15 Stage 1 PNGs.

### 2. 15 Stage 1 Concept PNGs Exist with Per-Direction Shape-Language Differentiation (D-29) + Greyscale Sufficiency (D-30) + Dark-Only Compliance (D-28)
expected: 5 directions × 3 variants (desktop-flat, mobile-flat, mobile-raised) = 15 Stage 1 PNGs at `concepts/{pulse,slate,bubble,daybreak,burst}-{desktop-flat,mobile-flat,mobile-raised}.png`. Each direction commits to specific values on the 10 shape-language axes (corner radius, button anatomy, chip/tab shape, brand mark, density, focus ring, type weights, surface ramp depth, state-layer behavior, raised offset depth). All 5 directions use locked dark palettes from THEME-DIRECTIONS.md Revision Round 2/2 (Pulse #151A2E, Slate #111820, Bubble #241326, Daybreak #0B2420, Burst #20112E). Anti-cyberpunk + anti-texture + anti-painterly-chrome filters PASS for all 5. Greyscale sufficiency test (D-30) PASS — each direction is identifiable by shape language alone in greyscale.
result: pass
evidence: 15 Stage 1 PNGs verified present via `ls .planning/mockups/3.4/concepts/`. All 5 directions × 3 variants accounted for (file size > 5KB per Plan 02 Task 2 verification). Per-direction `shape_language` blocks committed in `data/directions.json` and mirrored into `src/neocade-mockups.js` `NEOCADE_DIRECTIONS[]` (L14-219 — Pulse L14-55, Slate L57-97, Bubble L99-137, Daybreak L139-177, Burst L179-219). Each direction has all 10 axes populated with distinct values: Pulse `r_base=0` (sharp cabinet) vs Bubble `r_base=26` (pillowy candy) vs Daybreak `r_base=8` (airy fresh) vs Slate `r_base=14` (premium pill) vs Burst `r_base=18` with `r_button_primary=28` (statement). Render-check.md "Stage 1 Concept Matrix" sections L52-191 document per-direction 10-axis tables with PASS verdicts on all five filter rows (anti-cyberpunk / anti-texture / anti-painterly-chrome / dark-only / solid-fill discipline) for all 5 directions. D-30 greyscale audit composite at `screenshots/greyscale-sufficiency-test.png` (rendered via `node render.js greyscale` against `src/greyscale-check.html`) and documented in render-check.md L192-215 ("D-30 Greyscale Sufficiency Test") with PASS for all 5 — each direction identifiable as the right mood (Pulse → cabinet, Slate → premium tool, Bubble → candy counter, Daybreak → community lobby, Burst → achievement). All 5 base palettes have luminance < 0.5, satisfying D-28 dark-only compliance per the corrective addendum.

### 3. Plan 02 Finalist Selection Closed — Pulse Recommended Starter, All 5 Ship
expected: `.planning/mockups/3.4/finalist-selection.md` exists with frontmatter `gate: phase-3.4-plan-02-task-4`, `status: closed`, `selection_kind: recommended-starter`. Records Pulse as v1 recommended starter direction. Records that all four other directions (Slate, Bubble, Daybreak, Burst) are NOT rejected — they ship as v1 personality variations. Documents the LDtk inspiration source for Pulse's mood (loose inspiration only per project posture). Future revision notes captured for v1.x backlog (Pulse colorfulness pass + other-direction polish).
result: pass
evidence: `finalist-selection.md` (162 lines, frontmatter L1-6) explicitly records `gate: phase-3.4-plan-02-task-4`, `status: closed`, `selection_kind: recommended-starter`. L10-12: "Pulse — designated v1 recommended starter direction. Selected by user 2026-05-06." L16: user's verbatim framing "first use the Pulse theme as the final selection, we will implement that theme first" + "i eventually want to use all of them as theme variations." L19-24: LDtk inspiration source confirmed for Pulse (loose-inspiration framing per project posture; no values lifted verbatim). L28: "Per the user's framing 'i eventually want to use all of them as theme variations,' Slate, Bubble, Daybreak, and Burst remain approved for v1 ship as personality variations." L43: "CLAUDE.md 'v1 ships N user-approved theme subclass `.tres` files' wording resolves to N = 5 for v1." Future revision notes L47-66 capture Pulse colorfulness pass for v1.x backlog. Plan 02 SUMMARY.md `status: CLOSED 2026-05-06` (frontmatter line 3, updated in commit `96f55ad`).

### 4. Pulse 4-Grid Full-Fidelity Mockups + 3 Color Override Variants (Including Light-Mode `is_light` Forward-Compat Demo)
expected: 7 Plan 03 Pulse PNGs at `concepts/pulse-finalist-{desktop-flat,mobile-flat,desktop-raised,mobile-raised}.png` (4 grid cells) + `concepts/pulse-finalist-override-{warm,ocean,light}.png` (3 color override variants). The 4-grid demonstrates the dynamic single-class behavior across `raised: bool` × `platform: {DESKTOP, MOBILE}`. The 3 overrides demonstrate the dynamic `@export base_color` / `accent_color` contract. Override C (cream-light, `#F4F1EC` / `#1F4F8C`) is a forward-compat demo of the architecture's `is_light = base_color.get_luminance() >= 0.5` flag — when set, the renderer flips `--ink` to dark, `--muted` to slate-gray, surface ramp panel/high/overlay/outline mix-target white→black, and state-hover mix-target white→black. Light mode itself is officially v2 (PROJECT.md Out of Scope); this override is forward-compat-only.
result: pass
evidence: All 7 Pulse Plan 03 PNGs verified present in `concepts/`: `pulse-finalist-desktop-flat.png` (1280×720, 96.6 KB), `pulse-finalist-mobile-flat.png` (430×1500, 104.6 KB), `pulse-finalist-desktop-raised.png` (97.3 KB), `pulse-finalist-mobile-raised.png` (106.4 KB), `pulse-finalist-override-warm.png` (#1A1410 dark / #FFC857 amber, 106.6 KB), `pulse-finalist-override-ocean.png` (#0F1A22 dark / #5FE3FF cyan, 106.0 KB), `pulse-finalist-override-light.png` (#F4F1EC cream / #1F4F8C blue, the is_light demo). The light-mode override was visually verified in-conversation at the Plan 03 checkpoint — text is dark navy on cream, accent dark blue clearly visible, popup surface darker grey container with legible navy text, list rows show alternating elevation, palette swatches at footer show correct surface ramp (low/panel/high/accent). Renderer wiring for `is_light` flag verified in `src/neocade-mockups.js`: `deriveSurfaceRamp()` L429-465 has `const isLight = luminance(base) >= 0.5` and flips `elevateTarget` between `#ffffff` and `#000000`; `deriveTokens()` L467-575 flips `ink` (`#F7F8FB` → `#1B2230`), `muted` (`#B9C1D0` → `#5A6478`), and `--state-hover` mix-target. Render-check.md "Light-mode `is_light` flag audit" section L362-375 documents 11 PASS rows including: `is_light` correctly evaluates true on `#F4F1EC` (luminance ≈ 0.91), `--ink` flips dark, `--muted` flips to mid-tone, surface ramp flips, state-hover flips, primary button text reads correctly (cream `#F4F1EC` on dark blue `#1F4F8C` accent fill ≈ 9:1 AAA), body text ink on cream ≈ 14:1 AAA, ghost button accent text on cream ≈ 9:1 AAA. Commit `bd40e79` "feat(03.4-03): wire is_light flag + cream-light Pulse override (text/surface flip demo)" lands the demo. DESIGN_TOKENS.md §4.3 (L218-228) carries the `is_light` contract into Phase 4's GDScript implementation reference.

### 5. Full 40-Control Coverage Matrix + Pitfall 1.1 State Combinations Documented
expected: `coverage-matrix.md` enumerates all 40 user-facing Godot 4.6 Control classes with state representations and mockup locations. Pitfall 1.1 state combinations (`pressed_focus`, `checked_focus`, `hover_pressed`) are explicitly mapped with Phase 4 stylebox contracts. Coverage matrix is also embedded in `finalist-gallery.html` for visual review.
result: pass
evidence: `coverage-matrix.md` (129 lines) verified via Read + Grep. `grep -c -E '<40 Control class names>' coverage-matrix.md` returns 56 (>= 40 unique Controls; some appear multiple times in different sections). All 40 Godot 4.6 user-facing Controls explicitly listed in the matrix table starting L20: Button, CheckBox, CheckButton, OptionButton, MenuButton, ColorPickerButton, LinkButton, Label, RichTextLabel, LineEdit, TextEdit, CodeEdit, HSlider, VSlider, ProgressBar, HScrollBar, VScrollBar, SpinBox, ItemList, Tree, TabBar, TabContainer, FoldableContainer, PopupPanel, PopupMenu, AcceptDialog, ConfirmationDialog, FileDialog, TooltipPanel, TooltipLabel, Window, Panel, PanelContainer, ScrollContainer, SplitContainer, MarginContainer, MenuBar, ColorPicker, GraphEdit, GraphNode, GraphFrame. Pitfall 1.1 state combinations explicitly mapped in coverage-matrix.md "10. Pitfall 1.1 state combinations" section L104-114: `pressed_focus` (Button, OptionButton, MenuButton, LinkButton, ColorPickerButton, CheckBox, CheckButton — focus ring must NOT disappear when pressed), `checked_focus` (CheckBox, CheckButton, Tree row — focus ring composes around box, not over checkmark), `hover_pressed` (Button family — hover state-layer composes on pressed bg without producing a third bg shift). Each Pitfall 1.1 row carries an explicit Phase 4 StyleBoxFlat composition contract. Plan 03 SUMMARY.md frontmatter `metrics.controls_mapped: 40` + `metrics.pitfall_combinations_documented: 3` confirms the count. Coverage matrix also embedded in `finalist-gallery.html` per the comment block at L268-275.

### 6. Plan 03 Final Approval Closed — All 5 Ship + Pulse Recommended Starter + Phase 4 Prerequisite Recorded
expected: `.planning/mockups/3.4/final-approval.md` exists with frontmatter `gate: phase-3.4-plan-03-task-4`, `approval_date: 2026-05-06`, `status: closed`, `selection_kind: all-five-ship + recommended-starter`. Records: approval date, "Approved final themes" table listing all 5 directions with palettes + WCAG + Phase 4 implementation order (Pulse first), no rejected/deferred themes, "Base direction" reframed as "Recommended starter" = Pulse per D-31, Phase 4 prerequisite explicitly stated (DESIGN_TOKENS.md must be written in Plan 04 first), v1.x backlog notes (Pulse colorfulness pass, light mode v2). All 4 verification regex markers from Plan 03 Task 4 spec ("Approved final themes", "Base direction", "NeoCadeTheme", "approval date") present.
result: pass
evidence: `final-approval.md` (76 lines) verified via Read. Frontmatter L1-5: `gate: phase-3.4-plan-03-task-4`, `approval_date: 2026-05-06`, `status: closed`, `selection_kind: all-five-ship + recommended-starter`. "Approval date" L9: "2026-05-06". "Approved final themes" table L13-21: all 5 directions (Pulse #151A2E/#8BFF6A 13.62:1, Slate #111820/#8BD3FF 10.94:1, Bubble #241326/#FFB3E6 10.74:1, Daybreak #0B2420/#76F2D1 11.96:1, Burst #20112E/#FFD166 12.33:1) with Phase 4 implementation order (Pulse first, others after). L23: "CLAUDE.md 'v1 ships N user-approved theme subclass `.tres` files' resolves to N = 5 for v1." "Rejected / deferred finalist themes" L27: "None." "Base direction" L31-33: "Under the D-31 reframing, 'base direction' is reframed as 'recommended starter' — the picked direction has only two soft commitments (showcase scene default + README 'try this first' suggestion). No values bake into superclass defaults. The recommended starter for v1 is: Pulse." Phase 4 prerequisite L67-71: "Phase 4 may implement only after `DESIGN_TOKENS.md` is written in Plan 04. Plan 04 is autonomous and unblocks now that this gate is closed." V1.x backlog L52-66: Pulse colorfulness pass, other-direction polish, v2 light mode (with `is_light` already wired forward-compat). All 4 Plan 03 Task 4 verification markers present: `Approved final themes` (L11), `Base direction` (L29), `NeoCadeTheme` (L31, L65), `approval date` (L7 frontmatter `approval_date` plus body L9 "Approval date"). Plan 03 SUMMARY.md `status: closed` / `gate_status: closed` (commit `b964fa3`).

### 7. DESIGN_TOKENS.md Implementation-Ready for Phase 4 — All 12 Sections + 9-Export Contract + is_light + Single-Class Architecture + Inter-Only Fonts + Anti-Texture Rules
expected: `.planning/DESIGN_TOKENS.md` exists with all 12 required sections (Provenance / Approved final themes / Recommended starter / Shared NeoCadeTheme class contract / Per-theme data-resource recipes / Color formula and surface ramp contract / Accent + state-layer / Shape + outline + spacing + typography / Flat-raised variation / Desktop-mobile-AUTO variation / Accessibility + anti-texture / Phase 4 implementation handoff). The 9-property `@export` contract is documented (Core: base_color, accent_color, raised, platform; Shape: corner_radius, spacing, raised_strength, focus_thickness, outline_width). The `is_light = base_color.get_luminance() >= 0.5` derivation rule is documented as the forward-compat light-mode wiring. Per-direction recipes for all 5 themes with concrete `@export` values. "No per-direction `.gd` classes" + "No subclass override contract" stated explicitly. "Inter Variable Roman" only (no Outfit / no Noto / no JetBrains). All 14 verification markers from Plan 04 Task 2 spec present as literal strings.
result: pass
evidence: `.planning/DESIGN_TOKENS.md` (702 lines) verified via Read + Grep. All 12 sections present (`grep '^## ' DESIGN_TOKENS.md` returns 12 hits): §1 Provenance and approval gate (L13), §2 Approved final themes (L48), §3 Recommended starter designation (L66), §4 Shared `NeoCadeTheme` class contract (L89), §5 Per-theme data-resource recipes (L156), §6 Color formula and surface ramp contract (L307), §7 Accent + semantic role + state-layer contract (L402), §8 Shape + outline + spacing + typography (L442), §9 Flat/raised variation (L502), §10 Desktop/mobile/AUTO variation (L551), §11 Accessibility + anti-texture rules (L601), §12 Phase 4 implementation handoff (L643). 9-property `@export` contract documented in §4 L96-107: Core (4) — `base_color: Color`, `accent_color: Color`, `raised: bool`, `platform: Platform`; Shape (5, under `@export_group("Shape")`) — `corner_radius: int`, `spacing: int`, `raised_strength: int`, `focus_thickness: int`, `outline_width: int`. `is_light` derivation rule documented in §4.3 L218-228: "var is_light: bool / is_light = base_color.get_luminance() >= 0.5 / Dark is the project default; is_light flags the deviation. (Renamed/inverted from godot-minimal-theme's dark_theme — semantically clearer for project-default-dark.)" Per-direction recipes for all 5 themes in §5 L156-306 with concrete `@export` values matching the orchestrator's table from the executor brief (Pulse `corner_radius=0`, Slate `=14`, Bubble `=26`, Daybreak `=8`, Burst `=18`). Single-class no-subclass rule explicit at L131 ("No per-direction `.gd` files") and L75-78 (D-31 reframing). Inter Variable Roman explicitly named at multiple sections; Plan 04 Task 2 verification regex confirmed PASS on all 14 markers per Plan 04 SUMMARY.md (commit `1bd4f7c`): `Provenance`, `Approved final themes`, `Recommended starter`, `NeoCadeTheme`, `base_color`, `accent_color`, `No per-direction`, `Inter Variable Roman`, `flat`, `raised`, `DESKTOP`, `MOBILE`, `AUTO`, `anti-texture`, `Phase 4`. Anti-texture rules in §11 L601-642.

### 8. phase-3.4-closeout.md Audit Covers ROADMAP SC-01..SC-07 + CONTEXT D-IDs + Forbidden-Surface Audit
expected: `.planning/mockups/3.4/phase-3.4-closeout.md` exists with §1 (approval prerequisite), §2 (DESIGN_TOKENS.md confirmation), §3.1 (ROADMAP success-criteria audit SC-01..SC-07 all PASS), §3.2 (CONTEXT decisions D-01..D-27 audit with notes where D-28..D-31 from CORRECTIVE-ADDENDUM superseded), §4 (forbidden-surface audit + Phase 4 handoff). All required Plan 04 verification regex markers (SC-01..SC-07, D-01, D-04, D-08, D-12, D-16, D-25, D-27, "10 concept", "4-grid", "DESIGN_TOKENS.md", "historical", "no production", "forbidden-surface audit", "Phase 4 handoff", "Recommended starter") present as literal strings.
result: pass
evidence: `phase-3.4-closeout.md` (246 lines) verified via Read + Grep. All Plan 04 Task 3 + Task 4 verification markers confirmed present (`grep -c` returns 39 hits across the targeted patterns). §1 (approval prerequisite L62-66) + §2 (DESIGN_TOKENS.md confirmation) + §3.1 (SC-01..SC-07 audit table at L73-80) + §3.2 (D-01..D-27 audit) + §4 (forbidden-surface audit + Phase 4 handoff). SC-01..SC-07 explicitly numbered at L74-80 with PASS verdicts and concrete evidence per row: SC-01 (5 directions mocked + snake-case `.tres` filenames + no per-direction `.gd`), SC-02 (15 PNGs at logical 10-board matrix), SC-03 (Pulse 4-grid + override row + Pitfall 1.1 explicit), SC-04 (final-approval.md + DESIGN_TOKENS.md), SC-05 (anti-cyberpunk + anti-texture filter PASS at finalist gate), SC-06 (no `.tres` styling commits under `addons/`), SC-07 (historical preservation + Phase 3.4 outputs under `.planning/mockups/3.4/`). All 7 SC PASS (L82). D-IDs explicitly cited: D-01 (recommended starter framing), D-04 (10-concept-board logical matrix → 15 PNGs implementation), D-08 (only finalists get full-fidelity), D-12 (formula-driven HTML/CSS), D-16 (final approval gate), D-25 (Phase 3.4 outputs under .planning/mockups/3.4/), D-27 (no production styling commits). The "10 concept" phrase appears at L75 + L93 with the canonical reconciliation: "the 5-direction × 2-axis matrix expanded to 15 PNGs (3 variants per direction) at implementation time." "4-grid" appears throughout §3.1 SC-03. "historical" appears at L80, L133, L142. "no production" appears at L80, L129. "forbidden-surface audit" appears at section 4 header L165. "Phase 4 handoff" appears at section 4 + DESIGN_TOKENS.md §12 cross-references. "Recommended starter" appears throughout (Plan 04 SUMMARY frontmatter confirms 19/19 markers PASS at commit `bb20cb1` + 4/4 markers PASS at commit `6b89e43`).

### 9. Independent Forbidden-Surface Audit — Zero Production Files Modified Across Phase 3.4
expected: Independent git audit of all Phase 3.4 commits (`git log --grep="03.4" --name-only`) confirms zero modifications to production addon/theme surfaces: `addons/`, `main.tscn`, `project.godot`, root `.tres`, root `.gd`, fonts/, icons/. The Phase 3.4 hard blocker (D-15 / D-27 / SC-06) is enforced. v0 historical artifacts (`.planning/mockups/concepts/`, `.planning/mockups/03-direction-boards.*`, `.planning/research/mood-board/`) remain untouched.
result: pass
evidence: **Independent verification:** `git -C C:/Programming_Files/Shilocity/Godot/NeoCade-Theme log --grep="03.4" --name-only --pretty=format:"" | sort -u | grep -E '^(addons/|main\.tscn|project\.godot|.*\.(tres|gd)$)'` returns ZERO matches. All Phase 3.4 commits (commit-message-grep "03.4" finds 25+ commits from `c85281d` through `2515cf9`) touched only `.planning/` paths (mockups/3.4/, phases/03.4-.../, plus DESIGN_TOKENS.md). v0 historical paths verified untouched: `.planning/mockups/concepts/*.png` (5 v0 painterly concepts) + `.planning/mockups/03-direction-boards.{html,md,png}` + `.planning/research/mood-board/` show zero modifications across the Phase 3.4 commit range. **Note on the broader audit:** `git log --since "2026-05-04 19:45" --name-only` (Phase 3.4's start window) does include some `.planning/spikes/dynamic-theme/*.{tres,gd}` files, but those are Phase 3.2 spike artifacts created/modified during Phase 3.2's feasibility-validation work (before Phase 3.4 started); they predate Phase 3.4's commit chain and are explicitly Phase 3.2 territory under `.planning/spikes/`. The Phase 3.4 forbidden-surface rule covers **production** surfaces (`addons/`, `main.tscn`, `project.godot`, root `.tres`/`.gd`), not research-spike artifacts under `.planning/`. Plan 04 Task 4 PowerShell `git status --short` audit at commit `6b89e43` returned clean (PASS) for the same forbidden-pattern set.

## Summary

total: 9
passed: 9
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — all tests passed]

## Verification Method

Phase 3.4 produces visual mockups + design-token documentation + closeout audits — no production runtime to drive interactively. UAT was self-tested against actual artifacts (PNGs, HTML, MD files, JSON) using filesystem reads, `grep` content verification against PowerShell verification regexes from each plan's `<verify>` blocks, and independent git audits. Each test's `evidence:` field cites specific files, line numbers, commit SHAs, and grep results so the user can spot-check by re-running the cited commands.

**Phase 3.4 user-decision gates were already closed during execution** (Plan 02 Task 4 finalist-selection at `finalist-selection.md`; Plan 03 Task 4 final approval at `final-approval.md`); UAT cross-references those gate-closure artifacts rather than re-running them.

**The cream-light `is_light` forward-compat demo (Override C, commit `bd40e79`) was added at user request mid-checkpoint** to verify the architecture's light-mode text-flip semantics; UAT Test 4 documents both its presence and the mechanical evidence that `--ink` / `--muted` / surface ramp / `--state-hover` flip correctly when `is_light` is true. Phase 4 must carry the same branch into `addons/neocade_theme/neocade_theme.gd`.

**Forbidden-surface audit (Test 9) was independently re-run** beyond Plan 04's PowerShell self-check — the orchestrator independently grepped Phase-3.4-only commits (`git log --grep="03.4"`) and confirmed zero production-surface modifications. The broader window audit's spike-file matches were correctly attributed to Phase 3.2 (out of Phase 3.4 scope).

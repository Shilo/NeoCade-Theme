---
phase: 03.4-visual-direction-flat-extruded-flat-mockup-approval-gate
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - .planning/mockups/3.4/
autonomous: false
requirements:
  - DESIGN-NEW-03
  - DESIGN-NEW-04
  - DESIGN-NEW-05
  - DESIGN-NEW-DYNAMIC-01
  - DESIGN-NEW-MOBILE-01
  - DESIGN-NEW-MOBILE-02
  - DESIGN-NEW-MOBILE-03
must_haves:
  truths:
    - "Only user-selected finalists from `.planning/mockups/3.4/finalist-selection.md` receive full-fidelity mockups."
    - "Each finalist is shown as one direction/subclass concept in the full 4-grid: flat desktop, flat mobile, raised desktop, raised mobile."
    - "Each finalist includes a color override preview row for plausible `base_color` and `accent_color` customization."
    - "Full-fidelity mockups demonstrate every required user-facing Control class and required state combinations, including `pressed_focus`, `checked_focus`, and `hover_pressed`."
    - "The user receives a blocking final approval checkpoint to choose N approved final themes and exactly one base direction; the executor must not auto-approve or auto-pick the base."
  artifacts:
    - .planning/mockups/3.4/finalist-gallery.html
    - .planning/mockups/3.4/final-approval.md
  key_links:
    - ".planning/mockups/3.4/finalist-selection.md"
    - ".planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CONTEXT.md D-08..D-19"
    - ".planning/research/MINIMAL-THEME-DISSECTION.md"
    - ".planning/research/MINIMAL-THEME-COVERAGE-DELTA.md"
    - ".planning/research/PITFALLS.md Pitfall 1.1"
---

<objective>
Build full-fidelity finalist mockups and present the final visual approval gate.

Purpose: prove selected directions across flat/raised and desktop/mobile, then collect the user's final theme approvals and base-direction choice before design tokens are written.
Output: finalist 4-grid gallery, coverage/state audit, and final approval decision record.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CONTEXT.md
@.planning/mockups/3.4/finalist-selection.md
@.planning/research/THEME-DIRECTIONS.md
@.planning/research/MINIMAL-THEME-DISSECTION.md
@.planning/research/MINIMAL-THEME-COVERAGE-DELTA.md
@.planning/research/PITFALLS.md
@.planning/research/CROSS-PLATFORM.md
@.planning/mockups/3.4/

<interfaces>
Per-finalist 4-grid:
- flat desktop: `raised=false`, `platform=DESKTOP`
- flat mobile: `raised=false`, `platform=MOBILE`
- raised desktop: `raised=true`, `platform=DESKTOP`
- raised mobile: `raised=true`, `platform=MOBILE`

Required state combinations:
- normal
- hover
- pressed
- focused
- disabled
- checked/selected where applicable
- `pressed_focus`
- `checked_focus`
- `hover_pressed`

Blocking checkpoint output:
- `.planning/mockups/3.4/final-approval.md`
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Load finalist selection and generate 4-grid pages</name>
  <read_first>
    - .planning/mockups/3.4/finalist-selection.md
    - .planning/mockups/3.4/data/
    - .planning/mockups/3.4/concept-gallery.html
  </read_first>
  <files>.planning/mockups/3.4/finalist-gallery.html</files>
  <action>
    Read `.planning/mockups/3.4/finalist-selection.md`. If it is missing or does not name 1-3 selected finalists, stop and return `## CHECKPOINT REACHED` instructing the user to complete Plan 02 finalist selection first.

    For each selected finalist, generate full-fidelity 4-grid mockup content. The grid cells must share the same direction data and differ only by `raised` and `platform` inputs.

    Include a color override preview row for each finalist with at least two plausible `base_color` / `accent_color` override examples. Keep override previews secondary to the default identity.
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$sel='.planning/mockups/3.4/finalist-selection.md'; if(!(Test-Path $sel)){ throw 'Missing finalist-selection.md' }; $gallery='.planning/mockups/3.4/finalist-gallery.html'; Test-Path $gallery; $doc=Get-Content -Raw $gallery; foreach($n in 'raised=false','raised=true','platform=DESKTOP','platform=MOBILE','base_color','accent_color','override') { if($doc -notmatch [regex]::Escape($n)) { throw \"Missing finalist grid marker: $n\" } }"
    </automated>
  </verify>
  <done>Every selected finalist has a 4-grid page plus color override preview.</done>
</task>

<task type="auto">
  <name>Task 2: Add full Control/state coverage matrix</name>
  <read_first>
    - .planning/research/MINIMAL-THEME-DISSECTION.md
    - .planning/research/MINIMAL-THEME-COVERAGE-DELTA.md
    - .planning/research/PITFALLS.md
    - .planning/mockups/3.4/finalist-gallery.html
  </read_first>
  <files>.planning/mockups/3.4/</files>
  <action>
    Expand finalist mockups with every required user-facing Control class and all required state combinations.

    Add a coverage matrix artifact that maps Controls/states to visible mockup locations. The matrix must explicitly mention Pitfall 1.1 state combinations `pressed_focus`, `checked_focus`, and `hover_pressed`.

    If a Control is represented by a composed mockup instead of a literal browser-native widget, document that composition and which Godot Control it stands for.
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$files=Get-ChildItem '.planning/mockups/3.4' -Recurse -File | Where-Object { $_.Name -match 'finalist|coverage|matrix|check|html|md' }; $blob=($files|Get-Content -Raw) -join \"`n\"; foreach($n in 'Button','CheckBox','CheckButton','OptionButton','MenuButton','ColorPickerButton','LinkButton','Label','RichTextLabel','LineEdit','TextEdit','CodeEdit','HSlider','VSlider','ProgressBar','HScrollBar','VScrollBar','SpinBox','ItemList','Tree','TabBar','TabContainer','FoldableContainer','PopupPanel','PopupMenu','AcceptDialog','ConfirmationDialog','FileDialog','TooltipPanel','TooltipLabel','Window','Panel','PanelContainer','ScrollContainer','SplitContainer','MarginContainer','MenuBar','ColorPicker','GraphEdit','GraphNode','GraphFrame','pressed_focus','checked_focus','hover_pressed') { if($blob -notmatch [regex]::Escape($n)) { throw \"Missing finalist coverage marker: $n\" } }"
    </automated>
  </verify>
  <done>Finalist mockups include a documented full Control/state coverage matrix.</done>
</task>

<task type="auto">
  <name>Task 3: Render finalist screenshots and run approval-readiness audit</name>
  <read_first>
    - .planning/mockups/3.4/render-check.md
    - .planning/research/PITFALLS.md
    - .planning/research/CROSS-PLATFORM.md
  </read_first>
  <files>.planning/mockups/3.4/</files>
  <action>
    Render finalist pages using the Plan 01 render path and update the render-check artifact with:
    - finalist list
    - four grid cells per finalist
    - mobile viewport/tap-target notes
    - color override row presence
    - anti-cyberpunk and anti-texture pass
    - overlap/text-fit sanity notes
    - no production addon/theme file changes
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$doc=Get-Content -Raw '.planning/mockups/3.4/render-check.md'; foreach($n in 'finalist','4-grid','mobile','tap','color override','anti-cyberpunk','anti-texture','text','overlap','PASS') { if($doc -notmatch [regex]::Escape($n)) { throw \"Missing finalist render-check marker: $n\" } }; $changed=git status --short; $forbidden=$changed|Select-String -Pattern '(^.. addons/|^.. main\.tscn$|^.. project\.godot$|\.tres$|\.gd$)'; if($forbidden){ throw \"Forbidden implementation changes detected: $forbidden\" }"
    </automated>
  </verify>
  <done>Finalist mockups are rendered/reviewable and approval-readiness audit is recorded.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Present final approval and base-direction checkpoint</name>
  <files>.planning/mockups/3.4/final-approval.md</files>
  <action>
    Present finalist full-fidelity mockups to the user.

    Ask the user to:
    1. approve N final themes from the finalist set, where N >= 1;
    2. choose exactly one approved theme as the `NeoCadeTheme` base direction;
    3. optionally request focused revisions before approval.

    If approved, write `.planning/mockups/3.4/final-approval.md` with:
    - approval date
    - approved final theme list
    - rejected/deferred finalist themes, if any
    - base direction
    - notes or revision caveats
    - explicit statement that Phase 4 may implement only after DESIGN_TOKENS.md is written in Plan 04

    If revisions are requested, update relevant finalist mockups, rerun Tasks 2-3 checks, and return to this checkpoint. Do not exceed the Phase 3.4 three-revision-round limit from CONTEXT.md D-26. If approval cannot be collected in the current runtime, return `## CHECKPOINT REACHED`.
  </action>
  <decision>Approve N final themes and choose exactly one `NeoCadeTheme` base direction.</decision>
  <context>
    Per D-16 through D-19, base direction is chosen only after full-fidelity finalist mockups are reviewed.
  </context>
  <verify>
    <manual>User approves at least one final theme and chooses exactly one base direction, or requests focused revisions.</manual>
    <automated>
      powershell -NoProfile -Command "$p='.planning/mockups/3.4/final-approval.md'; if(Test-Path $p){ $doc=Get-Content -Raw $p; foreach($n in 'Approved final themes','Base direction','NeoCadeTheme','approval date') { if($doc -notmatch [regex]::Escape($n)) { throw \"Missing final approval marker: $n\" } } }"
    </automated>
  </verify>
  <resume-signal>User approves final themes and base direction or requests revisions.</resume-signal>
  <done>Final approval/base-direction selection is recorded or execution pauses for the user's decision.</done>
</task>
</tasks>

<verification>
- [ ] `finalist-selection.md` exists before full-fidelity work starts.
- [ ] Each selected finalist has a 4-grid mockup.
- [ ] Every required Control and state combination is represented and mapped.
- [ ] Render-check audit passes anti-cyberpunk, anti-texture, mobile, text-fit, and forbidden-surface checks.
- [ ] User approval and base-direction selection are recorded or execution pauses.
</verification>

<success_criteria>
- DESIGN-NEW-03, DESIGN-NEW-04, DESIGN-NEW-05, DESIGN-NEW-DYNAMIC-01, and DESIGN-NEW-MOBILE-01..03 are materially satisfied at mockup level.
- The final approval gate produces the decision Plan 04 needs to write design tokens.
- No production `.tres` styling begins before approval.
</success_criteria>

<output>
After completion or checkpoint, create `.planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-03-SUMMARY.md` with finalists, coverage matrix status, render audit status, approval/base-direction outcome, and forbidden-surface audit.
</output>

# Phase 3.4: Visual Direction Mockup + Approval Gate (Flat / Extruded-Flat) - Context

**Gathered:** 2026-05-06 (updated from 2026-05-04 auto-draft after Phase 3.1, 3.2, and 3.3 completed)
**Status:** Paused at Wave 3 Plan 03.4-03 Task 4 user gate. Plans 01-02 complete; Plan 03 mockup/audit artifacts complete; `final-approval.md` is still missing. Pulse is selected as recommended starter / implementation priority; all five directions remain v1 variations.

<domain>
## Phase Boundary

Mock up the five user-approved directions from Phase 3.3 - **Pulse**, **Slate**, **Bubble**, **Daybreak**, and **Burst** - in the flat MD3 / MD3 Expressive + optional extruded-flat idiom. This phase is the hard visual approval gate before Phase 4 implements any production `.tres` styling.

The phase must prove the dynamic `NeoCadeTheme` architecture visually, but it must not implement production theme resources. The approved output is a mockup-driven design contract: final theme selection, recommended-starter designation, and `.planning/DESIGN_TOKENS.md` written as a single-class/data-resource-oriented handoff for Phase 4.

**In scope:**
- Fifteen Stage 1 concept boards: 5 approved directions x 3 rendered variants (`desktop-flat`, `mobile-flat`, `mobile-raised`).
- Lightweight representative Control slice in concept boards: enough real UI to judge direction personality, state treatment, hierarchy, and raised behavior without building the full matrix for all five directions.
- User selection of finalist directions after concept boards (closed: Pulse is the implementation-priority finalist; all five directions still ship in v1).
- Full-fidelity finalist mockups for Pulse, including the full flat x raised x desktop x mobile 4-grid.
- Dynamic-system proof in mockups: same direction data-resource concept shown across flat/raised, desktop/mobile, and `base_color` / `accent_color` override examples.
- Final approval gate where the user confirms approved final themes and exactly one recommended starter direction.
- `.planning/DESIGN_TOKENS.md` finalized as the Phase 4 contract.
- Anti-cyberpunk, anti-texture, accessibility, and state-combination audits before final approval.

**Out of scope:**
- Production `.tres` styling commits.
- Implementing `NeoCadeTheme` or production direction `.tres` resources.
- Re-deriving, renaming, or replacing the five Phase 3.3 approved theme directions.
- Re-running Phase 3.1, Phase 3.2, or Phase 3.3 research.
- Full Godot showcase scene implementation.
- Real-device export QA.

</domain>

<decisions>
## Implementation Decisions

### Approved Direction Inputs

- **D-01:** Phase 3.4 uses the five Phase 3.3 approved text-level directions as direct inputs: **Pulse**, **Slate**, **Bubble**, **Daybreak**, and **Burst**. Names, base colors, accent colors, personality summaries, target use cases, flat-mode behavior, raised-mode behavior, and mobile-sizing notes come from `.planning/research/THEME-DIRECTIONS.md`.
- **D-02:** Phase 3.4 does not invent replacement directions. If a planner or reviewer believes a direction itself is wrong, the resolution path is a focused Phase 3.3 revision, not silent redesign inside mockups.
- **D-03:** All five directions remained peer candidates through Stage 1. Plan 02 closed with Pulse selected as recommended starter / implementation priority; this has no architectural privilege.

### Stage 1 Concept Boards

- **D-04:** Stage 1 produces **15 concept boards**: each of the 5 approved directions shown in `desktop-flat`, `mobile-flat`, and `mobile-raised` variants.
- **D-05:** Stage 1 concept boards use a **representative slice**, not the full Control matrix. They must include enough real Controls to judge the direction: button/action hierarchy, text/input treatment, selection/list surface, at least one popup/dialog sample, focus/hover/pressed/disabled examples, and a small palette/type/radius swatch area.
- **D-06:** Stage 1 concept boards do not need desktop/mobile 4-grid coverage. They should focus on directional taste and raised-vs-flat feel so the user can choose 1-3 finalists without paying full-fidelity cost for all five.
- **D-07:** Stage 1 must still be honest about the dynamic architecture: the flat and raised boards for a direction are two renderings of the same direction, not two separate themes.

### Finalist Funnel

- **D-08:** After Stage 1, the user selected **Pulse** as the implementation-priority finalist and recommended starter; Slate, Bubble, Daybreak, and Burst remain v1 variations.
- **D-09:** Only Pulse receives the Plan 03 full-fidelity 4-grid. This preserves the hard approval gate while avoiding full-matrix work for every variation before implementation.
- **D-10:** Full-fidelity finalist mockups demonstrate every required user-facing Control class and required state combinations, including Pitfall 1.1 combinations such as `pressed_focus`, `checked_focus`, and `hover_pressed`.
- **D-11:** If the user wants a non-finalist revived after seeing finalist mockups, treat that as a focused revision/escalation checkpoint before expanding full-fidelity work.

### Dynamic System Proof

- **D-12:** Phase 3.4 mockups use **formula-driven HTML/CSS rendered via Playwright/Edge** as the visual contract. The CSS should mirror the dynamic single-class model: derived surface ramp from `base_color`, role/accent behavior from `accent_color`, raised-mode hard-offset shapes, and desktop/mobile sizing tokens.
- **D-13:** Full-fidelity finalist mockups show the same direction in the 4-grid:
  - `raised=false`, `platform=DESKTOP`
  - `raised=false`, `platform=MOBILE`
  - `raised=true`, `platform=DESKTOP`
  - `raised=true`, `platform=MOBILE`
- **D-14:** Finalist mockups include a small color-override preview row showing plausible consumer overrides of `base_color` and `accent_color`.
- **D-15:** Phase 3.4 must not expand the Godot spike or author production `.gd`/`.tres` implementation. Phase 3.2 already locked dynamic architecture feasibility; Phase 4 owns production implementation.

### Recommended Starter and Final Approval

- **D-16:** The recommended starter is chosen after mockup review. It sets the showcase default and README "try this first" suggestion only.
- **D-17:** The recommended starter's values do **not** become `NeoCadeTheme` class defaults. A consumer instantiating `NeoCadeTheme` directly gets the class defaults until they set exports or load a named direction `.tres`.
- **D-18:** Approved final directions ship as data-only `.tres` files of type `NeoCadeTheme` at the addon root: `pulse_neocade_theme.tres`, `slate_neocade_theme.tres`, `bubble_neocade_theme.tres`, `daybreak_neocade_theme.tres`, and `burst_neocade_theme.tres`.
- **D-19:** `.planning/DESIGN_TOKENS.md` records:
  - final approved theme list and which direction is the recommended starter;
  - per-direction `.tres` filename stem;
  - default `base_color` and `accent_color`;
  - 9 export values and Theme Editor override intent: radii, outlines, state behavior, accent discipline;
  - shared single-class contract: Inter Variable Roman, state-layer model, focus strategy, anti-texture rules, regeneration mechanics;
  - flat/raised and desktop/mobile variation behavior;
  - explicit note that production has no per-direction `.gd` classes and no subclass override contract.

### Variation Mechanics

- **D-20:** Flat variation (`raised=false`) keeps `shadow_size = -1` on every `StyleBoxFlat` in the final Godot implementation contract.
- **D-21:** Raised variation (`raised=true`) uses extruded-flat hard offset shapes only: solid top shape plus darker offset duplicate. No blur, soft drop shadow, glow, bevel gradient, texture, pattern, embossing, or painterly chrome.
- **D-22:** Raised behavior is strongest on buttons and button-like controls, subtle on tabs/chips/lists/range handles, rare on popup/dialog shells, and absent from passive labels/separators.
- **D-23:** Desktop/mobile sizing follows the dynamic `platform` export model. Desktop uses denser desktop constants; mobile uses larger body text and touch targets. Corner radii remain part of brand identity and do not automatically change just because platform changes.
- **D-24:** `platform=AUTO` behavior is documented from Phase 3.2 but does not need visual proof beyond showing desktop and mobile outputs; Phase 10 owns real target/device validation.

### Review and Revision Gates

- **D-25:** Every concept and finalist board must pass anti-cyberpunk and anti-texture review before user selection or approval.
- **D-26:** Maximum targeted revision rounds remain 3 for Phase 3.4. If final approval is not reached by round 3, stop for escalation before continuing.
- **D-27:** Phase 4 cannot begin until the user approval decision and recommended starter selection are written in Phase 3.4 outputs.

### Agent's Discretion

- Exact representative Control slice composition for Stage 1, as long as it covers button/action hierarchy, input, selection/list, popup/dialog, and state samples.
- Exact HTML/CSS file structure and Playwright screenshot script shape.
- Exact presentation layout of the concept gallery and finalist gallery.
- Exact order in which Pulse, Slate, Bubble, Daybreak, and Burst appear, as long as the ordering is not used to imply architectural privilege.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Direct Inputs
- `.planning/research/THEME-DIRECTIONS.md` - five user-approved directions: Pulse, Slate, Bubble, Daybreak, Burst.
- `.planning/research/MD3-RESEARCH.md` - flat MD3 / MD3 Expressive visual grammar and flat-vs-raised guidance.
- `.planning/research/FLAT-3D-UI-RESEARCH.md` - extruded-flat construction rules, raised Control-family matrix, and wrapper/two-layer guidance.
- `.planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md` - dynamic `NeoCadeTheme` feasibility research and strict feasibility outcome.
- `.planning/spikes/dynamic-theme/VERIFY-RESULTS.md` - Godot 4.6.2 proof that export-driven regeneration, saved `.tres` runtime application, serialization, and AUTO simulation passed. Subclass positive/negative controls are historical research evidence, not the production architecture.
- `.planning/research/SOURCES.md` Sections 11-14 - source dossier updates for MD3, Flat-3D, dynamic theme architecture, and theme directions.

### Project Canon
- `.planning/PROJECT.md` - hard constraints, architecture lock, Inter-only font decision, and approved theme direction key decision.
- `.planning/ROADMAP.md` - Phase 3.4 goal, dependencies, success criteria, and Phase 4 dependency on approval.
- `.planning/REQUIREMENTS.md` - requirement IDs and traceability; note that older static mobile `.tres` and subclass language is superseded by PROJECT/ROADMAP single-class architecture decisions.
- `.planning/STATE.md` - active phase state and sequencing guard.

### Coverage and Pitfall Inputs
- `.planning/research/MINIMAL-THEME-DISSECTION.md` - godot-minimal-theme coverage bar and state enumeration discipline.
- `.planning/research/MINIMAL-THEME-COVERAGE-DELTA.md` - 37-row coverage comparison axis.
- `.planning/research/PITFALLS.md` - focus-overlay/state-combination pitfalls, popup Window inheritance pitfalls, rendering constraints, and anti-feature rules.
- `.planning/research/CROSS-PLATFORM.md` - mobile sizing and export validation context.
- `.planning/research/FONT-REVIEW.md` - Inter Variable Roman only in v1.

### Historical Reference Only
- `.planning/phases/03-visual-direction-mockup-approval-gate/REDIRECTED.md` - user feedback log from the rejected painterly direction.
- `.planning/mockups/concepts/*.png` - v0 painterly concept images for color/personality DNA only.
- `.planning/mockups/03-direction-boards.html` - v0 board-gallery structure reference only.
- `.planning/mockups/03-direction-boards.md` - v0 board-gallery notes, historical only.
- `.planning/research/mood-board/INDEX.md` - earlier arcade references downgraded to filtered inspiration only.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `.planning/mockups/03-direction-boards.html` and related screenshots provide a prior HTML-gallery pattern; visual treatment must be rebuilt for flat MD3.
- `.planning/spikes/dynamic-theme/` provides representative dynamic architecture proof, but remains research-only and must not be copied into production during Phase 3.4.
- `addons/neocade_theme/neocade_theme.tres` is an empty scaffold and must remain unstyled in Phase 3.4.
- `main.tscn` is a minimal Control root using the scaffold theme; showcase implementation remains later work.

### Established Patterns
- HTML/CSS mockups rendered via Playwright/Edge are the existing mockup workflow.
- Phase 1/2 evidence discipline applies: provenance, explicit adoption/rejection, and no fallback-masked coverage claims.
- Phase 3.1 raised-mode rule applies: extruded-flat is an affordance amplifier, not decorative 3D.
- Phase 3.2 subclass evidence is historical; production personality comes from data `.tres` export values and Theme Editor overrides.

### Integration Points
- `.planning/mockups/3.4/` - new output directory for Phase 3.4 mockups and screenshots.
- `.planning/DESIGN_TOKENS.md` - final Phase 3.4 handoff consumed by Phase 4.
- Phase 4 implementation will use the final approved direction list and recommended-starter designation from Phase 3.4.

</code_context>

<specifics>
## Specific Ideas

- Pulse, Slate, Bubble, Daybreak, and Burst remain v1 direction resources; Pulse is the Plan 02 selected recommended starter / implementation priority.
- Stage 1 must show both flat and raised for every direction because raised can change the perceived personality of a theme.
- Full fidelity belongs only to finalists, so the user can make a taste cut before exhaustive Control/state work.
- The finalist 4-grid is the central proof that one direction can regenerate across flat/raised and desktop/mobile from the same dynamic single-class model.
- Color override previews should be small and clearly secondary; the primary decision is the default direction identity, not a full dynamic-color product.
- The recommended starter should remain a taste call, not an architectural default.

</specifics>

<deferred>
## Deferred Ideas

- Implementing `NeoCadeTheme` and direction `.tres` resources - Phase 4.
- Full 37-row production theme matrix - Phases 5-7.
- Mobile branch implementation and tap-target audit - Phase 8.
- Showcase scene and runtime theme toggles - Phase 9.
- Real-device/mobile/Web export validation - Phase 10.
- Light mode, alternate palettes, additional theme directions beyond the approved v1 set - v2 or v1.x.

</deferred>

---

*Phase: 3.4-visual-direction-flat-extruded-flat-mockup-approval-gate*
*Context gathered: 2026-05-06*

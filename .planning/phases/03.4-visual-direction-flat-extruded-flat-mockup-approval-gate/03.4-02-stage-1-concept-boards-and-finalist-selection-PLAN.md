---
phase: 03.4-visual-direction-flat-extruded-flat-mockup-approval-gate
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - .planning/mockups/3.4/
autonomous: false
status: REDIRECTED 2026-05-06b — first execution rejected; re-execution required under direction-shape-language-spec.md and 03.4-CORRECTIVE-ADDENDUM.md
requirements:
  - DESIGN-NEW-01
  - DESIGN-NEW-02
  - DESIGN-NEW-03
  - DESIGN-NEW-DYNAMIC-01
must_haves:
  truths:
    - "Stage 1 produces exactly 15 concept PNGs: 5 directions (Pulse, Slate, Bubble, Daybreak, Burst) × 3 variants each (desktop-flat, mobile-flat, mobile-raised). The 10-board logical matrix from CONTEXT.md D-04 (5 directions × flat + raised) is satisfied by these 15 images: every direction gets both flat and raised representation."
    - "Mockups follow the same-screen contract: identical control inventory, identical control order, identical content/labels across all five directions, all dark mode. See direction-shape-language-spec.md 'Constants' section."
    - "Mockups commit specific values per direction on the ten shape-language axes from direction-shape-language-spec.md 'Required-to-vary per direction' section: corner radius, button anatomy, chip/tab shape, brand mark style, density, focus-ring style, type weights, surface ramp depth, state-layer behavior, raised offset depth. Color tokens alone are not sufficient differentiation."
    - "Each concept image includes the fixed control inventory: brand mark + nav tabs, action panel (primary/secondary/ghost buttons + input + toggle row), dialog stack (segmented controls + popup + progress + dialog actions), list/tree (selected row + 2 normal rows + scrollbar), state strip (normal/hover/focus/pressed/disabled), palette strip (surface-low/surface-panel/surface-high/accent)."
    - "Flat and raised boards for a direction are presented as two renderings of the same dynamic direction, not separate themes."
    - "Mockups pass a greyscale sufficiency test (D-30): a colorblind reviewer must still be able to identify each direction by shape language."
    - "The user receives a blocking checkpoint to select 1-3 finalists; the executor must not auto-select finalists."
  artifacts:
    - .planning/mockups/3.4/concept-gallery.html
    - .planning/mockups/3.4/concepts/{pulse,slate,bubble,daybreak,burst}-{desktop-flat,mobile-flat,mobile-raised}.png
    - .planning/mockups/3.4/render-check.md (with greyscale sufficiency results per direction)
    - .planning/mockups/3.4/finalist-selection.md
  key_links:
    - ".planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CONTEXT.md D-04..D-11, D-25..D-27"
    - ".planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CORRECTIVE-ADDENDUM.md D-28, D-29, D-30 (binding for re-execution)"
    - ".planning/mockups/3.4/image-prompts/direction-shape-language-spec.md (authoritative spec; replaces fixed-control-order-spec.md)"
    - ".planning/research/THEME-DIRECTIONS.md (Revision Round 2/2 applied — all dark; Bubble #241326 + #FFB3E6; Daybreak #0B2420 + #76F2D1)"
    - ".planning/mockups/3.4/README.md"
    - ".planning/mockups/3.4/data/directions.json"
    - ".planning/mockups/3.4/wcag-palette-audit.md"
---

<objective>
Render and audit the Stage 1 concept boards, then stop at the finalist-selection checkpoint.

Purpose: let the user choose 1-3 directions after seeing both flat and raised versions for every approved direction.
Output: concept gallery, screenshots/check report, and recorded finalist-selection decision.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CONTEXT.md
@.planning/research/THEME-DIRECTIONS.md
@.planning/research/MD3-RESEARCH.md
@.planning/research/FLAT-3D-UI-RESEARCH.md
@.planning/mockups/3.4/

<interfaces>
Stage 1 board matrix:
- Pulse flat
- Pulse raised
- Slate flat
- Slate raised
- Bubble flat
- Bubble raised
- Daybreak flat
- Daybreak raised
- Burst flat
- Burst raised

Representative Control slice required on each board:
- primary button/action group
- secondary or ghost button
- text input or search field
- checkbox/toggle or segmented control
- tab/list/menu selection sample
- popup/dialog sample
- hover/focus/pressed/disabled state strip
- palette/type/radius swatches

Blocking checkpoint output:
- `.planning/mockups/3.4/finalist-selection.md`
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 0: Cleanup discarded first-execution outputs</name>
  <read_first>
    - .planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CORRECTIVE-ADDENDUM.md
  </read_first>
  <files>.planning/mockups/3.4/concepts/</files>
  <action>
    Per CORRECTIVE-ADDENDUM disposition table, the 15 PNGs from the first execution of Plan 02 must be discarded before re-rendering — they encode the wrong (single-template, color-only-differentiation) contract and risk being mistaken for the new outputs.

    Remove every existing `*-{desktop-flat,mobile-flat,mobile-raised}.png` from `.planning/mockups/3.4/concepts/`. Do NOT remove any file outside that directory. Do NOT remove `.planning/mockups/concepts/` (those are the v0 historical concepts, preserved by the Phase 3 redirect — not Phase 3.4 outputs).
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$dir='.planning/mockups/3.4/concepts'; if(Test-Path $dir){ $remaining=Get-ChildItem $dir -Filter '*.png' -ErrorAction SilentlyContinue; if($remaining){ throw 'First-execution PNGs still present in concepts dir' } }"
    </automated>
  </verify>
  <done>The concepts directory is empty of PNGs and ready for re-rendering under the new spec.</done>
</task>

<task type="auto">
  <name>Task 1: Commit per-direction shape-language token values</name>
  <read_first>
    - .planning/mockups/3.4/CLAUDE-CODE-HANDOFF.md (READ FIRST — its "Mood priming" section is the lens for everything below)
    - .planning/mockups/3.4/image-prompts/direction-shape-language-spec.md
    - .planning/research/THEME-DIRECTIONS.md
    - .planning/research/MD3-RESEARCH.md
    - .planning/research/FLAT-3D-UI-RESEARCH.md
    - .planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CORRECTIVE-ADDENDUM.md
  </read_first>
  <files>
    .planning/mockups/3.4/data/directions.json
    .planning/mockups/3.4/src/neocade-mockups.css
  </files>
  <action>
    **Internalize the per-direction mood priming from CLAUDE-CODE-HANDOFF.md before picking any number.** Each direction has a distinct emotional target — Pulse "vibrant arcade hall by day", Slate "premium iOS-style polish", Bubble "playful candy-counter at night", Daybreak "fresh evening lobby", Burst "celebratory event-like". The 10 shape-language axes are TOOLS for expressing these moods, not the moods themselves.

    For each of the five directions (Pulse, Slate, Bubble, Daybreak, Burst), commit specific token values on each of the ten shape-language axes from `direction-shape-language-spec.md` "Required-to-vary per direction" section. The committed values must:

    - Match the direction's documented personality from `THEME-DIRECTIONS.md` AND the mood priming in `CLAUDE-CODE-HANDOFF.md`.
    - Be distinct from the same axis on at least 3 of the other 4 directions.
    - Use the dark palettes locked in `THEME-DIRECTIONS.md` Revision Round 2/2 (no light themes).

    Encode the committed values either as a `shape_language` block under each direction in `data/directions.json`, OR as per-direction CSS variable scopes in `src/neocade-mockups.css` (e.g., `[data-direction="Bubble"] { --radius-base: 18px; --button-padding: 20px 14px; ... }`). Both is also acceptable.

    The shared `.nc-artboard` CSS rules MUST consume the per-direction tokens, not hard-code values like `--radius: 12px`. The previous CSS hard-coded that radius for all five directions; this task removes that.

    Preserve the constants from the shape-language spec (control inventory, control order, label/content text, dark mode, aspect ratios, brand-mark slot, nav-tabs slot, body grid, footer slots, type family).

    **Before declaring this task done, run the mood self-check from CLAUDE-CODE-HANDOFF.md:** for each direction, ask "if a stranger looked at this mockup with all color removed, would they correctly guess which one is the playful candy-counter vs the premium iOS-style vs the celebratory event?" If the answer is no for any direction, push the differentiation harder on more axes BEFORE moving to Task 2 rendering. The greyscale test in Task 3 is the formal gate; this self-check catches problems earlier.
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$j=Get-Content -Raw '.planning/mockups/3.4/data/directions.json'; $css=Get-Content -Raw '.planning/mockups/3.4/src/neocade-mockups.css'; $blob=$j+\"`n\"+$css; $directions='Pulse','Slate','Bubble','Daybreak','Burst'; foreach($d in $directions){ $hasShape=$blob -match ('(?ms)' + [regex]::Escape($d) + '.{0,4000}(radius|shape_language|--radius|button|chip|brand|focus|density)'); if(-not $hasShape){ throw \"Direction $d has no shape-language commitment in data or CSS\" } }; if($css -match 'nc-artboard.*--radius:\s*12px' -or $css -match 'nc-board.*--radius:\s*12px'){ throw 'Hard-coded --radius: 12px on artboard/board still present; per-direction tokens not consumed' }; foreach($d in $directions){ if(($blob|Select-String -Pattern $d -AllMatches).Matches.Count -lt 1){ throw \"Direction missing entirely: $d\" } }"
    </automated>
  </verify>
  <done>Each direction has committed values on all ten shape-language axes; the artboard consumes per-direction tokens rather than hard-coded shared values.</done>
</task>

<task type="auto">
  <name>Task 2: Render the 15 concept PNGs</name>
  <read_first>
    - .planning/mockups/3.4/image-prompts/direction-shape-language-spec.md
    - .planning/mockups/3.4/concept-image.html
    - .planning/mockups/3.4/render.js
    - .planning/mockups/3.4/data/directions.json
    - .planning/research/PITFALLS.md
  </read_first>
  <files>.planning/mockups/3.4/concepts/</files>
  <action>
    Render exactly 15 concept PNGs: 5 directions × 3 variants each. Filenames follow the existing convention from Plan 01:

    - `{direction-stem}-desktop-flat.png` (1280×720, raised=false, platform=DESKTOP)
    - `{direction-stem}-mobile-flat.png` (430×932, raised=false, platform=MOBILE)
    - `{direction-stem}-mobile-raised.png` (430×932, raised=true, platform=MOBILE)

    Use the rendering path established by Plan 01 (`render.js` + bundled Playwright/Edge runtime, or browser-direct rendering of `concept-image.html` with appropriate query parameters).

    Every rendered image must:

    - Use the dark palettes from THEME-DIRECTIONS.md Revision 2/2.
    - Use the constants from direction-shape-language-spec.md (control inventory, order, content, dark mode, slots).
    - Express the per-direction shape-language token values committed in Task 1.
    - Pass anti-cyberpunk, anti-texture, anti-painterly-chrome, anti-embossing review per Phase 3 redirect rules.
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$dir='.planning/mockups/3.4/concepts'; $expected=@(); foreach($d in 'pulse','slate','bubble','daybreak','burst'){ foreach($v in 'desktop-flat','mobile-flat','mobile-raised'){ $expected+= \"$d-$v.png\" } }; foreach($f in $expected){ $p=Join-Path $dir $f; if(-not (Test-Path $p)){ throw \"Missing concept image: $f\" }; $size=(Get-Item $p).Length; if($size -lt 5000){ throw \"Concept image suspiciously small (<5KB): $f\" } }; if((Get-ChildItem $dir -Filter '*.png').Count -ne 15){ throw 'Concept dir does not contain exactly 15 PNGs' }"
    </automated>
  </verify>
  <done>15 concept PNGs are present with reasonable file sizes and follow the naming convention.</done>
</task>

<task type="auto">
  <name>Task 3: Render-check audit including greyscale sufficiency test (D-30)</name>
  <read_first>
    - .planning/mockups/3.4/render-check.md
    - .planning/mockups/3.4/image-prompts/direction-shape-language-spec.md
    - .planning/research/FLAT-3D-UI-RESEARCH.md
    - .planning/research/PITFALLS.md
    - .planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CORRECTIVE-ADDENDUM.md
  </read_first>
  <files>.planning/mockups/3.4/render-check.md</files>
  <action>
    Update `render-check.md` with the following audit rows per direction (Pulse, Slate, Bubble, Daybreak, Burst):

    1. anti-cyberpunk PASS/FAIL with one-line note
    2. anti-texture PASS/FAIL with one-line note
    3. anti-painterly-chrome / anti-embossing PASS/FAIL
    4. raised-mode feel (too subtle / appropriate / too toy-like) with one-line note
    5. dark-only compliance PASS/FAIL (D-28)
    6. shape-language differentiation: list which of the 10 axes (D-29) this direction visibly differs from the other 4 on
    7. **greyscale sufficiency test (D-30):** convert each direction's desktop-flat PNG to greyscale, place all 5 greyscale renders side by side, and record whether each direction is still identifiable by shape alone. PASS = identifiable AS the right mood (e.g., the cabinet-control-panel one is identifiable as Pulse, not just "the small-radius one"). FAIL = visually merges with at least one other direction OR the shape is distinct but reads as the wrong mood (e.g., Bubble greyscale that looks "minimal" instead of "playful candy-counter"). If FAIL, the affected direction must be re-committed on more axes in Task 1 and re-rendered in Task 2 before this audit can PASS. Refer to CLAUDE-CODE-HANDOFF.md "Mood priming" section for each direction's intended emotional target.

    Also include:
    - confirmation that no v0 historical outputs (`.planning/mockups/concepts/`, `.planning/mockups/03-direction-boards*`) were modified
    - confirmation that no production files were modified (`addons/`, `main.tscn`, `project.godot`, any `.tres` or `.gd`)
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$doc=Get-Content -Raw '.planning/mockups/3.4/render-check.md'; foreach($n in 'Pulse','Slate','Bubble','Daybreak','Burst','anti-cyberpunk','anti-texture','greyscale','PASS','dark-only','shape-language','historical') { if($doc -notmatch [regex]::Escape($n)) { throw \"Missing render-check marker: $n\" } }; $changed=git status --short; $forbidden=$changed|Select-String -Pattern '(^.. addons/|^.. main\.tscn$|^.. project\.godot$|\.tres$|\.gd$|^.. \.planning/mockups/concepts/|^.. \.planning/mockups/03-direction-boards)'; if($forbidden){ throw \"Forbidden implementation/historical changes detected: $forbidden\" }"
    </automated>
  </verify>
  <done>Render-check audit covers all five directions on six audit rows plus the greyscale sufficiency test, with PASS recorded per direction.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Present finalist-selection checkpoint</name>
  <files>.planning/mockups/3.4/finalist-selection.md</files>
  <action>
    Present the 10 concept boards to the user and ask them to select 1-3 finalist directions.

    The user-facing prompt must list all five directions and make clear that selecting a finalist chooses the direction, not only its flat or raised variant. Both variants remain supported for every finalist.

    Allowed outcomes:
    - user selects 1 finalist
    - user selects 2 finalists
    - user selects 3 finalists
    - user requests focused revisions to Stage 1 boards before selection

    If the user selects finalists, write `.planning/mockups/3.4/finalist-selection.md` with date, selected finalists, any notes, and any rejected/deferred directions. If the user requests revisions, update the relevant boards and rerun Task 2 before presenting the checkpoint again. Do not auto-select finalists.

    Return `## CHECKPOINT REACHED` if the executor cannot collect the decision in the current runtime.
  </action>
  <decision>Select 1-3 finalist directions from Pulse, Slate, Bubble, Daybreak, and Burst.</decision>
  <context>
    Per D-08 and D-09, only selected finalists receive full-fidelity mockups in Plan 03.
  </context>
  <options>
    <option id="select-1-to-3">
      <name>Select 1-3 finalists</name>
      <pros>Moves full-fidelity effort to the directions the user likes most.</pros>
      <cons>Non-finalists will not get exhaustive Control/state mockups in this phase.</cons>
    </option>
    <option id="revise-concepts">
      <name>Revise specific concept boards</name>
      <pros>Fixes direction rendering problems before the taste cut.</pros>
      <cons>Costs one focused revision pass before full-fidelity work starts.</cons>
    </option>
  </options>
  <verify>
    <manual>User selects 1-3 finalists or requests specific revisions.</manual>
    <automated>
      powershell -NoProfile -Command "$p='.planning/mockups/3.4/finalist-selection.md'; if(Test-Path $p){ $doc=Get-Content -Raw $p; if($doc -notmatch 'Selected finalists'){ throw 'Finalist selection file exists but lacks Selected finalists marker' } }"
    </automated>
  </verify>
  <resume-signal>User selects 1-3 finalists or requests concept revisions.</resume-signal>
  <done>Finalist selection is recorded or execution pauses for the user's decision.</done>
</task>
</tasks>

<verification>
- [ ] First-execution PNGs in `.planning/mockups/3.4/concepts/` are removed before re-rendering.
- [ ] Exactly 15 concept PNGs exist (5 directions × 3 variants), filenames match `{direction}-{desktop-flat,mobile-flat,mobile-raised}.png`.
- [ ] Each direction has committed values on all 10 shape-language axes from `direction-shape-language-spec.md` (recorded in `data/directions.json` under a `shape_language` block, in CSS as per-direction variable scopes, or both).
- [ ] The shared artboard CSS no longer hard-codes `--radius: 12px` (or any other shape-language token) for all five directions; each direction's tokens flow from per-direction scope.
- [ ] Every board includes the fixed control inventory (brand mark + nav tabs + action panel + dialog stack + list/tree + state strip + palette strip) in the fixed order from `direction-shape-language-spec.md`.
- [ ] All five directions use dark base colors (D-28); palettes match `THEME-DIRECTIONS.md` Revision Round 2/2.
- [ ] `render-check.md` records PASS for: anti-cyberpunk, anti-texture, anti-painterly-chrome, raised-mode feel, dark-only compliance, shape-language differentiation, and greyscale sufficiency test (D-30) per direction.
- [ ] Greyscale sufficiency test passes for all five directions — each direction is identifiable by shape language alone in greyscale.
- [ ] User selected 1-3 finalists or requested revisions.
- [ ] No production addon/theme files changed.
- [ ] No v0 historical outputs (`.planning/mockups/concepts/`, `.planning/mockups/03-direction-boards*`) modified.
</verification>

<success_criteria>
- The user has seen flat and raised versions for all five directions, on the same comparison screen, with each direction expressing its personality through committed shape-language token values rather than color alone.
- Greyscale sufficiency test passes — directions are identifiable without color.
- Finalist selection is recorded for Plan 03.
- Stage 1 stays representative, not full-matrix.
- Phase 3.4 corrective addendum decisions D-28 (dark only), D-29 (per-direction shape-language tokens), and D-30 (greyscale sufficiency) are honored.
</success_criteria>

<output>
After completion or checkpoint, create `.planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-02-SUMMARY.md` with concept board matrix, render/audit result, checkpoint outcome, and forbidden-surface audit.
</output>

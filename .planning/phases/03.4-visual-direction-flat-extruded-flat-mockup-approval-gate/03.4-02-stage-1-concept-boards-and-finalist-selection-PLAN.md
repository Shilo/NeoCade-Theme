---
phase: 03.4-visual-direction-flat-extruded-flat-mockup-approval-gate
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - .planning/mockups/3.4/
autonomous: false
requirements:
  - DESIGN-NEW-01
  - DESIGN-NEW-02
  - DESIGN-NEW-03
  - DESIGN-NEW-DYNAMIC-01
must_haves:
  truths:
    - "Stage 1 produces exactly 10 concept boards: Pulse, Slate, Bubble, Daybreak, and Burst, each in flat and raised modes."
    - "Concept boards use a representative Control slice only; they do not attempt the full Control/state matrix for all five directions."
    - "Each concept board includes real UI surfaces: button/action hierarchy, input treatment, selection/list treatment, popup/dialog sample, state samples, and palette/type/radius swatches."
    - "Flat and raised boards for a direction are presented as two renderings of the same dynamic direction, not separate themes."
    - "The user receives a blocking checkpoint to select 1-3 finalists; the executor must not auto-select finalists."
  artifacts:
    - .planning/mockups/3.4/concept-gallery.html
    - .planning/mockups/3.4/finalist-selection.md
  key_links:
    - ".planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CONTEXT.md D-04..D-11, D-25..D-27"
    - ".planning/mockups/3.4/README.md"
    - ".planning/mockups/3.4/data/"
---

<objective>
Render and audit the Stage 1 concept boards, then stop at the finalist-selection checkpoint.

Purpose: let the user choose 1-3 directions after seeing both flat and raised versions for every approved direction.
Output: concept gallery, screenshots/check report, and recorded finalist-selection decision.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-CONTEXT.md
@.planning/research/THEME-DIRECTIONS.md
@.planning/research/MD3-RESEARCH.md
@.planning/research/FLAT-3D-UI-RESEARCH.md
@.planning/mockups/3.4/

<interfaces>
Stage 1 board matrix:
- Pulse flat
- Pulse raised
- Slate flat
- Slate raised
- Bubble flat
- Bubble raised
- Daybreak flat
- Daybreak raised
- Burst flat
- Burst raised

Representative Control slice required on each board:
- primary button/action group
- secondary or ghost button
- text input or search field
- checkbox/toggle or segmented control
- tab/list/menu selection sample
- popup/dialog sample
- hover/focus/pressed/disabled state strip
- palette/type/radius swatches

Blocking checkpoint output:
- `.planning/mockups/3.4/finalist-selection.md`
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Populate the 10 concept boards</name>
  <read_first>
    - .planning/mockups/3.4/data/
    - .planning/mockups/3.4/README.md
    - .planning/research/THEME-DIRECTIONS.md
  </read_first>
  <files>.planning/mockups/3.4/concept-gallery.html</files>
  <action>
    Populate `concept-gallery.html` or equivalent Stage 1 gallery so it renders exactly 10 boards: one flat and one raised board for each approved direction.

    Every board must include the representative Control slice from `<interfaces>`. Keep boards lightweight, but do not reduce them to mood boards. Use direction-specific personality from `THEME-DIRECTIONS.md` while staying inside the shared formula/render system from Plan 01.
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$files=Get-ChildItem '.planning/mockups/3.4' -Recurse -File | Where-Object { $_.Name -match 'concept|gallery|html|js|css' }; $blob=($files|Get-Content -Raw) -join \"`n\"; foreach($d in 'Pulse','Slate','Bubble','Daybreak','Burst'){ if(($blob|Select-String -Pattern $d -AllMatches).Matches.Count -lt 2){ throw \"Direction does not appear at least twice for flat/raised: $d\" } }; foreach($n in 'flat','raised','button','input','dialog','focus','hover','pressed','disabled','palette','radius') { if($blob -notmatch [regex]::Escape($n)) { throw \"Missing concept marker: $n\" } }"
    </automated>
  </verify>
  <done>All 10 concept boards are present with representative Controls and state samples.</done>
</task>

<task type="auto">
  <name>Task 2: Render concept screenshots and audit filters</name>
  <read_first>
    - .planning/mockups/3.4/render-check.md
    - .planning/research/FLAT-3D-UI-RESEARCH.md
    - .planning/research/PITFALLS.md
  </read_first>
  <files>.planning/mockups/3.4/</files>
  <action>
    Render the concept gallery to screenshot(s) using the Plan 01 render path. If automated screenshot tooling is unavailable, document the manual render path and capture enough evidence for the user to review in the browser.

    Update the render-check artifact with:
    - list of all 10 boards
    - anti-cyberpunk pass/fail per direction
    - anti-texture pass/fail per direction
    - notes on raised mode feeling too subtle/too toy-like if visible
    - confirmation that no v0 historical outputs were overwritten
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$doc=Get-Content -Raw '.planning/mockups/3.4/render-check.md'; foreach($n in 'Pulse','Slate','Bubble','Daybreak','Burst','anti-cyberpunk','anti-texture','PASS','flat','raised','historical') { if($doc -notmatch [regex]::Escape($n)) { throw \"Missing concept audit marker: $n\" } }; $changed=git status --short; $forbidden=$changed|Select-String -Pattern '(^.. addons/|^.. main\.tscn$|^.. project\.godot$|\.tres$|\.gd$)'; if($forbidden){ throw \"Forbidden implementation changes detected: $forbidden\" }"
    </automated>
  </verify>
  <done>Concept boards are rendered or reviewable, and filter audit is documented.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Present finalist-selection checkpoint</name>
  <files>.planning/mockups/3.4/finalist-selection.md</files>
  <action>
    Present the 10 concept boards to the user and ask them to select 1-3 finalist directions.

    The user-facing prompt must list all five directions and make clear that selecting a finalist chooses the direction, not only its flat or raised variant. Both variants remain supported for every finalist.

    Allowed outcomes:
    - user selects 1 finalist
    - user selects 2 finalists
    - user selects 3 finalists
    - user requests focused revisions to Stage 1 boards before selection

    If the user selects finalists, write `.planning/mockups/3.4/finalist-selection.md` with date, selected finalists, any notes, and any rejected/deferred directions. If the user requests revisions, update the relevant boards and rerun Task 2 before presenting the checkpoint again. Do not auto-select finalists.

    Return `## CHECKPOINT REACHED` if the executor cannot collect the decision in the current runtime.
  </action>
  <decision>Select 1-3 finalist directions from Pulse, Slate, Bubble, Daybreak, and Burst.</decision>
  <context>
    Per D-08 and D-09, only selected finalists receive full-fidelity mockups in Plan 03.
  </context>
  <options>
    <option id="select-1-to-3">
      <name>Select 1-3 finalists</name>
      <pros>Moves full-fidelity effort to the directions the user likes most.</pros>
      <cons>Non-finalists will not get exhaustive Control/state mockups in this phase.</cons>
    </option>
    <option id="revise-concepts">
      <name>Revise specific concept boards</name>
      <pros>Fixes direction rendering problems before the taste cut.</pros>
      <cons>Costs one focused revision pass before full-fidelity work starts.</cons>
    </option>
  </options>
  <verify>
    <manual>User selects 1-3 finalists or requests specific revisions.</manual>
    <automated>
      powershell -NoProfile -Command "$p='.planning/mockups/3.4/finalist-selection.md'; if(Test-Path $p){ $doc=Get-Content -Raw $p; if($doc -notmatch 'Selected finalists'){ throw 'Finalist selection file exists but lacks Selected finalists marker' } }"
    </automated>
  </verify>
  <resume-signal>User selects 1-3 finalists or requests concept revisions.</resume-signal>
  <done>Finalist selection is recorded or execution pauses for the user's decision.</done>
</task>
</tasks>

<verification>
- [ ] Exactly 10 concept boards exist or are rendered in the gallery.
- [ ] Every board includes representative real Controls and state samples.
- [ ] Anti-cyberpunk and anti-texture checks are recorded before user selection.
- [ ] User selected 1-3 finalists or requested revisions.
- [ ] No production addon/theme files changed.
</verification>

<success_criteria>
- The user has seen flat and raised versions for all five directions.
- Finalist selection is recorded for Plan 03.
- Stage 1 stays representative, not full-matrix.
</success_criteria>

<output>
After completion or checkpoint, create `.planning/phases/03.4-visual-direction-flat-extruded-flat-mockup-approval-gate/03.4-02-SUMMARY.md` with concept board matrix, render/audit result, checkpoint outcome, and forbidden-surface audit.
</output>

---
phase: 07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - addons/neocade_theme/neocade_theme.gd
  - addons/neocade_theme/icons/popup_submenu.svg
  - addons/neocade_theme/icons/popup_submenu.svg.import
  - addons/neocade_theme/icons/popup_submenu_mirrored.svg
  - addons/neocade_theme/icons/popup_submenu_mirrored.svg.import
  - .planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/helpers/_phase7_verify_headless.gd
autonomous: true
requirements:
  - COV-06
  - COV-08
  - COV-01
  - COV-07
  - COV-09
must_haves:
  truths:
    - "D-03: Window, PopupPanel, PopupMenu, AcceptDialog, ConfirmationDialog, TooltipPanel, and TooltipLabel are first-class theme types; no parent override inheritance is assumed."
    - "D-04: Popup/dialog shells use solid tonal surfaces, subtle borders, existing radius tokens, and the v1 no-shadow/no-glow/no-texture policy."
    - "D-05: Window chrome is quiet but complete: title colors/fonts, close icons, embedded border/unfocused variants, and spacing/offset constants."
    - "D-06: AcceptDialog and ConfirmationDialog reuse established internal Button/Label/container recipes where applicable but still get explicit shell/chrome class entries."
    - "D-07: Tooltip readability is non-negotiable; TooltipPanel is compact structure while TooltipLabel owns readable text color, font size, and padding."
    - "D-08: MenuBar triggers align with Button/MenuButton/Tab language: low-emphasis normal, clear hover/pressed/active, disabled opacity, no decorative page styling."
    - "D-09: MenuBar-spawned menus resolve through PopupMenu type entries with explicit row states, separators, submenu arrows, check/radio icons, and accelerator readability."
    - "D-10: PopupMenu shape stays practical for editor/runtime scanning: desktop dense, readable, visible states, no excessive ornament."
    - "D-11: New popup/menu icons follow the Phase 4/6 icon contract: bespoke SVG, 32x32 reference, #FFFFFF, Godot-generated imports, no external packs."
    - "D-18: This wave advances the 37-row desktop structural scorecard for popup/menu classes."
    - "D-21: Preserve one addon-root .gd, data-only direction .tres, no Theme.clear, no per-direction scripts, and no root neocade_theme.tres."
    - "D-22: Focus entries use only official focus-capable slots and the established outer-ring recipe."
  artifacts:
    - path: "addons/neocade_theme/neocade_theme.gd"
      provides: "Window, popup/dialog, tooltip, MenuBar, and PopupMenu bindings"
    - path: "addons/neocade_theme/icons/popup_submenu.svg"
      provides: "PopupMenu submenu arrow"
---

<objective>
Complete the popup/menu structural slice: Window chrome, PopupPanel, PopupMenu, AcceptDialog, ConfirmationDialog, TooltipPanel, TooltipLabel, and MenuBar.
</objective>

<execution_context>
@C:/Users/shilo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shilo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/07-CONTEXT.md
@.planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/07-RESEARCH.md
@.planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/helpers/phase7-slot-freeze.txt
@addons/neocade_theme/neocade_theme.gd

<interfaces>
Official slots from the local probe:

- Window styleboxes: `embedded_border`, `embedded_unfocused_border`; colors: `title_color`, `title_outline_modulate`; constants: `close_h_offset`, `close_v_offset`, `resize_margin`, `title_height`, `title_outline_size`; font/font size: `title_font`, `title_font_size`; icons: `close`, `close_pressed`.
- PopupMenu styleboxes: `hover`, `labeled_separator_left`, `labeled_separator_right`, `panel`, `separator`; colors: `font_accelerator_color`, `font_color`, `font_disabled_color`, `font_hover_color`, `font_outline_color`, `font_separator_color`, `font_separator_outline_color`; constants: `gutter_compact`, `h_separation`, `icon_max_width`, `indent`, `item_end_padding`, `item_start_padding`, `outline_size`, `separator_outline_size`, `v_separation`; fonts/font sizes: `font`, `font_separator`, `font_size`, `font_separator_size`; icons: `checked`, `checked_disabled`, `radio_checked`, `radio_checked_disabled`, `radio_unchecked`, `radio_unchecked_disabled`, `submenu`, `submenu_mirrored`, `unchecked`, `unchecked_disabled`.
- MenuBar styleboxes: `disabled`, `hover`, `normal`, `pressed`; colors: `font_color`, `font_disabled_color`, `font_focus_color`, `font_hover_color`, `font_hover_pressed_color`, `font_outline_color`, `font_pressed_color`; constants: `h_separation`, `outline_size`; font/font size: `font`, `font_size`.

Font wiring rule: all Phase 7 `font` and `font_size` slots must be written with direct `set_font()` / `set_font_size()` calls in `_regenerate_theme()` after the BINDING_TABLE walk. Do not add `"font"` or `"font_size"` recipe data-types to BINDING_TABLE; the existing iterator has no font branch and would silently skip them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author and import PopupMenu submenu icons</name>
  <files>addons/neocade_theme/icons/popup_submenu.svg, addons/neocade_theme/icons/popup_submenu.svg.import, addons/neocade_theme/icons/popup_submenu_mirrored.svg, addons/neocade_theme/icons/popup_submenu_mirrored.svg.import</files>
  <action>Create simple 32x32 monochrome `#FFFFFF` submenu arrow icons matching the existing OptionButton/Tree disclosure family. Use separate mirrored SVG for RTL/submenu mirrored slot. Run Godot import so `.svg.import` files have real `uid://` values, `svg/scale=2.0`, mipmaps enabled, and normal Phase 4/6 import settings. Reuse existing check/radio/checkbox icons for checked/unchecked/radio slots.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$godot = (Get-Content '.planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/helpers/godot-cli-path.txt' -Raw).Trim(); & $godot --headless --path . --import --quit-after 2"</automated>
    <automated>powershell -NoProfile -Command "rg -n '#FFFFFF|uid://|svg/scale=2.0|mipmaps/generate=true' addons/neocade_theme/icons/popup_submenu.svg addons/neocade_theme/icons/popup_submenu.svg.import"</automated>
  </verify>
  <done>PopupMenu submenu icons exist, import cleanly, and follow the monochrome contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Window, popups, dialogs, tooltips, and MenuBar coverage</name>
  <files>addons/neocade_theme/neocade_theme.gd</files>
  <behavior>
    - Test 1: Window has all official stylebox/color/constant/font/font_size/icon slots populated.
    - Test 2: PopupPanel, AcceptDialog, ConfirmationDialog, TooltipPanel, and TooltipLabel have explicit first-class entries.
    - Test 3: TooltipLabel text slots preserve readable AA contrast and use controlled/no shadow offsets.
    - Test 4: MenuBar has all official stylebox/color/constant/font/font_size slots populated.
    - Test 5: No popup/dialog shell stylebox uses StyleBoxFlat soft shadow or texture chrome.
  </behavior>
  <action>Expand `BINDING_TABLE` and direct font calls as needed for Window, PopupPanel, AcceptDialog, ConfirmationDialog, TooltipPanel, TooltipLabel, and MenuBar. Font/font_size slots for Window (`title_font`, `title_font_size`), TooltipLabel (`font`, `font_size`), MenuBar (`font`, `font_size`), and any PopupMenu fonts touched in this wave must be explicit `set_font()` / `set_font_size()` calls in `_regenerate_theme()` outside BINDING_TABLE. Window must set `embedded_border` and `embedded_unfocused_border`, both title colors, all five constants, title font/font size, and `close`/`close_pressed` icons using existing `close.svg`. PopupPanel/AcceptDialog/ConfirmationDialog shells should use the overlay/high surface ramp with larger popup/dialog radius where existing shape helpers support it, no shadows. ConfirmationDialog must keep an explicit `BINDING_TABLE.ConfirmationDialog` key for its shell/chrome entries even if Godot inheritance would expose AcceptDialog slots at runtime. TooltipPanel should be compact; TooltipLabel should set `font_color`, `font_outline_color`, `font_shadow_color`, `outline_size`, `shadow_offset_x: 0`, `shadow_offset_y: 0`, `font`, and `font_size` with AA contrast and no offset shadow. MenuBar should complete its official colors and metrics while staying low-emphasis and desktop dense.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/helpers/_run-phase7-verify.ps1 -Stage popups-menus</automated>
  </verify>
  <done>Window, basic popup/dialog shells, tooltips, and MenuBar pass strict slot coverage and architecture checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete PopupMenu state, separator, font, constant, and icon coverage</name>
  <files>addons/neocade_theme/neocade_theme.gd, .planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/helpers/_phase7_verify_headless.gd</files>
  <behavior>
    - Test 1: PopupMenu has all official stylebox/color/constant/font/font_size/icon slots populated.
    - Test 2: Disabled check/radio/unchecked icon variants reuse the base bespoke icons with disabled tinting instead of separate art.
    - Test 3: Submenu and submenu_mirrored resolve to the new submenu SVGs.
    - Test 4: Separator and labeled separator slots are structural line styleboxes, not panel/card surfaces.
    - Test 5: Menu row metrics remain practical desktop density.
  </behavior>
  <action>Extend PopupMenu beyond the current baseline. Add `font_outline_color`, `font_separator_outline_color`, `gutter_compact`, `icon_max_width`, `indent`, `item_end_padding`, `outline_size`, and `separator_outline_size`. `font`, `font_separator`, `font_size`, and `font_separator_size` must be direct `set_font()` / `set_font_size()` calls in `_regenerate_theme()` outside BINDING_TABLE. Bind `font_separator_outline_color` to the structural `outline_color` role rather than a text role so separator outlines stay muted. Wire all ten official icon slots: checked/unchecked and disabled variants, radio checked/unchecked and disabled variants, submenu and submenu_mirrored. Keep hover visible but not loud, separator lines thin, accelerator text muted but readable, and disabled rows consistent with direction `disabled_opacity`.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/helpers/_run-phase7-verify.ps1 -Stage popups-menus</automated>
  </verify>
  <done>PopupMenu stage passes with every official 4.6.2 slot populated and no unsupported popup menu names.</done>
</task>

</tasks>

<verification>
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/helpers/_run-phase7-verify.ps1 -Stage popups-menus
```
</verification>

<success_criteria>
- COV-06 popup/menu shell coverage is substantially complete except FileDialog's dedicated icon wave.
- MenuBar and PopupMenu satisfy COV-08 menu surface coverage.
- Tooltip readability is structurally enforced.
- Popup/menu verifier stage has zero pending groups.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/07-02-SUMMARY.md`.
</output>

---
phase: 08-mobile-variant-token-block-tap-target-audit-updated-for-dyna
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/Resolve-Godot46.ps1
  - .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_run-phase8-verify.ps1
  - .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_verify_headless.gd
  - .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_tap_target_audit.gd
  - .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/phase8-mobile-contract.txt
autonomous: true
requirements:
  - MOBILE-01
  - MOBILE-02
  - MOBILE-06
  - MOBILE-08
must_haves:
  truths:
    - "D-01: Phase 8 executes inside the authorized autonomous Phases 6-8 lane without introducing a taste checkpoint."
    - "D-02: Phase 8 proceeds without resolving UD-5; real Android/iOS devices remain Phase 10 scope."
    - "D-03: The verifier enforces one concrete addon-root neocade_theme.gd plus five data-only direction .tres resources, with no mobile .tres, density resources, subclasses, root fallback .tres, _dev, or themes folder."
    - "D-04: Forced DESKTOP and MOBILE are explicit deterministic QA paths, and AUTO is a separate host-resolution smoke path."
    - "D-12: The tap-target audit helper loads a direction, forces platform=MOBILE, regenerates, and evaluates documented per-type sizing formulas."
    - "D-13: The audit classifies scorecard rows as interactive, display-only, or layout-only; it does not pad non-interactive rows into fake tap targets."
    - "D-14: The audit uses Godot-exposed theme-side evidence such as stylebox margins, font sizes, constants, row/tab/grabber metrics, and documented limitations."
    - "D-20: Phase 8 helpers follow Phase 6/7 staged verifier discipline with a full stage that fails on pending groups."
  artifacts:
    - path: ".planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_verify_headless.gd"
      provides: "Strict staged Phase 8 verifier"
    - path: ".planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_tap_target_audit.gd"
      provides: "Mobile 48px tap-target audit helper"
    - path: ".planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/phase8-mobile-contract.txt"
      provides: "Frozen Phase 8 mobile contract, direction list, platform matrix, and audit classification"
---

<objective>
Create the Phase 8 verification foundation before production mobile changes land.

Purpose: Make mobile sizing measurable and protect the dynamic architecture from accidental file/resource drift.
Output: Phase-local verifier runner, headless verifier, tap-target audit helper, and a mobile contract artifact.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/08-CONTEXT.md
@.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/08-RESEARCH.md
@.planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/helpers/_phase7_verify_headless.gd
@.planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/helpers/_run-phase7-verify.ps1
@.planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/helpers/Resolve-Godot46.ps1
@addons/neocade_theme/neocade_theme.gd

<interfaces>
Verifier stages to create: `architecture`, `platform-tokens`, `tap-targets`, `docs`, `scene-toggle`, and `full`.

The `architecture` stage must pass before production mobile edits begin. Later stages may be pending until their owner plans implement them, but `full` must fail if any pending group remains.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 8 runner and architecture verifier stage</name>
  <files>.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/Resolve-Godot46.ps1, .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_run-phase8-verify.ps1, .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_verify_headless.gd</files>
  <read_first>.planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/helpers/_run-phase7-verify.ps1, .planning/phases/07-dialogs-popups-advanced-window-popups-menubar-colorpicker-gr/helpers/_phase7_verify_headless.gd, addons/neocade_theme/neocade_theme.gd</read_first>
  <action>Copy/adapt the Phase 7 helper pattern. The runner must accept `architecture`, `platform-tokens`, `tap-targets`, `docs`, `scene-toggle`, and `full`. The architecture stage must assert: Pulse loads as `NeoCadeTheme`; production script exists; addon root contains exactly `neocade_theme.gd`; public exports equal the locked 9 names in order; no non-comment production source contains `Theme.clear(`, `.clear(`, or `set_theme(null)`; no `addons/neocade_theme/neocade_theme.tres`; no `addons/neocade_theme/neocade_mobile_theme.tres`; no addon-root per-direction `.gd`; and all five approved direction resources load as `NeoCadeTheme`, contain script linkage, include all 9 explicit exports, contain no `[sub_resource]`, and stay under 2048 bytes.</action>
  <verify>powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_run-phase8-verify.ps1 -Stage architecture</verify>
  <acceptance_criteria>
    - `_run-phase8-verify.ps1` contains all six stage names.
    - `_phase8_verify_headless.gd` contains `APPROVED_DIRECTIONS`, `EXPECTED_EXPORTS`, `ROOT_FALLBACK_PATH`, and `MOBILE_FALLBACK_PATH`.
    - The architecture stage exits 0 on the current codebase.
  </acceptance_criteria>
  <done>Architecture verifier stage is green and later stages are wired as explicit pending groups.</done>
</task>

<task type="auto">
  <name>Task 2: Add tap-target audit helper skeleton and classification table</name>
  <files>.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_tap_target_audit.gd</files>
  <read_first>.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/08-RESEARCH.md, addons/neocade_theme/neocade_theme.gd</read_first>
  <action>Create a headless SceneTree helper that loads all five directions, duplicates each theme before mutation, forces `platform = NeoCadeTheme.Platform.MOBILE`, forces `raised = false` and `raised = true` in separate passes, and evaluates an audit table. The table must classify all 37 scorecard rows as `interactive`, `display`, or `layout`, with formulas documented per row. Implement skeleton formulas for Button-family, text input, row/list/tree/menu, tabs, range/scrollbar/split handles, and limitation rows. For this foundation plan, allow unimplemented formula groups to emit `PENDING` outside the `full` verifier; do not claim pass yet.</action>
  <verify>powershell -NoProfile -Command "rg -n 'PHASE8_TAP_TARGET_AUDIT|MIN_TAP_TARGET := 48|SCORECARD_37|interactive|display|layout|Platform.MOBILE' .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_tap_target_audit.gd"</verify>
  <acceptance_criteria>
    - The audit helper defines `MIN_TAP_TARGET := 48`.
    - The helper references all five direction resources.
    - The helper has explicit scorecard classification strings: `interactive`, `display`, and `layout`.
    - The helper never creates or references `neocade_mobile_theme.tres`.
  </acceptance_criteria>
  <done>Tap-target audit helper exists with the correct direction loop, mobile forcing, and classification model.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze the Phase 8 mobile contract artifact</name>
  <files>.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/phase8-mobile-contract.txt</files>
  <read_first>.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/08-CONTEXT.md, .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/08-RESEARCH.md</read_first>
  <action>Write a concise text artifact that records: local Godot version source, five approved direction paths, forbidden resource paths, locked 9 exports, forced platform matrix (`DESKTOP`, `MOBILE`, `AUTO`), 48px tap-target floor, all verifier stage names, and the interactive/display/layout classification policy. Include the explicit note that Android density buckets are not represented by theme resources.</action>
  <verify>powershell -NoProfile -Command "rg -n '48px|DESKTOP|MOBILE|AUTO|pulse_neocade_theme|neocade_mobile_theme.tres|9 exports|density' .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/phase8-mobile-contract.txt"</verify>
  <acceptance_criteria>
    - Contract artifact names all five direction `.tres` resources.
    - Contract artifact names `neocade_mobile_theme.tres` only as forbidden.
    - Contract artifact states the 48px floor and the forced platform matrix.
  </acceptance_criteria>
  <done>Phase 8 contract artifact is present and grep-verifiable.</done>
</task>

</tasks>

<verification>
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_run-phase8-verify.ps1 -Stage architecture
```
</verification>

<success_criteria>
- Phase 8 helper runner and verifier exist.
- Architecture invariants are enforced before mobile edits.
- Tap-target audit skeleton is ready for production formulas.
- No production or direction-resource mutation happens in this foundation plan.
</success_criteria>

<output>
After completion, create `.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/08-01-SUMMARY.md`.
</output>

---
phase: 08-mobile-variant-token-block-tap-target-audit-updated-for-dyna
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - addons/neocade_theme/neocade_theme.gd
  - .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_verify_headless.gd
autonomous: true
requirements:
  - MOBILE-01
  - MOBILE-03
  - MOBILE-04
  - MOBILE-08
must_haves:
  truths:
    - "D-04: Forced DESKTOP, forced MOBILE, and host AUTO are verified separately so Phase 8 results do not depend on the developer machine."
    - "D-05: Platform mode affects sizing, spacing, typography scale, and minimum target constants only; palette, direction personality, semantic colors, icon vocabulary, and corner radii remain stable."
    - "D-06: Any AUTO resolver hardening remains Godot-feature based and does not read DisplayServer scale, system DPI, native platform APIs, JavaScript bridges, or EditorSettings."
    - "D-08: Mobile type direction is preserved unless local verification proves a mismatch: body 16px, label/caption 14px, kicker 13px, and documented heading values."
    - "D-09: Mobile spacing grows through densityScale=1.5 and tapPadding=12; integer stroke and focus widths stay crisp."
    - "D-10: Corner radii do not scale between desktop and mobile."
    - "D-11: Raised mode remains orthogonal to platform mode, with no stale state or entry loss across raised/platform toggles."
  artifacts:
    - path: "addons/neocade_theme/neocade_theme.gd"
      provides: "Forced platform mobile token branch and additive regeneration behavior"
    - path: ".planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_verify_headless.gd"
      provides: "Platform token, typography, spacing, radius, and raised/platform orthogonality assertions"
  key_links:
    - from: "addons/neocade_theme/neocade_theme.gd"
      to: ".planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_verify_headless.gd"
      via: "Verifier loads directions, mutates platform/raised exports, and checks regenerated Theme entries"
      pattern: "Platform\\.MOBILE|Platform\\.DESKTOP|Platform\\.AUTO"
---

<objective>
Harden and verify the platform token branch in the existing dynamic theme generator.

Purpose: Make desktop/mobile/AUTO deterministic and prove mobile changes are ergonomic sizing changes, not identity changes.
Output: Production token corrections as needed plus a passing `platform-tokens` verifier stage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/08-CONTEXT.md
@.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/08-RESEARCH.md
@.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/08-01-SUMMARY.md
@addons/neocade_theme/neocade_theme.gd
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Add forced platform and typography assertions</name>
  <files>.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_verify_headless.gd</files>
  <read_first>.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_verify_headless.gd, addons/neocade_theme/neocade_theme.gd</read_first>
  <action>Replace the `platform-tokens` pending group with assertions that duplicate Pulse, set `platform=DESKTOP`, then `platform=MOBILE`, then `platform=AUTO`, and verify regenerated entries. Required checks: `default_font_size` is 14 desktop and 16 mobile; `HeaderLarge` is 36 desktop and 32 mobile; `Caption` is 12 desktop and 14 mobile; `Kicker` is 12 desktop and 13 mobile; `FileDialog.thumbnail_size` is 96 desktop and 128 mobile; spacing constants using `tokens.tapPadding` grow from 8 to 12 where applicable; Button stylebox vertical content margins are larger in mobile; a representative focus ring has the same border width on both platforms; and a representative Button radius is unchanged across desktop/mobile. Also assert host `AUTO` resolves and regenerates without failure.</action>
  <verify>powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_run-phase8-verify.ps1 -Stage platform-tokens</verify>
  <acceptance_criteria>
    - The verifier contains exact expected values `14`, `16`, `36`, `32`, `12`, `13`, `96`, and `128` in the platform-token assertions.
    - The verifier explicitly mutates `theme.platform`.
    - This RED task may fail until Task 2 completes production corrections.
  </acceptance_criteria>
  <done>Verifier encodes the Phase 8 platform-token contract.</done>
</task>

<task type="auto">
  <name>Task 2 GREEN: Correct mobile token consumption without changing public exports</name>
  <files>addons/neocade_theme/neocade_theme.gd</files>
  <read_first>addons/neocade_theme/neocade_theme.gd, .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/08-RESEARCH.md</read_first>
  <action>Make the smallest production corrections needed for the `platform-tokens` stage to pass. Preserve the 9 public exports and the existing `_platform_tokens()` keys. If `_resolve_platform()` is hardened, only add Godot feature-tag checks such as `mobile`, `android`, `ios`, `web_android`, or `web_ios`; do not add DisplayServer scale/DPI/native/JavaScript/EditorSettings reads. Keep corner-radius sources independent of platform. Keep all regeneration additive with set_* calls only and no `Theme.clear()` or `.clear()` reset.</action>
  <verify>powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_run-phase8-verify.ps1 -Stage platform-tokens</verify>
  <acceptance_criteria>
    - `addons/neocade_theme/neocade_theme.gd` still has exactly 9 `@export var` lines.
    - Non-comment production source does not contain `Theme.clear(`, `.clear(`, or `set_theme(null)`.
    - No new `.gd` file appears under `addons/neocade_theme/`.
    - No root `neocade_theme.tres` or `neocade_mobile_theme.tres` appears.
  </acceptance_criteria>
  <done>`platform-tokens` stage exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Add raised/platform orthogonality checks</name>
  <files>.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_verify_headless.gd</files>
  <read_first>.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_phase8_verify_headless.gd, addons/neocade_theme/neocade_theme.gd</read_first>
  <action>Extend the `platform-tokens` stage to duplicate Pulse and verify all four forced combinations: `raised=false/platform=DESKTOP`, `raised=true/platform=DESKTOP`, `raised=false/platform=MOBILE`, `raised=true/platform=MOBILE`. Assert a raised stylebox has a positive hard offset when raised is true, a no-shadow sentinel when raised is false, mobile content margins remain larger than desktop in both raised states, and toggling platform after raised does not remove the representative Button/LineEdit/Tree entries. This proves raised/platform orthogonality without adding new exports.</action>
  <verify>powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_run-phase8-verify.ps1 -Stage platform-tokens</verify>
  <acceptance_criteria>
    - Verifier contains all four combination names or equivalent assertions.
    - Verifier checks both `raised = true` and `raised = false`.
    - Verifier checks representative entries still exist after repeated toggles.
  </acceptance_criteria>
  <done>Forced platform and raised/platform orthogonality stage is green.</done>
</task>

</tasks>

<verification>
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/helpers/_run-phase8-verify.ps1 -Stage platform-tokens
```
</verification>

<success_criteria>
- DESKTOP, MOBILE, and AUTO are deterministic verifier paths.
- Mobile typography and spacing deltas match Phase 8 context values.
- Corner radii remain platform-stable.
- Raised mode and platform mode toggle independently.
- Architecture invariants from Plan 08-01 still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/08-mobile-variant-token-block-tap-target-audit-updated-for-dyna/08-02-SUMMARY.md`.
</output>

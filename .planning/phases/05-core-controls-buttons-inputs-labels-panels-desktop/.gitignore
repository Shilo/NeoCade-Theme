# Phase 5 verifier/CLI scratch logs (regenerated by helpers; not source).
logs/

# Godot CLI provenance and resolved path are operator-specific; keep them out
# of version control. The resolver writes them on demand under helpers/.
helpers/godot-cli-path.txt
helpers/godot-cli-provenance.txt

# GODOT-CLI-MISSING.md IS committed when present (it documents a checkpoint
# state for the next agent), so it is not ignored here.

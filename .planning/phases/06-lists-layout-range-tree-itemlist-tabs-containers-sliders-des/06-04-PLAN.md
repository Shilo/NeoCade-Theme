---
phase: 06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des
plan: 04
type: execute
wave: 4
depends_on:
  - "06-03"
files_modified:
  - addons/neocade_theme/neocade_theme.gd
  - addons/neocade_theme/icons/tab_increment.svg
  - addons/neocade_theme/icons/tab_increment.svg.import
  - addons/neocade_theme/icons/tab_decrement.svg
  - addons/neocade_theme/icons/tab_decrement.svg.import
  - addons/neocade_theme/icons/tab_drop_mark.svg
  - addons/neocade_theme/icons/tab_drop_mark.svg.import
  - addons/neocade_theme/icons/tab_menu.svg
  - addons/neocade_theme/icons/tab_menu.svg.import
  - .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_phase6_verify_headless.gd
autonomous: true
requirements:
  - COV-05
  - COV-01
  - COV-09
  - TYPEVAR-06
must_haves:
  truths:
    - "D-08: TabBar and TabContainer share the same active/inactive/hover/disabled/focus tab construction wherever official slots overlap, with active tabs connected to content and `tab_focus` kept as an outer ring."
    - "D-09: Tab personality comes from existing `DIRECTION_PRESETS.shape` values such as `tab_radius`, `raised_lifts.selected_tab`, and `raised_lifts.unselected_tab`; no tab-specific public exports are added."
    - "D-10: Tab increment/decrement/menu/drop/close affordances use crisp monochrome bespoke icons in the OptionButton/SpinBox arrow family and only official Godot 4.6 slots discovered by introspection."
    - "TabBar and TabContainer share the same tab state model wherever official slots overlap."
    - "Selected tabs read as attached to the TabContainer panel; inactive tabs are quieter surface chips."
    - "Official tab navigation/menu/drop/close icons load from NeoCade assets or existing Phase 4 assets."
    - "`tab_focus` is an outer focus ring; no unsupported tab constants such as `tab_separation` are bound."
  artifacts:
    - path: "addons/neocade_theme/neocade_theme.gd"
      provides: "Shared TabBar/TabContainer tab recipes and official slot wiring"
    - path: "addons/neocade_theme/icons/tab_increment.svg"
      provides: "Tab increment affordance for TabBar and TabContainer"
    - path: "addons/neocade_theme/icons/tab_decrement.svg"
      provides: "Tab decrement affordance for TabBar and TabContainer"
    - path: "addons/neocade_theme/icons/tab_menu.svg"
      provides: "TabContainer menu affordance"
  key_links:
    - from: "addons/neocade_theme/neocade_theme.gd"
      to: "TabBar and TabContainer"
      via: "shared tab stylebox recipes/helper"
      pattern: "tab_selected[\\s\\S]*shape\\.tab_radius"
    - from: "addons/neocade_theme/icons/tab_menu.svg"
      to: "TabContainer.menu"
      via: "icon recipe"
      pattern: "menu_highlight[\\s\\S]*tab_menu"
---

<objective>
Complete the tab-family polish slice: TabBar, TabContainer, shared tab states, tab strip/content-panel relationship, and official tab navigation icons.

Purpose: Tabs are a visual grammar point for NeoCade. If TabBar and TabContainer diverge, selected tabs look detached from content and the theme feels inconsistent. This plan centralizes the tab model while respecting the official 4.6.2 slot surface.
Output: TabBar/TabContainer styleboxes, colors, constants, font sizes, icons, and verifier assertions.
</objective>

<execution_context>
@C:/Users/shilo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shilo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/DESIGN_TOKENS.md
@.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/06-CONTEXT.md
@.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/06-RESEARCH.md
@.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/logs/06-research-slot-probe.log
@.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/phase6-slot-freeze.txt
@addons/neocade_theme/neocade_theme.gd

<interfaces>
Official TabBar slots:

Styleboxes: `button_highlight`, `button_pressed`, `tab_disabled`, `tab_focus`, `tab_hovered`, `tab_selected`, `tab_unselected`.

Colors: `drop_mark_color`, `font_disabled_color`, `font_hovered_color`, `font_outline_color`, `font_selected_color`, `font_unselected_color`, `icon_disabled_color`, `icon_hovered_color`, `icon_selected_color`, `icon_unselected_color`.

Constants: `h_separation`, `hover_switch_wait_msec`, `icon_max_width`, `outline_size`.

Font/font size/icons: `font`, `font_size`, `close`, `decrement`, `decrement_highlight`, `drop_mark`, `increment`, `increment_highlight`.

Official TabContainer slots:

Styleboxes: `panel`, `tab_disabled`, `tab_focus`, `tab_hovered`, `tab_selected`, `tab_unselected`, `tabbar_background`.

Colors: same tab font/icon/drop-mark colors as TabBar.

Constants: `icon_max_width`, `icon_separation`, `outline_size`, `side_margin`.

Font/font size/icons: `font`, `font_size`, `decrement`, `decrement_highlight`, `drop_mark`, `increment`, `increment_highlight`, `menu`, `menu_highlight`.

Do not bind `tab_separation`; the local 4.6.2 probe reports it missing.

Tab icon recipe mapping:

| Type | Official icon slot | SVG file |
|------|--------------------|----------|
| TabBar | `close` | existing `close.svg` |
| TabBar | `increment` | `tab_increment.svg` |
| TabBar | `increment_highlight` | `tab_increment.svg` |
| TabBar | `decrement` | `tab_decrement.svg` |
| TabBar | `decrement_highlight` | `tab_decrement.svg` |
| TabBar | `drop_mark` | `tab_drop_mark.svg` |
| TabContainer | `increment` | `tab_increment.svg` |
| TabContainer | `increment_highlight` | `tab_increment.svg` |
| TabContainer | `decrement` | `tab_decrement.svg` |
| TabContainer | `decrement_highlight` | `tab_decrement.svg` |
| TabContainer | `drop_mark` | `tab_drop_mark.svg` |
| TabContainer | `menu` | `tab_menu.svg` |
| TabContainer | `menu_highlight` | `tab_menu.svg` |

Required color/constant coverage: populate all 10 official colors for both TabBar and TabContainer (`drop_mark_color`, `font_disabled_color`, `font_hovered_color`, `font_outline_color`, `font_selected_color`, `font_unselected_color`, `icon_disabled_color`, `icon_hovered_color`, `icon_selected_color`, `icon_unselected_color`). Populate all four TabBar constants (`h_separation`, `hover_switch_wait_msec`, `icon_max_width`, `outline_size`) and all four TabContainer constants (`icon_max_width`, `icon_separation`, `outline_size`, `side_margin`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author and import tab navigation icons</name>
  <files>addons/neocade_theme/icons/tab_increment.svg, addons/neocade_theme/icons/tab_increment.svg.import, addons/neocade_theme/icons/tab_decrement.svg, addons/neocade_theme/icons/tab_decrement.svg.import, addons/neocade_theme/icons/tab_drop_mark.svg, addons/neocade_theme/icons/tab_drop_mark.svg.import, addons/neocade_theme/icons/tab_menu.svg, addons/neocade_theme/icons/tab_menu.svg.import</files>
  <action>Create 32x32 monochrome `#FFFFFF` SVGs for tab increment, decrement, drop mark, and menu per D-10. Reuse existing `addons/neocade_theme/icons/close.svg` for TabBar `close`. Highlight variants must reuse the same SVG and rely on icon color slots for hover/selected tint; do not create redundant highlight SVG files. Run Godot import and commit the generated `.svg.import` sidecars with real UIDs and Phase 4 import settings.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$godot = (Get-Content '.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/godot-cli-path.txt' -Raw).Trim(); & $godot --headless --path . --import --quit-after 2"</automated>
    <automated>powershell -NoProfile -Command "rg -n '#FFFFFF|uid://|svg/scale=2.0|mipmaps/generate=true' addons/neocade_theme/icons/tab_increment.svg addons/neocade_theme/icons/tab_increment.svg.import addons/neocade_theme/icons/tab_menu.svg addons/neocade_theme/icons/tab_menu.svg.import"</automated>
  </verify>
  <done>Tab icon SVGs and imports exist, normalize through Godot, and follow the existing monochrome contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared TabBar and TabContainer state model</name>
  <files>addons/neocade_theme/neocade_theme.gd</files>
  <behavior>
    - Test 1: Every official TabBar and TabContainer stylebox/color/constant/font_size/icon slot is populated.
    - Test 2: Overlapping tab stylebox slots use the same helper or equivalent recipes.
    - Test 3: `tab_selected` uses `shape.tab_radius` and `shape.raised_lifts.selected_tab`; `tab_unselected` uses `shape.raised_lifts.unselected_tab`.
    - Test 4: `tab_focus` is a transparent outer ring and `tab_separation` remains absent.
    - Test 5: `BINDING_TABLE.TabBar.stylebox` explicitly includes `button_highlight` and `button_pressed` as small overflow-button surfaces.
    - Test 6: `BINDING_TABLE.TabContainer.icon` explicitly includes both `menu` and `menu_highlight`, and both resolve to `tab_menu.svg`.
    - Test 7: All probed color and constant slots for TabBar and TabContainer are populated, including `font_outline_color`, icon color slots, `hover_switch_wait_msec`, `icon_max_width`, `icon_separation`, `outline_size`, and `side_margin`.
  </behavior>
  <action>Add a narrow shared tab stylebox helper or shared recipe builder only if it removes real duplication between TabBar and TabContainer. Implement `tab_selected`, `tab_unselected`, `tab_hovered`, `tab_disabled`, and `tab_focus` per D-08/D-09/D-22. Selected tabs must visually connect to `TabContainer.panel`; use `shape.tab_radius` for top corners and zero the bottom corners when needed so selected tabs do not read as detached chips. Inactive tabs stay quieter surface chips; hover increases contrast slightly; disabled dims through per-direction disabled opacity. Use existing `DIRECTION_PRESETS.shape.tab_radius`, `shape.raised_lifts.selected_tab`, and `shape.raised_lifts.unselected_tab`; do not add public exports. Add explicit `BINDING_TABLE.TabBar.stylebox` recipes for `button_highlight` and `button_pressed` as small overflow icon-button surfaces, not primary buttons. Populate all 10 official colors for both TabBar and TabContainer, including `font_outline_color` and the icon color family (`icon_disabled_color`, `icon_hovered_color`, `icon_selected_color`, `icon_unselected_color`). Populate all official constants: TabBar `h_separation`, `hover_switch_wait_msec`, `icon_max_width`, `outline_size`; TabContainer `icon_max_width`, `icon_separation`, `outline_size`, `side_margin`. Bind official icons exactly as in this plan's mapping table: TabContainer must have separate `menu` and `menu_highlight` recipe entries, both resolving to `tab_menu.svg`. If explicit tab fonts are needed, set them directly with Inter body font beside existing explicit font calls.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_run-phase6-verify.ps1 -Stage tabs</automated>
  </verify>
  <done>Tabs strict stage passes: all official TabBar/TabContainer styleboxes, colors, constants, font sizes, fonts, and icon slots are populated; `button_highlight`/`button_pressed` and `menu`/`menu_highlight` are explicitly wired; shared model verified; `tab_focus` is outer-ring; `tab_separation` is absent.</done>
</task>

<task type="auto">
  <name>Task 3: Harden tab verifier and cumulative markers</name>
  <files>.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_phase6_verify_headless.gd</files>
  <action>Make the `tabs` stage fail on missing official TabBar/TabContainer slots, divergent selected-tab recipes, missing icons, `tab_separation`, or `tab_focus` that is not a transparent outer ring. The stage must explicitly assert `button_highlight` and `button_pressed` styleboxes on TabBar; all 10 color slots on both tab types; all 4 constants on each tab type; and separate TabContainer icon recipes for `menu` and `menu_highlight`, both resolving to `tab_menu.svg`. Emit COV-05/COV-01/COV-09/TYPEVAR-06 trace markers. Keep visual screenshot verification deferred to Phase 9 per Phase 5 verifier precedent.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_run-phase6-verify.ps1 -Stage tabs</automated>
  </verify>
  <done>Stage `tabs` has zero pending groups and exits 0 only when TabBar and TabContainer satisfy the shared official slot contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG assets -> Godot import pipeline | New tab navigation icons are imported as Texture2D resources. |
| Theme resource -> Tab renderers | Generated Theme entries drive built-in TabBar and TabContainer rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | Tab slot names | mitigate | Verifier requires official 4.6.2 slots and rejects `tab_separation`. |
| T-06-04-02 | Spoofing | Selected tab state | mitigate | Shared recipes make selected/inactive/hover/disabled semantics consistent across TabBar and TabContainer. |
| T-06-04-03 | Denial of Service | Icon asset load failures | mitigate | Import command and verifier require each official icon slot to resolve to a Texture2D. |
| T-06-04-04 | Elevation of Privilege | New tab scripts | mitigate | Plan keeps all behavior inside Theme entries; no tab subclasses or scripts are added. |
</threat_model>

<verification>
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_run-phase6-verify.ps1 -Stage tabs
```
</verification>

<success_criteria>
- TabBar and TabContainer official slot coverage passes.
- Shared selected/inactive/hover/disabled/focus tab model is verifier-enforced.
- Tab navigation/menu/drop/close icons load from official slots.
- No unsupported `tab_separation` binding exists.
</success_criteria>

<output>
After completion, create `.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/06-04-SUMMARY.md`.
</output>

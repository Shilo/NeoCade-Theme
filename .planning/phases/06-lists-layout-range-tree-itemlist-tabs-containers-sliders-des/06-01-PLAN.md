---
phase: 06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - addons/neocade_theme/neocade_theme.gd
  - .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/Resolve-Godot46.ps1
  - .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_run-phase6-verify.ps1
  - .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_phase6_verify_headless.gd
  - .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_phase6_resource_saver.gd
  - .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/phase6-slot-freeze.txt
  - .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/godot-cli-path.txt
  - .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/godot-cli-provenance.txt
  - .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/GODOT-CLI-MISSING.md
autonomous: true
requirements:
  - COV-04
  - COV-05
  - COV-01
  - COV-07
  - COV-09
  - TYPEVAR-06
must_haves:
  truths:
    - "D-01: Delegated Phase 6 gray-area decisions are handled by the executor using project canon, Phase 5 patterns, official Godot slot evidence, and conservative visual judgment; no taste checkpoint is added."
    - "D-02: The foundation verifier explicitly covers all four Phase 6 gray areas: Tree/ItemList density, Tabs/Foldable headers, Range/Scroll affordances, and Container boundaries."
    - "D-19: Slot freeze and later icon wiring use Godot 4.6.2 introspection output before adding Tree, Tab, or ScrollBar affordance icons."
    - "D-20: Helper and production file discipline is enforced: one addon-root production `.gd`, Phase 6 helpers under `.planning/phases/06-.../helpers/`, and small data-only direction `.tres` resources."
    - "D-21: The Phase 6 verifier foundation includes official slot discovery/seed output so Tree and TabBar version-sensitive slots are backed by local Godot evidence."
    - "D-22: Focus coverage is limited to official focus slots such as `focus`, `scroll_focus`, and `tab_focus`; invented combo focus slots are rejected."
    - "Phase 6 uses exact official Godot 4.6.2 slot names from logs/06-research-slot-probe.log, not stale remembered names."
    - "Tree.hover, FoldableContainer title_hover/title_collapsed/title_font_color, and tab_separation are absent from Phase 6 production bindings unless a new probe documents changed Godot evidence."
    - "Strict Phase 6 verifier stages exist before Tree, list, tab, range, and ResourceSaver work begins."
    - "All later Phase 6 plans can verify COV-04, COV-05, COV-07, and COV-09 through the same headless Godot helper."
  artifacts:
    - path: "addons/neocade_theme/neocade_theme.gd"
      provides: "Official Phase 6 CANONICAL_SLOT_NAMES correction and stale-slot removal"
    - path: ".planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_phase6_verify_headless.gd"
      provides: "Strict staged Phase 6 verifier"
    - path: ".planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_phase6_resource_saver.gd"
      provides: "ResourceSaver round-trip and strip pass for direction .tres files"
    - path: ".planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/phase6-slot-freeze.txt"
      provides: "Human-readable official slot freeze generated from the local Godot 4.6.2 probe"
  key_links:
    - from: ".planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/logs/06-research-slot-probe.log"
      to: "addons/neocade_theme/neocade_theme.gd"
      via: "CANONICAL_SLOT_NAMES and stale-slot migration"
      pattern: "Tree.*hovered|FoldableContainer.*title_hover_panel"
    - from: ".planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_phase6_verify_headless.gd"
      to: "addons/neocade_theme/neocade_theme.gd"
      via: "Theme introspection and non-comment source scans"
      pattern: "get_stylebox_list|has_stylebox|has_icon"
---

<objective>
Create the Phase 6 execution foundation: freeze the official Godot 4.6.2 slots, remove known stale production slot names, and add strict verifier/ResourceSaver helpers for the remaining Phase 6 plans.

Purpose: Phase 6 research found current production bindings are partially stale. Tree uses invalid `hover`; FoldableContainer uses invalid `title_hover`, `title_collapsed`, and `title_font_color`; `tab_separation` appears in older notes but not in the local 4.6.2 probe. This plan prevents later polish from building on invalid slots.
Output: corrected slot freeze in `neocade_theme.gd`, generated slot-freeze artifact, staged verifier, ResourceSaver helper, and Godot CLI resolver under the Phase 6 helpers directory.
</objective>

<execution_context>
@C:/Users/shilo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shilo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/DESIGN_TOKENS.md
@.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/06-CONTEXT.md
@.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/06-RESEARCH.md
@.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/logs/06-research-slot-probe.log
@addons/neocade_theme/neocade_theme.gd
@.planning/phases/05-core-controls-buttons-inputs-labels-panels-desktop/helpers/_phase5_verify_headless.gd
@.planning/phases/05-core-controls-buttons-inputs-labels-panels-desktop/helpers/_phase5_resource_saver.gd

<interfaces>
Existing production contracts:

```gdscript
const CANONICAL_SLOT_NAMES: Dictionary = { ... }
const BINDING_TABLE: Dictionary = {
  "ThemeType": {
    "stylebox": { "slot_name": {"role": "...", "radius": "...", "padding": "..."} },
    "color": { "slot_name": {"role": "..."} },
    "constant": { "slot_name": {"value": 0} },
    "font_size": { "slot_name": {"value": "tokens.body"} },
    "icon": { "slot_name": {"icon": "filename_without_svg"} },
  }
}
func _resolve_recipe(recipe: Dictionary, data_type: String, role_table: Dictionary, tokens: Dictionary, presets: Dictionary) -> Variant
```

Official Phase 6 slot freeze from the local Godot 4.6.2 probe:

Tree styleboxes: `button_hover`, `button_pressed`, `cursor`, `cursor_unfocused`, `custom_button`, `custom_button_hover`, `custom_button_pressed`, `focus`, `hovered`, `hovered_dimmed`, `hovered_selected`, `hovered_selected_focus`, `panel`, `selected`, `selected_focus`, `title_button_hover`, `title_button_normal`, `title_button_pressed`.

FoldableContainer styleboxes: `focus`, `panel`, `title_collapsed_hover_panel`, `title_collapsed_panel`, `title_hover_panel`, `title_panel`.

FoldableContainer colors: `collapsed_font_color`, `font_color`, `font_outline_color`, `hover_font_color`.

TabBar constants: `h_separation`, `hover_switch_wait_msec`, `icon_max_width`, `outline_size`. Do not bind `tab_separation` unless this plan re-probes and records changed evidence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze official Phase 6 slots and migrate stale bindings</name>
  <files>addons/neocade_theme/neocade_theme.gd, .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/phase6-slot-freeze.txt</files>
  <behavior>
    - Test 1: `Tree.hover` is absent from production slot lists and BINDING_TABLE; `Tree.hovered` is present.
    - Test 2: FoldableContainer uses `title_hover_panel`, `title_collapsed_panel`, `title_collapsed_hover_panel`, `collapsed_font_color`, and `hover_font_color`.
    - Test 3: `tab_separation` is absent from Phase 6 canonical slots and bindings because the 4.6.2 probe reports it missing.
  </behavior>
  <action>Update `CANONICAL_SLOT_NAMES` for every Phase 6 control using only `logs/06-research-slot-probe.log` official `stylebox/color/constant/font/font_size/icon` lines per D-19/D-21. Replace stale production `Tree.hover` with `Tree.hovered` per D-03/D-04. Migrate existing FoldableContainer baseline recipes from invalid `title_hover`, `title_collapsed`, and `title_font_color` to the official 4.6.2 slots per D-11/D-22, preserving the same semantic values until Plan 03 enriches them. Remove `tab_separation` from any Phase 6 canonical slot list or binding unless you first re-run `_phase6_research_slot_probe.gd`, update the log, and document changed evidence in `phase6-slot-freeze.txt`. Keep regeneration additive: no `Theme.clear()`, no public export changes, no per-direction scripts/subclasses.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n 'Tree\.hover|\"hover\"\\s*:' addons/neocade_theme/neocade_theme.gd; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
    <automated>powershell -NoProfile -Command "rg -n 'title_hover\"|title_collapsed\"|title_font_color|tab_separation' addons/neocade_theme/neocade_theme.gd; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>`CANONICAL_SLOT_NAMES` and baseline bindings match the official 4.6.2 freeze; known stale names are gone; `phase6-slot-freeze.txt` records the source log path, engine version, and final official slot lists.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 6 verifier and Godot helper foundation</name>
  <files>.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/Resolve-Godot46.ps1, .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_run-phase6-verify.ps1, .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_phase6_verify_headless.gd, .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/godot-cli-path.txt, .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/godot-cli-provenance.txt, .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/GODOT-CLI-MISSING.md</files>
  <action>Create the Phase 6 verifier by adapting the Phase 5 strict verifier structure, not by starting a new testing style. Use delegated implementation judgment per D-01; do not add taste checkpoints. Cover every Phase 6 gray area per D-02 by supporting stages `slot-freeze`, `tree`, `itemlist-foldable`, `tabs`, `range-containers`, and `full`. The `slot-freeze` stage must assert official slot lists, known-bad stale names absent, non-comment no-`Theme.clear()` scan, addon root exactly one production `.gd`, and no public export changes. Later stages may contain explicit pending groups until their dependent plans implement them, but `full` must fail on any pending group. Add `_run-phase6-verify.ps1` to resolve Godot then call the headless helper with a `-Stage` parameter. Copy/adapt the Phase 5 resolver into the Phase 6 helper directory so execution does not depend on Phase 5-generated local path files.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_run-phase6-verify.ps1 -Stage slot-freeze</automated>
  </verify>
  <done>`_run-phase6-verify.ps1 -Stage slot-freeze` exits 0 and prints Phase 6 OK markers for slot-freeze, no stale slots, no Theme.clear, one addon-root `.gd`, and public export lock.</done>
</task>

<task type="auto">
  <name>Task 3: Add ResourceSaver round-trip helper shell</name>
  <files>.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_phase6_resource_saver.gd</files>
  <action>Copy the Phase 5 ResourceSaver/strip pattern into a Phase 6 helper with Phase 6 names and markers. It must load all five direction resources, call `ResourceSaver.save(theme, path)`, strip generated theme entries while preserving script linkage and the 9 exported values, reload each file as `NeoCadeTheme`, and assert each `.tres` remains data-only and under 2048 bytes. This plan only creates the reusable helper; Plan 05 runs it after all Phase 6 production bindings exist.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n 'PHASE6_RESOURCE_SAVER|ResourceSaver.save|_strip_theme_entries|_strip_load_steps_attr' .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_phase6_resource_saver.gd"</automated>
  </verify>
  <done>ResourceSaver helper exists with Phase 6 markers and the same strip guarantees Phase 5 verification approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Godot resource loader -> addon script | Godot loads `NeoCadeTheme` from project resources and executes `@tool` regeneration in editor/runtime. |
| Helper scripts -> local filesystem | Phase helpers inspect and save project files during verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | `neocade_theme.gd` regeneration | mitigate | Verifier scans non-comment production code for `Theme.clear()`, `.clear()`, `set_theme(null)`, and public export drift. |
| T-06-01-02 | Repudiation | Slot evidence | mitigate | `phase6-slot-freeze.txt` records engine version and the exact source log so later changes are attributable. |
| T-06-01-03 | Information Disclosure | Local Godot path files | accept | Helper path/provenance files are machine-local and contain no secrets; do not add credentials or external tokens. |
| T-06-01-04 | Denial of Service | ResourceSaver helper | mitigate | Helper only processes the five known addon direction `.tres` files and fails on unexpected missing/reload states. |
</threat_model>

<verification>
Run the slot-freeze gate before moving to Plan 02:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_run-phase6-verify.ps1 -Stage slot-freeze
```
</verification>

<success_criteria>
- Official 4.6.2 slot freeze is captured and enforced.
- Known invalid slots are absent from production bindings.
- Phase 6 verifier and ResourceSaver helpers exist under the Phase 6 helper directory.
- No addon-root helper scripts are created; addon root remains exactly one production `.gd`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/06-01-SUMMARY.md`.
</output>

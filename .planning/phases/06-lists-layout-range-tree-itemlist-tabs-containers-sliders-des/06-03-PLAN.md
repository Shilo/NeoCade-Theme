---
phase: 06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - addons/neocade_theme/neocade_theme.gd
  - .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_phase6_verify_headless.gd
autonomous: true
requirements:
  - COV-05
  - COV-01
  - COV-09
  - TYPEVAR-06
must_haves:
  truths:
    - "D-07: ItemList follows Tree's selected/focus vocabulary, with any direction-specific selected-row geometry routed through existing direction shape mechanisms instead of new public exports."
    - "D-11: FoldableContainer header chrome borrows from Tab/Panel language and reuses or mirrors Tree disclosure icons for collapsed/open affordances."
    - "ItemList has panel, selection, hover, cursor, focus, color, font-size, and separation coverage using the same selected-row vocabulary as Tree."
    - "FoldableContainer uses only official 4.6.2 title panel/color/icon slots and no stale guessed title slots."
    - "FoldableContainer disclosure affordances reuse the Tree disclosure visual language."
    - "List and foldable focus indicators use official focus slots and the Phase 5 outer-ring pattern."
  artifacts:
    - path: "addons/neocade_theme/neocade_theme.gd"
      provides: "ItemList and FoldableContainer official slot coverage"
    - path: ".planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_phase6_verify_headless.gd"
      provides: "Strict ItemList/FoldableContainer verification stage"
  key_links:
    - from: "addons/neocade_theme/neocade_theme.gd"
      to: "ItemList"
      via: "BINDING_TABLE official slot recipes"
      pattern: "\"ItemList\"[\\s\\S]*hovered_selected_focus"
    - from: "addons/neocade_theme/neocade_theme.gd"
      to: "FoldableContainer"
      via: "official title panel and disclosure icon slots"
      pattern: "title_collapsed_hover_panel|folded_arrow_mirrored"
---

<objective>
Complete the ItemList and FoldableContainer desktop polish slice after Tree establishes the shared list-selection and disclosure vocabulary.

Purpose: ItemList and FoldableContainer are smaller than Tree but share the same high-risk failure mode: stale or incomplete slot coverage silently falls back to Godot defaults. This plan gives them official 4.6.2 slot coverage while keeping layout density and header chrome conservative.
Output: ItemList selected/cursor/focus/panel coverage, FoldableContainer section-header chrome, and strict verifier assertions.
</objective>

<execution_context>
@C:/Users/shilo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shilo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/DESIGN_TOKENS.md
@.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/06-CONTEXT.md
@.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/06-RESEARCH.md
@.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/logs/06-research-slot-probe.log
@.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/phase6-slot-freeze.txt
@.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/06-02-SUMMARY.md
@addons/neocade_theme/neocade_theme.gd

<interfaces>
Official ItemList slots:

Styleboxes: `cursor`, `cursor_unfocused`, `focus`, `hovered`, `hovered_selected`, `hovered_selected_focus`, `panel`, `selected`, `selected_focus`.

Colors: `font_color`, `font_hovered_color`, `font_hovered_selected_color`, `font_outline_color`, `font_selected_color`, `guide_color`, `scroll_hint_color`.

Constants: `h_separation`, `icon_margin`, `line_separation`, `outline_size`, `v_separation`.

Font/font size/icon: `font`, `font_size`, `scroll_hint`.

Official FoldableContainer slots:

Styleboxes: `focus`, `panel`, `title_collapsed_hover_panel`, `title_collapsed_panel`, `title_hover_panel`, `title_panel`.

Colors: `collapsed_font_color`, `font_color`, `font_outline_color`, `hover_font_color`.

Constants: `h_separation`, `outline_size`.

Font/font size/icons: `font`, `font_size`, `expanded_arrow`, `expanded_arrow_mirrored`, `folded_arrow`, `folded_arrow_mirrored`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ItemList official slot coverage</name>
  <files>addons/neocade_theme/neocade_theme.gd</files>
  <behavior>
    - Test 1: Every official ItemList stylebox/color/constant/font_size/icon slot is populated on loaded NeoCade direction themes.
    - Test 2: ItemList selected, selected_focus, hovered_selected, and hovered_selected_focus use the Tree selected-row vocabulary.
    - Test 3: ItemList cursor overlays are transparent or semi-transparent and do not obscure item text.
    - Test 4: ItemList focus is the official `focus` outer ring.
  </behavior>
  <action>Extend `BINDING_TABLE.ItemList` per D-03/D-04/D-07/D-22. Reuse the Tree selected-row helper/recipe shape only if it prevents contrast or geometry drift; otherwise keep the recipes inline. `panel` should be a calm surface, `hovered` a state overlay, selected states an accent-derived surface with high-contrast text, cursor states transparent/semi-transparent, and `focus` a `focus_ring`. Use integer separation constants that preserve editor/data-view density and keep platform-token awareness where the existing resolver supports it. Wire `scroll_hint` to the existing Tree scroll-hint icon from Plan 02. If explicit `font` is needed, set it directly with the Inter body font beside existing explicit font calls rather than adding a `font` branch to BINDING_TABLE.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_run-phase6-verify.ps1 -Stage itemlist-foldable</automated>
  </verify>
  <done>ItemList verifier assertions pass for official slots, selected-row semantics, cursor alpha, and focus-ring structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FoldableContainer header and disclosure chrome</name>
  <files>addons/neocade_theme/neocade_theme.gd</files>
  <behavior>
    - Test 1: Every official FoldableContainer stylebox/color/constant/font_size/icon slot is populated.
    - Test 2: Invalid `title_hover`, `title_collapsed`, and `title_font_color` names remain absent.
    - Test 3: Foldable header chrome reads as section/tab-panel language, not push-button language.
    - Test 4: Foldable expanded/folded arrows reuse the Tree disclosure family, including mirrored variants.
  </behavior>
  <action>Replace the baseline FoldableContainer row with official 4.6.2 slots only per D-08/D-11/D-19/D-22. Use `title_panel`, `title_hover_panel`, `title_collapsed_panel`, and `title_collapsed_hover_panel` as section headers derived from tab/panel surface roles, not Button primary/secondary roles. Use `collapsed_font_color` and `hover_font_color` for title states. Wire `expanded_arrow`, `expanded_arrow_mirrored`, `folded_arrow`, and `folded_arrow_mirrored` to Plan 02 disclosure icons. Keep focus as the official `focus` outer ring. Do not create new public exports or Foldable-specific scripts.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_run-phase6-verify.ps1 -Stage itemlist-foldable</automated>
  </verify>
  <done>FoldableContainer verifier assertions pass; official title slots render; disclosure icons load; stale title slots are absent.</done>
</task>

<task type="auto">
  <name>Task 3: Harden verifier coverage for list/foldable cumulative requirements</name>
  <files>.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_phase6_verify_headless.gd</files>
  <action>Make the `itemlist-foldable` stage fail if either Control has missing official slots, stale Foldable names, opaque cursor overlays, missing focus styleboxes, or missing disclosure/scroll-hint icons. Include pass markers for COV-05, COV-01 contribution, COV-09 contribution, and TYPEVAR-06 carry-forward. Do not create Phase 8 documentation here; the marker is for traceability.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_run-phase6-verify.ps1 -Stage itemlist-foldable</automated>
  </verify>
  <done>Stage `itemlist-foldable` has zero pending groups and exits 0 only when both controls satisfy the official 4.6.2 slot contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme resource -> ItemList renderer | Generated Theme entries drive built-in ItemList selection/focus rendering. |
| Theme resource -> FoldableContainer renderer | Generated Theme entries drive built-in Foldable header and panel rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | Foldable slot names | mitigate | Verifier rejects stale guessed title slots and requires official 4.6.2 title panel/color names. |
| T-06-03-02 | Denial of Service | Opaque list cursor overlays | mitigate | Verifier checks cursor alpha so overlays do not hide content. |
| T-06-03-03 | Spoofing | Disclosure state affordance | mitigate | Foldable reuses Tree disclosure icons so expanded/collapsed state is visually consistent. |
| T-06-03-04 | Elevation of Privilege | New scripts/subclasses | mitigate | Plan forbids per-Control scripts and keeps changes inside the single `NeoCadeTheme` class. |
</threat_model>

<verification>
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/helpers/_run-phase6-verify.ps1 -Stage itemlist-foldable
```
</verification>

<success_criteria>
- ItemList official slot coverage is complete and verifier-enforced.
- FoldableContainer official slot coverage is complete with Tree-aligned disclosure icons.
- No stale Foldable title slots return.
- Focus-ring and density decisions are traceable to D-03/D-04/D-07/D-11/D-22.
</success_criteria>

<output>
After completion, create `.planning/phases/06-lists-layout-range-tree-itemlist-tabs-containers-sliders-des/06-03-SUMMARY.md`.
</output>

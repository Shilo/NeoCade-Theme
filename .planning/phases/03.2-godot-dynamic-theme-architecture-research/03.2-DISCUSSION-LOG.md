# Phase 3.2: godot-dynamic-theme-architecture-research - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-06
**Phase:** 03.2-godot-dynamic-theme-architecture-research
**Areas discussed:** Feasibility Gate, Spike Depth, Subclass Freedom, AUTO Platform

---

## Feasibility Gate

| Question | Options Considered | Selected |
|----------|--------------------|----------|
| Which validation result should count as the architecture passed? | Strict pass; Core pass; Minimum pass | Strict pass |
| If one strict-pass check fails, how should the phase classify it? | Fail architecture; Escalate to user; Patch around it | Escalate to user |
| Which checks are load-bearing enough to trigger escalation? | All strict checks; Architecture checks only; Runtime checks only | All strict checks |
| How should Phase 3.2 handle fallback design if the strict pass fails? | Document options only; Recommend one fallback; Auto-fallback | Recommend one fallback |

**Notes:** The user chose the recommended strict gate. Any failure in export regeneration, subclass super-first behavior, runtime application, serialization behavior, or platform detection triggers escalation. The phase should recommend a fallback, but not silently switch architecture.

---

## Spike Depth

| Question | Options Considered | Selected |
|----------|--------------------|----------|
| How much of the Control surface should the feasibility spike instantiate? | Representative subset; Broad skeleton; Near real theme | Representative subset |
| Which representative Controls must be in that subset? | Mechanics set; Coverage-risk set; Tiny set | Coverage-risk set |
| How real should the spike code be? | Throwaway proof; Prototype template; Production seed | Prototype template |
| What verifier should the spike include? | Manual scene only; Scripted assertions; Both | Both |

**Notes:** Required spike Controls/surfaces are `Button`, `OptionButton`, `CheckBox`, `LineEdit`, `Tree`, `PopupMenu`, `Window`, and `ScrollBar`. The spike stays under `.planning/spikes/dynamic-theme/` but should be structured like a miniature future template with visual and scripted proof.

---

## Subclass Freedom

| Question | Options Considered | Selected |
|----------|--------------------|----------|
| What should subclasses be allowed to override? | Personality hooks only; Any theme entry; Hybrid | Hybrid |
| What guardrail should prevent subclasses from breaking full coverage? | Coverage verifier required; Review only; Base owns coverage | Coverage verifier required |
| How should subclass overrides interact with mobile sizing and accessibility? | Cannot override core a11y/mobile tokens; Override with tests; Free override | Override with tests |
| How should subclass personality be represented in the architecture recipe? | Small hook API; Open cookbook; Subclass contract table | Subclass contract table |

**Notes:** Subclasses should use named hooks by default, with direct entry overrides allowed as documented exceptions. Mobile and accessibility-sensitive overrides are allowed only when verification proves tap targets and contrast still pass.

---

## AUTO Platform

| Question | Options Considered | Selected |
|----------|--------------------|----------|
| What should `platform=AUTO` do when detection is ambiguous, especially Web in a mobile browser? | Desktop fallback; Mobile-preferred fallback; Document unsupported | Mobile-preferred fallback |
| What kind of Web fallback detection should Phase 3.2 investigate first? | Godot feature tags only; JavaScript bridge; Layered strategy | Layered Godot-only strategy |
| Should consumers be able to override AUTO detection manually at runtime? | Yes, force platform; AUTO only for runtime; Expose custom detector later | Yes, force platform |
| How should Phase 3.2 validate AUTO behavior? | Local only; Local plus simulated; Full export matrix now | Local plus simulated |

**Notes:** The user specifically noted `OS.get_name()` as a likely fallback and said a JavaScript bridge is probably unnecessary unless research later proves otherwise. AUTO should try Godot-only detection first, then prefer mobile if still ambiguous.

## the agent's Discretion

- Exact recipe document structure beyond the required subclass contract table.
- Whether to include a regeneration performance note.

## Deferred Ideas

- JavaScript bridge for Web platform detection, only if Godot-only detection is insufficient.

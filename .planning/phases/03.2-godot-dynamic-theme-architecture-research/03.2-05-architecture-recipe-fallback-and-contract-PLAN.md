---
phase: 03.2-godot-dynamic-theme-architecture-research
plan: 05
type: execute
wave: 3
depends_on:
  - "04"
files_modified:
  - .planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
autonomous: true
requirements:
  - RES-NEW-03
  - RES-NEW-04
  - RES-NEW-05
must_haves:
  truths:
    - "D-13 is satisfied: if strict feasibility fails or is blocked, document fallback options and recommend one fallback for user approval; do not auto-fallback."
    - "D-17, D-18, D-19, and D-20 are satisfied through a subclass contract table covering hook-first hybrid design, documented direct overrides, required coverage verifier, mobile/a11y override tests, and forbidden APIs."
    - "D-21, D-22, D-23, and D-24 are satisfied through an AUTO platform recipe using forced modes, Godot-only layered detection, mobile-preferred fallback, and local-plus-simulated validation evidence."
    - "The architecture recipe only locks the dynamic superclass/subclass model if Plan 04 produced an all-checks PASS; otherwise it records NOT LOCKED and names the recommended fallback."
    - "The recipe is explicitly Phase 4-facing but does not edit Phase 4 code, addon files, or production `.tres` resources."
  artifacts:
    - .planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
  key_links:
    - ".planning/spikes/dynamic-theme/VERIFY-RESULTS.md"
    - ".planning/research/MD3-RESEARCH.md"
    - ".planning/research/FLAT-3D-UI-RESEARCH.md"
---

<objective>
Write the final Phase 3.2 architecture recipe and fallback recommendation based on the strict feasibility evidence.

This plan is the synthesis step. It translates evidence into a Phase 4 contract, or into a clearly recommended fallback if the strict gate did not fully pass.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.2-godot-dynamic-theme-architecture-research/03.2-CONTEXT.md
@.planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
@.planning/spikes/dynamic-theme/VERIFY-RESULTS.md
@.planning/research/MD3-RESEARCH.md
@.planning/research/FLAT-3D-UI-RESEARCH.md

<interfaces>
Required final sections to populate or finalize:
- `## Subclass Contract`
- `## AUTO Platform Strategy`
- `## Serialization Findings`
- `## Runtime and Editor-Time Findings`
- `## Pitfall Catalogue`
- `## Fallback Options and Recommendation`
- `## Architecture Recipe for Phase 4`

Subclass contract table columns:
- Hook/category
- Purpose
- Allowed subclass freedom
- Direct override allowed?
- Required verifier coverage
- Mobile/a11y guardrail
- Forbidden APIs

Fallback options to compare:
- dynamic `NeoCadeTheme` superclass + subclasses (only if strict gate passed)
- hybrid `@tool` generator that creates static `.tres` resources from the formula model
- older 4N static `.tres` matrix generator
- reduced dynamic model with explicit regeneration command, if evidence supports it
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Finalize subclass contract table</name>
  <read_first>
    - .planning/phases/03.2-godot-dynamic-theme-architecture-research/03.2-CONTEXT.md
    - .planning/spikes/dynamic-theme/VERIFY-RESULTS.md
    - .planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
  </read_first>
  <files>.planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md</files>
  <action>
    Populate `## Subclass Contract` with the required table from `<interfaces>`.

    The table must encode:
    - hook-first hybrid model
    - direct overrides only after `super._regenerate()`
    - coverage verifier required after each subclass regenerates
    - mobile/a11y-sensitive overrides allowed only with tests
    - forbidden APIs: `EditorInterface`, `EditorSettings`, `EDSCALE`, editor-only theme types as runtime dependencies
  </action>
  <verify>
    ```powershell
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "Subclass Contract"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "hook-first hybrid"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "super._regenerate"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "coverage verifier"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "tap"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "contrast"
    ```
  </verify>
  <done>Subclass contract table is final and test-backed.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize AUTO platform recipe</name>
  <read_first>
    - .planning/spikes/dynamic-theme/VERIFY-RESULTS.md
    - .planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
  </read_first>
  <files>.planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md</files>
  <action>
    Populate `## AUTO Platform Strategy` with:
    - forced `DESKTOP` and `MOBILE` escape hatch behavior
    - `AUTO` detection order: `OS.has_feature("mobile")`, Web tags such as `web_android`/`web_ios` if available, `OS.get_name()`, then mobile-preferred fallback
    - local result from Plan 04
    - expected-output table for native desktop, native mobile, Web desktop, Web mobile, and ambiguous Web
    - explicit note that a JavaScript bridge is deferred unless Godot-only detection proves insufficient
  </action>
  <verify>
    ```powershell
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "OS.has_feature"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "OS.get_name"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "web_android"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "mobile-preferred"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "JavaScript"
    ```
  </verify>
  <done>AUTO platform behavior is documented as a Phase 4 recipe with clear escape hatches.</done>
</task>

<task type="auto">
  <name>Task 3: Write fallback comparison and recommendation</name>
  <read_first>
    - .planning/spikes/dynamic-theme/VERIFY-RESULTS.md
    - .planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
    - .planning/ROADMAP.md
  </read_first>
  <files>.planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md</files>
  <action>
    Populate `## Fallback Options and Recommendation`.

    If strict gate passed:
    - recommend proceeding with dynamic `NeoCadeTheme` superclass + subclasses.
    - still document fallback options for future regression recovery.

    If any strict gate failed or was blocked:
    - set the dynamic architecture status to `NOT LOCKED`.
    - compare fallback options.
    - recommend one strongest fallback for user approval, likely a hybrid `@tool` formula generator that emits static `.tres` resources while preserving the formula/subclass design learning.
    - explicitly say user approval is required before Phase 3.3/Phase 4 assumes the fallback.
  </action>
  <verify>
    ```powershell
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "Fallback Options and Recommendation"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "NOT LOCKED"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "hybrid"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "user approval"
    ```
  </verify>
  <done>Fallback options are compared and one recommendation is named when needed.</done>
</task>

<task type="auto">
  <name>Task 4: Write Phase 4 architecture recipe</name>
  <read_first>
    - .planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
    - .planning/ROADMAP.md
    - .planning/research/MD3-RESEARCH.md
    - .planning/research/FLAT-3D-UI-RESEARCH.md
  </read_first>
  <files>.planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md</files>
  <action>
    Populate `## Architecture Recipe for Phase 4` with:
    - class/resource layout recommendation
    - export list and setter guard pattern
    - regeneration ordering
    - subclass hook API
    - coverage verifier concept
    - serialization caveats
    - raised-mode caveat from Phase 3.1/3.2 evidence
    - no-addon/no-production-change caveat for Phase 3.2

    If strict gate did not pass, make this section a conditional recipe or fallback recipe rather than an implementation instruction.
  </action>
  <verify>
    ```powershell
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "Architecture Recipe for Phase 4"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "setter"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "regeneration ordering"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "coverage verifier"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "conditional"
    ```
  </verify>
  <done>Phase 4 has a clear recipe or a clear fallback recipe, based on evidence.</done>
</task>
</tasks>

<verification>
- [ ] Subclass contract table exists.
- [ ] AUTO strategy is final and Godot-only by default.
- [ ] Fallback options and recommendation exist.
- [ ] Architecture recipe is conditional on strict-gate outcome.
- [ ] No addon implementation file changed.
</verification>

<success_criteria>
- Phase 4 will receive an evidence-backed architecture contract if feasibility passed.
- If feasibility failed or was blocked, the user receives a concrete fallback recommendation without silent architecture drift.
- D-13, D-17, D-18, D-19, D-20, D-21, D-22, D-23, and D-24 are explicitly satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-godot-dynamic-theme-architecture-research/03.2-05-SUMMARY.md` with subclass contract summary, AUTO recipe, fallback recommendation, recipe status, and verification command outputs.
</output>


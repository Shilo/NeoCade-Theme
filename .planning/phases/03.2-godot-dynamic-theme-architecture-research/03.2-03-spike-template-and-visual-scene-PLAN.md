---
phase: 03.2-godot-dynamic-theme-architecture-research
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - .planning/spikes/dynamic-theme/
  - .planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
autonomous: true
requirements:
  - RES-NEW-04
must_haves:
  truths:
    - "D-10 is implemented as a contained code spike under `.planning/spikes/dynamic-theme/`, never under `addons/neocade_theme/`."
    - "D-14 is implemented with the required coverage-risk subset: Button, OptionButton, CheckBox, LineEdit, Tree, PopupMenu, Window, and ScrollBar."
    - "D-15 is implemented as a prototype template: superclass script, good subclass script, bad subclass script, saved `.tres` resources, visual test scene, and verifier script."
    - "D-16 is prepared by including both visual scene coverage and scripted assertion hooks for required entries and the super-first negative control."
    - "D-17 begins here: the good subclass must use hook-first personality plus at least one documented direct override after `super._regenerate()`."
    - "Plan 03 is blocked on Plan 02's formula-port research: spike `_regenerate()` color/surface logic must consume the documented Passivestar Formula Port recipe instead of inventing ad-hoc color math."
  artifacts:
    - .planning/spikes/dynamic-theme/
    - .planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
  key_links:
    - ".planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md Runtime Theme API Surface"
    - ".planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md Passivestar Formula Port"
    - ".planning/research/MINIMAL-THEME-COVERAGE-DELTA.md"
    - ".planning/research/PITFALLS.md"
---

<objective>
Build the Phase 3.2 prototype-template spike that execution will use to prove or reject the dynamic theme architecture.

This plan creates research-only Godot scripts/resources under `.planning/spikes/dynamic-theme/`. It must not modify the addon implementation.

Execution precondition: Plan 02 must be complete enough that `GODOT-DYNAMIC-THEME-RESEARCH.md` contains the `## Passivestar Formula Port` recipe. If that section is missing or still placeholder-only, stop this plan and record the blocker instead of writing spike formula code.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.2-godot-dynamic-theme-architecture-research/03.2-CONTEXT.md
@.planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
@.planning/research/MINIMAL-THEME-COVERAGE-DELTA.md
@.planning/research/PITFALLS.md

<interfaces>
Required spike files, names may be adjusted if Godot import constraints require it:
- `.planning/spikes/dynamic-theme/README.md`
- `.planning/spikes/dynamic-theme/SpikeNeoCadeTheme.gd`
- `.planning/spikes/dynamic-theme/PrizePopSpikeNeoCadeTheme.gd`
- `.planning/spikes/dynamic-theme/BrokenNoSuperSpikeTheme.gd`
- `.planning/spikes/dynamic-theme/prize_pop_spike_neocade_theme.tres`
- `.planning/spikes/dynamic-theme/broken_no_super_spike_theme.tres`
- `.planning/spikes/dynamic-theme/dynamic_theme_spike.tscn`
- `.planning/spikes/dynamic-theme/verify_dynamic_theme_spike.gd`

Required Control subset:
- Button
- OptionButton
- CheckBox
- LineEdit
- Tree
- PopupMenu
- Window
- ScrollBar, covering at least HScrollBar or VScrollBar and documenting which is used

Required exported properties:
- `base_color`
- `accent_color`
- `raised`
- `platform` with `DESKTOP`, `MOBILE`, `AUTO`
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create spike directory and README contract</name>
  <read_first>
    - .planning/phases/03.2-godot-dynamic-theme-architecture-research/03.2-CONTEXT.md
    - .planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
  </read_first>
  <files>.planning/spikes/dynamic-theme/README.md</files>
  <action>
    Create `.planning/spikes/dynamic-theme/README.md` documenting:
    - research-only status
    - no-addon implementation boundary
    - file inventory
    - how to run the visual scene
    - how to run the verifier
    - strict-pass checks the spike is intended to support
    - known limitations: not full 35-Control implementation, not Phase 4 production code
    - explicit risk transfer: omitted hard controls such as `TabBar`, `TabContainer`, `ColorPicker`, and `MenuBar` are not proven by this spike and must be treated as Phase 4 coverage risks
  </action>
  <verify>
    ```powershell
    Test-Path .planning\spikes\dynamic-theme\README.md
    Select-String -Path .planning\spikes\dynamic-theme\README.md -SimpleMatch "research-only"
    Select-String -Path .planning\spikes\dynamic-theme\README.md -SimpleMatch "strict-pass"
    Select-String -Path .planning\spikes\dynamic-theme\README.md -SimpleMatch "addons/neocade_theme"
    Select-String -Path .planning\spikes\dynamic-theme\README.md -SimpleMatch "TabBar"
    Select-String -Path .planning\spikes\dynamic-theme\README.md -SimpleMatch "ColorPicker"
    ```
  </verify>
  <done>Spike README exists and clearly bounds the artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Create SpikeNeoCadeTheme superclass script</name>
  <read_first>
    - .planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
    - .planning/research/MINIMAL-THEME-DISSECTION.md
  </read_first>
  <files>.planning/spikes/dynamic-theme/SpikeNeoCadeTheme.gd</files>
  <action>
    Before writing formula code, verify that `GODOT-DYNAMIC-THEME-RESEARCH.md` has a populated `## Passivestar Formula Port` section from Plan 02. Use that recipe as the source for `_derive_surface()` / `_get_base_color`-style behavior. If the section is absent or only a placeholder, stop and document the blocker in the Plan 03 summary.

    Create a `@tool extends Theme` superclass prototype with:
    - `class_name SpikeNeoCadeTheme`
    - exported `base_color`, `accent_color`, `raised`, and `platform`
    - setter methods that update backing fields and call a guarded `_regenerate()`
    - an enum for `DESKTOP`, `MOBILE`, `AUTO`
    - `_resolve_platform()` using `OS.has_feature("mobile")`, Web-specific feature tags if available, `OS.get_name()`, and mobile-preferred ambiguity fallback
    - hook methods for surface formula parameters, radius, outline width, and direct override extension points
    - regeneration for the required Control subset only
    - a tiny timing observation hook around `_regenerate()` using Godot time APIs where practical, so Plan 04 can record rough regeneration duration

    Keep the code clear and small; it is a feasibility spike, not final implementation.
  </action>
  <verify>
    ```powershell
    Select-String -Path .planning\spikes\dynamic-theme\SpikeNeoCadeTheme.gd -SimpleMatch "@tool"
    Select-String -Path .planning\spikes\dynamic-theme\SpikeNeoCadeTheme.gd -SimpleMatch "extends Theme"
    Select-String -Path .planning\spikes\dynamic-theme\SpikeNeoCadeTheme.gd -SimpleMatch "base_color"
    Select-String -Path .planning\spikes\dynamic-theme\SpikeNeoCadeTheme.gd -SimpleMatch "accent_color"
    Select-String -Path .planning\spikes\dynamic-theme\SpikeNeoCadeTheme.gd -SimpleMatch "OS.has_feature"
    Select-String -Path .planning\spikes\dynamic-theme\SpikeNeoCadeTheme.gd -SimpleMatch "OS.get_name"
    Select-String -Path .planning\spikes\dynamic-theme\SpikeNeoCadeTheme.gd -SimpleMatch "_regenerate"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "Passivestar Formula Port"
    Select-String -Path .planning\spikes\dynamic-theme\SpikeNeoCadeTheme.gd -Pattern "Time|get_ticks|regeneration"
    ```
  </verify>
  <done>Superclass prototype exists with exports, guarded regeneration, platform resolution, and hooks.</done>
</task>

<task type="auto">
  <name>Task 3: Create good and bad subclass scripts plus saved resources</name>
  <read_first>
    - .planning/spikes/dynamic-theme/SpikeNeoCadeTheme.gd
    - .planning/phases/03.2-godot-dynamic-theme-architecture-research/03.2-CONTEXT.md
  </read_first>
  <files>
    .planning/spikes/dynamic-theme/PrizePopSpikeNeoCadeTheme.gd
    .planning/spikes/dynamic-theme/BrokenNoSuperSpikeTheme.gd
    .planning/spikes/dynamic-theme/prize_pop_spike_neocade_theme.tres
    .planning/spikes/dynamic-theme/broken_no_super_spike_theme.tres
  </files>
  <action>
    Create:
    - a good subclass that calls `super._regenerate()` first, then changes personality through hooks and at least one documented direct override.
    - a bad subclass that intentionally skips `super._regenerate()` and only sets a tiny number of Button entries.
    - saved `.tres` instances for both subclasses with representative export values.

    The bad subclass is a negative-control test artifact, not an implementation model.
  </action>
  <verify>
    ```powershell
    Select-String -Path .planning\spikes\dynamic-theme\PrizePopSpikeNeoCadeTheme.gd -SimpleMatch "super._regenerate"
    Select-String -Path .planning\spikes\dynamic-theme\BrokenNoSuperSpikeTheme.gd -SimpleMatch "intentionally"
    Test-Path .planning\spikes\dynamic-theme\prize_pop_spike_neocade_theme.tres
    Test-Path .planning\spikes\dynamic-theme\broken_no_super_spike_theme.tres
    ```
  </verify>
  <done>Good subclass, bad subclass, and saved resource instances exist.</done>
</task>

<task type="auto">
  <name>Task 4: Create visual test scene and verifier script</name>
  <read_first>
    - .planning/spikes/dynamic-theme/SpikeNeoCadeTheme.gd
    - .planning/research/MINIMAL-THEME-COVERAGE-DELTA.md
  </read_first>
  <files>
    .planning/spikes/dynamic-theme/dynamic_theme_spike.tscn
    .planning/spikes/dynamic-theme/verify_dynamic_theme_spike.gd
  </files>
  <action>
    Create a visual scene with the required Control subset and enough sample content to render meaningfully.

    Create a verifier script that can be run headlessly if Godot supports it in the local environment. The verifier must check:
    - required theme entries exist on the good subclass after regeneration
    - the bad subclass leaves expected gaps
    - local AUTO resolution returns desktop on the current Windows development machine
    - saved resource files exist and can be loaded
    - rough regeneration duration is recorded as informational performance evidence

    If a headless Godot invocation is unavailable during execution, the script still must exist and the summary must record the blocker.
  </action>
  <verify>
    ```powershell
    Test-Path .planning\spikes\dynamic-theme\dynamic_theme_spike.tscn
    Test-Path .planning\spikes\dynamic-theme\verify_dynamic_theme_spike.gd
    Select-String -Path .planning\spikes\dynamic-theme\verify_dynamic_theme_spike.gd -SimpleMatch "Button"
    Select-String -Path .planning\spikes\dynamic-theme\verify_dynamic_theme_spike.gd -SimpleMatch "OptionButton"
    Select-String -Path .planning\spikes\dynamic-theme\verify_dynamic_theme_spike.gd -SimpleMatch "CheckBox"
    Select-String -Path .planning\spikes\dynamic-theme\verify_dynamic_theme_spike.gd -SimpleMatch "LineEdit"
    Select-String -Path .planning\spikes\dynamic-theme\verify_dynamic_theme_spike.gd -SimpleMatch "Tree"
    Select-String -Path .planning\spikes\dynamic-theme\verify_dynamic_theme_spike.gd -SimpleMatch "PopupMenu"
    Select-String -Path .planning\spikes\dynamic-theme\verify_dynamic_theme_spike.gd -SimpleMatch "Window"
    Select-String -Path .planning\spikes\dynamic-theme\verify_dynamic_theme_spike.gd -SimpleMatch "ScrollBar"
    Select-String -Path .planning\spikes\dynamic-theme\verify_dynamic_theme_spike.gd -Pattern "duration|ticks|performance"
    ```
  </verify>
  <done>Visual scene and verifier script exist with the required subset and assertion targets.</done>
</task>

<task type="auto">
  <name>Task 5: Record spike construction evidence in research doc</name>
  <read_first>
    - .planning/spikes/dynamic-theme/README.md
    - .planning/spikes/dynamic-theme/SpikeNeoCadeTheme.gd
    - .planning/spikes/dynamic-theme/PrizePopSpikeNeoCadeTheme.gd
    - .planning/spikes/dynamic-theme/BrokenNoSuperSpikeTheme.gd
    - .planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md
  </read_first>
  <files>.planning/research/GODOT-DYNAMIC-THEME-RESEARCH.md</files>
  <action>
    Populate or append `## Dynamic Theme Spike Evidence` with the spike file inventory and what each file is intended to prove.

    Mark all strict-pass rows as `pending execution evidence` until Plan 04 actually runs the verifier and inspects serialization.
  </action>
  <verify>
    ```powershell
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "SpikeNeoCadeTheme.gd"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "pending execution evidence"
    Select-String -Path .planning\research\GODOT-DYNAMIC-THEME-RESEARCH.md -SimpleMatch "BrokenNoSuperSpikeTheme"
    ```
  </verify>
  <done>Research doc points to the spike inventory and clearly defers pass/fail until Plan 04.</done>
</task>
</tasks>

<verification>
- [ ] Spike directory exists.
- [ ] Superclass, good subclass, bad subclass, two `.tres` resources, visual scene, and verifier script exist.
- [ ] Required Control subset appears in the visual scene or verifier.
- [ ] Good subclass calls `super._regenerate()` first.
- [ ] Bad subclass intentionally skips `super._regenerate()`.
- [ ] No addon implementation file changed.
</verification>

<success_criteria>
- The prototype-template spike is ready for strict validation in Plan 04.
- The spike demonstrates the intended subclass freedom model without becoming Phase 4 production code.
- D-10, D-14, D-15, D-16, and D-17 are explicitly satisfied or prepared for validation.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-godot-dynamic-theme-architecture-research/03.2-03-SUMMARY.md` with spike file inventory, known blockers, and verification command outputs.
</output>

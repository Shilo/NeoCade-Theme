# Planning Docs Sync — 2026-05-13

## Scope

This pass reconciles `.planning/` documentation with the current implementation without changing production addon code.

Current implementation truth:

- One concrete `@tool class_name NeoCadeTheme extends Theme` at `addons/neocade_theme/scripts/neocade_theme.gd`.
- One canonical resource at `addons/neocade_theme/neocade_theme.tres`.
- 12 exported properties: `style`, `raised`, `platform`, 7 Style Overrides, and 2 Advanced toggles.
- Built-in styles are selected through `NeoCadeTheme.Style`; there are no per-style `.tres` resources or per-direction subclasses.
- Mobile sizing is selected through `platform`, not `neocade_mobile_theme.tres`.
- Showcase has 10 sections after Phase 13, including Role Variations.
- Live registry counts as of this sync: `BINDING_TABLE.size() == 150`, `TYPE_VARIATIONS.size() == 62`.
- Addon helper scripts include `neocade_theme_option_button.gd` and optional `neocade_theme_autoload.gd`.

## Updated Living Docs

- `STATE.md`
- `PROJECT.md`
- `REQUIREMENTS.md`
- `ROADMAP.md`
- `DESIGN_TOKENS.md`
- `MOBILE-DESIGN-SPEC.md`
- `qa/coverage-audit.md`
- Key research/design summaries under `research/`
- Current Phase 13 validation/verifier docs and helper constants

## Historical Artifacts

Completed phase plans, reviews, logs, old helper scripts, and old verification outputs under `.planning/phases/` remain historical execution records unless explicitly updated. They may mention old intermediate states such as five per-style `.tres` files, 9 showcase sections, 35/37 coverage counts, or earlier BT/TV constants. Use the living docs above for current implementation truth.
